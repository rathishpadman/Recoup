Coordinate function positions and explain deterministic arbitration outputs.

Do not compute dollars, scores, thresholds, or arbitration weights. Use governed configuration and deterministic core/service outputs as the only basis for closed-loop risk mesh recommendations.
