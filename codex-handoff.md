# Feeding Recoup v2 / O2C Design System v3.1 to Codex — Developer Handoff

This is the machine-readable UI handoff for building the **Recoup Revenue-Integrity Cockpit** with OpenAI Codex.

## Current Backend Checkpoint (2026-06-23)

Use `docs/vscode-handoff-status.md` as the detailed restart anchor. The active backend SDD-audit continuation has already closed these implementation slices with subagents/reviewers:

- Round 2 owner-input implementation complete for the supplied values: the owner supplied Risk Mesh option values, R-score component scores, and the decision-confidence threshold. Local tests cover default ranked Risk Mesh, source-owned R-score seed SQL, and decision confidence when the threshold row is present. Live Supabase read-back confirmed active `risk_mesh_cases` hash `1e85d61fd4c7faa5614d10bc371eed8a8777dd3d1a40b1b05b1baaab43556863`, `arbitration_eval_labels` hash `246b86ea6db527a4209956412a8e92bb1726dbc0d7124c6953712e88ede22a0d`, `decision_confidence_threshold` hash `a58cd791f61e56c2679c74b0263bb7fded555caae14437b2b1e68a9690c9d697`, and the four Tools_data customer R-score JSON rows. The OpenAI evidence vector store was provisioned from `.env.local`, four synthetic dossiers were uploaded, and only `OPENAI_EVIDENCE_VECTOR_STORE_ID` was written back locally; the owner-approved first-provision/reseed path is now exposed as `npm run provision:openai-evidence-vector-store`.
- Final Supabase/static-runtime cleanup: reviewer `Kepler the 2nd` re-audited the current worktree and the remaining production static-value leaks were closed. Runtime rule facts now come from Supabase `recoup_deduction_lines.rule_input_json` instead of a hard-coded Forensics rule-input switch; live migration `add_rule_input_json_to_recoup_deduction_lines` added/populated that column. The active Supabase `recoup_config.risk_mesh_cases` row now includes `containmentIntentLabel` plus Harbor risk observation criteria (`criticalAlertType`, `criticalAlertSeverity`, `citedDeductionVerdicts`) and is validated by hash `fb71aa187325b74969ebe90bf7e4553e18567cdf9bb73695c3afe212657f063c`. Sentinel/Containment/Risk Mesh selectors are governed-config/source-backed, `/memory` no longer fabricates demo records, approval/session memory no longer writes fake epoch timestamps, Realtime manifests no longer hard-code the Harbor case id, query/CFO citations derive from runtime audit/source records, and the review-only containment action id is generated from assessed customer plus cited evidence hash.
- SAP read-only retrieval hardening: `SAP_ODATA_CLIENT_ID=100` can supply the SAP Gateway client, the SAP HTTP sandbox path is accepted, and the billing service alias maps `ZUI_BILLINGDOCUMENTFS_0001` to the live `UI_BILLINGDOCUMENTFS` route. Live read-only SAP billing header retrieval was verified, and numeric `INV-*` records now plan a second GET-only `C_BillingDocumentItemFs` collection read filtered by `BillingDocument`; empty or malformed item collections do not become evidence. No ERP write path exists.
- Supabase baseline: live project `nmwfftudympcvcjtyjbf` has governed config, audit-chain table, memory, gold-set deduction rows, customers, synthetic non-SAP source tables, and the Harbor Sentinel Tools_data source rows in `payments`, `bureau_alerts`, and `deductions_backlog`. Live migration `create_remaining_tools_data_readiness_tables` added the remaining connector/readiness tables (`pod_records`, `promotions`, `contracts`, `carrier_reports`, `damage_photos`, `remittance_headers`, `remittance_lines`, `billing_requests`, `recovery_packages`, `credit_decisions`, `immutable_audit_log`) with read-only service-role grants/RLS. The Tools_data `customers` table has nullable checked `r_score_component_scores_json`; Round 2 owner-supplied component scores are live for `USCU_S04`, `USCU_L10`, `USCU_S07`, and `USCU_S03`. Runtime memory is using `recoup_memory_records`.
- Owner-answer source of truth correction: older docs mention `CODEX_BUILD_ANSWERS.md`, but that file is not present in this checkout. The current repo evidence for Day-1 owner-ratified constants is `config/governed.ts` plus the `recoup_config` seed rows in `docs/supabase-memory-schema.sql`; production calibration remains VERIFY-PROD.
- Owner input response consumed: `docs/Tools_data/CODEX_OWNER_INPUTS_RESPONSE.md` has been checked and its non-secret owner values are now stored in live Supabase `recoup_config`, not as runtime-only TypeScript constants. Migration `store_owner_release_inputs_in_recoup_config` widened the key constraint and added active `run_control`, `release_eval_label_manifest`, `intent_eval_labels`, and `arbitration_eval_labels` rows approved by `human:rathish-owner`; follow-up migration `restore_billing_risk_mesh_dimension` restored the SDD Risk Mesh function keys to `credit 0.35 / fulfillment 0.30 / billing 0.15 / collections 0.20`.
- Fresh Supabase setup now carries those non-secret release rows too: live Supabase `recoup_config` is the source of truth, `config/releaseOwnerInputs.ts` validates row schemas/hashes, and `docs/supabase-memory-schema.sql` inserts `run_control`, `release_eval_label_manifest`, `intent_eval_labels`, and `arbitration_eval_labels` into `recoup_config` for fresh projects. Runtime still reads Supabase rows and fails closed; checked-in artifacts are setup/test fixtures, not runtime fallbacks.
- Release-readiness gate is DB-backed: `evals/releaseReadinessCli.ts` loads `.env`/`.env.local`, reads Supabase `recoup_config` through `createSupabaseGovernedConfigRepositoryFromEnv(...)` and `createSupabaseReleaseOwnerInputRepositoryFromEnv(...)`, validates row hashes, and builds eval cases from deterministic code predictions. `npm.cmd run verify:release` passed after the Round 2 live Supabase updates.
- Governed runtime values are now DB-backed at service entry points: Risk Mesh, Sentinel, Partial Hold, Forensics containment, cockpit read models, approvals, Realtime tool calls, MCP advisory calls, and release readiness all receive an injected governed config snapshot. The cockpit API loads that snapshot from Supabase `recoup_config` and returns 503 when rows/credentials are missing or invalid; exported helpers fail closed with `Governed runtime config snapshot required.` rather than falling back to Day-1 code constants. Active Supabase rows are runtime authority and are structurally validated for shape, ranges, and sum-to-one invariants; checked-in Day-1 values are setup/test seed fixtures, not exact runtime locks. `src/core/` remains pure and knows only the neutral `governed-config-snapshot` basis marker, not Supabase. Risk Mesh Harbor case facts now live in the Supabase-backed `risk_mesh_cases` row, including order amount, terms text, positions, partial-hold component scores, David account readout values, Sentinel display values, the David credit action queue, and the Supabase Tools_data source refs used for Harbor R-drift (`USCU_S04`, baseline payments `90000036`/`90000060`/`INV-HARB-003`, current payment `90000085`).
- Runtime run-control is now DB-backed at the API boundary: `/healthz` loads the Supabase `recoup_config` release-owner `run_control` row through `createSupabaseReleaseOwnerInputRepositoryFromEnv(...)` and reports only the validated status/counts, while `/run` fails closed with JSON 503 before opening SSE if those rows are absent or invalid. `src/services/conductor.ts` now exposes a run-budget controller that enforces owner-configured step, retry, and token budgets from the DB-loaded snapshot; `src/agents/liveForensicsStream.ts` receives live Agents SDK `maxTurns` and retry caps from DB-loaded `forensics.stepBudget`/`retryCap`, and explicit SDK usage metadata is recorded against the DB-loaded token budget; `src/middleware/budgets.ts` accepts injected config and no longer reports only a static blocked status.
- Supabase settlement/source correction: Forensics, Risk Mesh/Sentinel service tools, MCP advisory calls, Realtime `query.answer`, release readiness, and cockpit read models now require an injected `SourcePort`. Production HTTP/MCP/CLI entry points construct that source from Supabase `recoup_customers`, `recoup_deduction_lines`, and the Harbor Tools_data tables; missing rows return 503/fail-closed instead of constructing `SyntheticSource({ seed: 42 })`. Tests still use explicit fixture sources so static data is visible as test/setup-only.
- Production SAP/docs/TPM/bureau evidence retrieval is now Supabase-backed for `/run` and MCP: `src/services/cockpitApi.ts` and `src/mcp/server.ts` preload a required evidence context from `recoup_src_sap`, `recoup_src_docs`, `recoup_src_tpm`, and `recoup_src_bureau`; `src/services/serviceLayer.ts` routes `retrieval.sap`, `retrieval.docs`, `retrieval.tpm`, and `retrieval.bureau` through that context and fails closed if the required Supabase evidence context is unavailable. A present source with no matching document returns empty evidence, and decision guardrails reject unsupported decisions. Generic source `correspondence` does not become decision evidence unless linked record IDs map it to a decision-safe credit memo. Live read-back confirmed S8 credit memo evidence (`DOC-S8-L1`) and S7 TPM evidence (`TPM-HARBOR-JUNE`).
- Frontend/MCP-callable R1 source reads are now implemented for the owner-locked SAP-primary/Supabase-fallback set: `src/services/serviceLayer.ts` exposes read-only MCP-visible `sources.r1Read`, `src/services/cockpitApi.ts` exposes `GET /sources/r1/:need`, and `src/adapters/sapOData.ts` builds metadata-validated GET read plans for the SAP-primary needs (`invoice`, `sales-order`, `credit-account-dso`, `credit-exposure`, `dispute-case`, `accrual-cap`). Locked fallback needs return Supabase authoritative read-plan envelopes instead of static business values. Reviewer `Mill the 2nd` found one unused import, and reviewer `Avicenna the 2nd` found two request-boundary issues: malformed SAP-primary params could fetch `$metadata` before validation, and extra query keys could be stripped into broader reads. All three findings are fixed: the route validates before SAP context creation, R1 schema branches are strict, per-need query allowlists reject unknown keys, and regressions cover no SAP call on malformed SAP-primary params plus extra-key rejection. Focused gates passed: `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd run test -- tests/unit/r1-source-read.test.ts tests/unit/sap-odata.test.ts tests/unit/cockpit-api.test.ts tests/invariants/mcp-visibility.test.ts tests/invariants/integration-contract.test.ts tests/invariants/readme-contract.test.ts` (`6` files / `105` tests).
- Fresh Supabase setup now creates and seeds `recoup_customers` plus `recoup_deduction_lines` in `docs/supabase-memory-schema.sql`, with read-only `service_role` grants, RLS, and row-shape tests in `tests/unit/supabase-memory.test.ts`; runtime reads these rows through `createSupabaseSettlementRunReader(...)`. The same generated setup SQL now creates the full connector/readiness table set so frontend/source connector calls can target Supabase tables rather than static code.
- Forensics stale metadata cleanup: `runForensicsInvestigation()` no longer reports `run-control-token-budget`, `run-control-step-budget`, or `run-control-retry-cap` as open run metadata because those values are governed by Supabase `recoup_config` release-owner rows at `/healthz`, `/run`, and `verify:release`. Round 2 owner input supplied the decision-confidence threshold and live Supabase read-back confirmed it; production confidence gating still needs deterministic confidence-score source evidence.
- Harbor Risk Mesh source correction: the deterministic Harbor run now cites SAP blocked order `6534`, uses order amount `$640,010.00`, computes the 55% split in core as `$352,005.50` ship / `$288,004.50` back-order, and uses owner terms `2/10 Net-30 + 25% deposit`.
- Durable Supabase audit-chain approval finality: `src/audit/supabaseTrail.ts`, `src/memory/supabaseStore.ts`, `src/services/cockpitApi.ts`, `src/services/serviceLayer.ts`, `docs/supabase-memory-schema.sql`, and focused tests now commit Supabase approval audit rows plus final approval memory through the locked `recoup_commit_approval_audit` RPC. The app reads the durable DB tail, retries once on tail mismatch, maps Postgres `P0001/audit_tail_mismatch` bodies as retryable, and records local action consumption only after the RPC succeeds.
- Audit RPC permission hardening: the generated SQL now revokes default function execution from `PUBLIC`, `anon`, `authenticated`, and `service_role` before granting `EXECUTE` back only to `service_role`; `recoup_audit_chain` is `SELECT` only for `service_role`, and the RPC accepts only the generated memory table name.
- SQLite/local approval fallback atomicity: non-Supabase durable memory now writes one final approval record containing `auditEntryHash` via append-if-absent before process-local audit/action consumption; durable duplicate responses also close the process-local approval gate so direct service calls cannot contradict a pre-existing durable decision.
- Post-approval audit receipt visibility: `/memory` now exposes sanitized `approvalAuditReceipts` for persisted SQLite/Supabase approval records so the cockpit can recover `actionId`, verified human approver, decision, status, audit hash, optional safe reason, and cited record IDs after refresh without exposing arbitrary memory payloads. Approval memory rows now preserve the underlying action evidence IDs as well as the action ID.
- Bounded R-score/R-drift deterministic core: `src/core/risk.ts` now computes R-score only from caller-supplied finite `0-100` component values using governed Day-1 weights, requires cited `recordIds`, evaluates R-drift from supplied observations, supports R1 stateless DSO-only drift without inventing dispute/risk-tier signals, and rejects undefined zero-baseline dispute-rate relative increases instead of inventing an infinity policy.
- Source-normalized Sentinel/R-drift wiring: `src/adapters/source.ts`, `src/adapters/synthetic.ts`, `src/adapters/supabaseSyntheticSource.ts`, and `src/agents/sentinel.ts` now route Sentinel through a cited source risk snapshot. The cockpit API requires a Supabase Tools_data snapshot for `/credit`, `/cfo`, and `/trace`; missing/invalid `customers`, `payments`, `bureau_alerts`, or `deductions_backlog` rows return JSON 503 instead of falling back to synthetic runtime values. Harbor R-drift computes from cited `payments.days_to_pay`, requires a matching `customers` row, cites the unresolved `bureau_alerts` tax-lien row with explicit `resolved=false`, requires invalid/partial `deductions_backlog` citations before asserting `disputeSpike`, and never reads `credit_decisions` as Sentinel R-score input. Supabase `customers.r_score_component_scores_json` is the source table field for strict JSON-number `0-100` R-score components; Round 2 owner inputs supplied component scores, local tests cover source-owned seed SQL, and live row read-back is recorded.
- Behavioral Containment action closure: `src/agents/containment.ts`, `src/agents/forensics.ts`, `src/services/approvals.ts`, and `src/services/serviceLayer.ts` now produce a HITL containment-review artifact from the single governed gaming-gate assessment. It remains `contained: false`, `no-external-action-staged`, `pending_human`, cites `recordIds`/behavioral evidence/R-score components, uses a deterministic `containment-review:<customerId>:<evidenceHash>` action id, is discoverable by `approvals.decide`, and writes a retrievable local approval audit entry when a verified human decides it.
- OpenAI evidence vector-store readiness: `.env.example` and `config/env.ts` now recognize `OPENAI_EVIDENCE_VECTOR_STORE_ID` and expose redacted `openai.fileSearch` readiness without selecting an embeddings model. `src/adapters/openAiVectorStore.ts` adds a fetch-based, read-only vector-store search adapter with Zod validation, bounded result count, metadata-only record citations, customer/scenario hit rejection, duplicate-file dedupe, and no OpenAI SDK dependency. It now enforces the owner metadata contract on every hit: `source_table`, `record_id`, `customer_id`, `scenario_type`, and `provenance='synthetic'`, with malformed/missing metadata failing closed. `src/adapters/docRepo.ts` exposes it only as an injected async path; deterministic sync `retrieval.docs` remains unchanged. The manual `npm run provision:openai-evidence-vector-store` command creates/reuses the owner-approved seed-42 store and should be run only for first provisioning or deterministic seed regeneration unless cleanup-idempotency is added later.
- Risk Mesh ranked-readiness: `src/agents/riskMesh.ts` now accepts the core `ArbitrationResult` union end-to-end when Supabase-shaped `risk_mesh_cases.harbor.arbitrationPositions` rows include source-owned `optionValue` values. Round 2 owner inputs supplied those option values, local tests cover the default ranked path plus ranked-safe query/cockpit read models, and live Supabase read-back confirms the active row has 16 option-value positions.
- Forensics injected evidence-source seam: `src/agents/forensics.ts` now exposes `runForensicsInvestigationWithEvidenceSources(...)`, an opt-in async sibling that can attach injected document evidence while sharing the same deterministic rule, verdict, routing, recovery/Billing draft, and HITL logic as the default run. The default `runForensicsInvestigation(...)` performs no env-driven live OpenAI/vector reads and no hidden static source construction; callers must provide governed config plus a settlement source snapshot, and production service contexts now pass Supabase docs/TPM/bureau evidence as well. `src/tools/retrieval/docs.ts` merges/dedupes line-relevant evidence, and `src/services/decisionTools.ts` cites attached evidence document IDs/record IDs on decisions.
- Backend guardrails/source depth: draft action HITL boundaries, amount/evidence/deterministic-basis checks, non-SAP Supabase synthetic retrieval adapters, `retrieval.bureau`, prompt files, shared decision schemas, model pins, POST `/run` SSE, live Agents SDK trace streaming, and bounded MCP-visible agent-to-agent advisory tools (`agent_tool_sentinel_position`, `agent_tool_containment_intent_position`) are implemented while core/decision/approval internals stay hidden.
- AgentHooks live-runner audit wiring: `src/agents/liveForensicsStream.ts` now registers `registerRunHookAuditReceipts(...)` on the actual OpenAI Agents SDK runner before `runner.run(...)`. `src/services/cockpitApi.ts` passes deterministic cited record IDs from the local Forensics trace into that live hook path, requires verified human cockpit auth before using a configured OpenAI key on `/run`, keeps unauthenticated `/run` deterministic-only, suppresses raw model delta text behind a fixed non-decision status, and flushes sanitized hook lifecycle receipts before a failed live stream closes.
- No-new-dependency middleware path: `src/middleware/errors.ts`, `src/middleware/logging.ts`, and `src/middleware/budgets.ts` now provide shared compact JSON body-parse errors, safe correlation ID propagation over `x-recoup-correlation-id`, and DB-injected run-budget status without adding Winston or inventing budget constants. `src/services/cockpitApi.ts` installs the shared error/correlation middleware, exposes the correlation header to cockpit clients, and refuses runtime runs when Supabase release-owner rows are missing.
- Deployment readiness: `render.yaml`, `vercel.json`, `.env.example`, and deployment-readiness invariants now cover Render API and Vercel cockpit deployment contracts. Reviewers passed after the Vercel `outputDirectory: "cockpit/.next"` fix.
- Latest focused gates after the MCP fail-closed repair and Supabase source-table status cleanup: `npm.cmd run test -- tests/unit/enterprise-connectors.test.ts tests/invariants/connector-readiness.test.ts tests/invariants/readme-contract.test.ts tests/invariants/mcp-visibility.test.ts tests/invariants/tool-permissions.test.ts tests/unit/retrieval-tools.test.ts tests/unit/supabase-memory.test.ts` passed (`7` files / `77` tests). Earlier focused gates after the Supabase table-backed runtime slice passed (`8` files / `152` tests), settlement-source correction passed (`15` files / `189` tests), and the R-score source-column focused gate passed (`91/91`). Round 2 local tests now cover default ranked Risk Mesh, source-owned R-score seed SQL, and decision confidence when the supplied threshold is present.
- Historical full gate during Round 2 implementation: `npm.cmd run verify` passed after live Supabase read-back and vector-store provisioning. Result: ESLint passed, TypeScript passed, Vitest passed (`79` files / `566` tests), Dependency Cruiser passed (`109` modules / `331` dependencies), and `verify:release` passed against live Supabase `recoup_config`.
- Backend SDD audit artifact: `docs/backend-sdd-compliance-audit.md` records closed backend evidence, owner/source-bound gaps, and the distinction between green R1 release readiness and full production SDD completion. Use it with `docs/sdd-owner-inputs-needed.md` before launching new backend subagents.
- Latest reviewer wave after the final static-runtime cleanup: `Leibniz the 2nd` found no Critical/Important owner-free backend gap, `Planck the 2nd` confirmed static synthetic IDs are setup/test/eval fixtures and not production `/run`/MCP fallbacks, `Aquinas the 2nd` rated owner-free backend R1 scope complete from repo evidence, and `Nash the 2nd` found handoff-doc staleness only. The stale `docs/vscode-handoff-status.md` proof/client/tunable wording was patched.
- Optional decision-confidence threshold plumbing: read-only reviewer `Nietzsche the 2nd` confirmed the safe owner-free slice was DB plumbing only, not inventing a threshold or confidence score. Implementer `Pauli the 2nd` added optional `decision_confidence_threshold` parsing/fetching from Supabase `recoup_config`; missing rows keep current blocked confidence/open dependency behavior, while present rows validate human approval, row hash, and `{ threshold: 0..1 }`. Live migration `allow_optional_decision_confidence_threshold_config_key` widened `recoup_config_key_check` so the optional key can be stored. Round 2 owner input supplied the threshold, local tests cover the present-threshold path, and live read-back confirmed the row/hash; deterministic confidence-score source evidence remains separate production proof.
- Expanded subagent audit after the owner challenged the active-agent count and specifically called out MCP agent-to-agent coverage: broad reviewer `Epicurus the 2nd` found no confirmed owner-free backend code gap after Round 2; MCP reviewer `Zeno the 2nd` found MCP agent-to-agent is not missing for the bounded read-only advisory scope (`agent_tool_sentinel_position` and `agent_tool_containment_intent_position`) and found no owner-free MCP gap. The SAP/table reviewer `Carson the 2nd` produced a partial checkpoint, then stalled after the final nudge and was closed; the controller used its partial findings plus local code/test evidence instead of waiting. Follow-up implementer `Bacon the 2nd` added a transport-level proof that the authenticated StreamableHTTP `tools/list` path exposes both advisory agent tools while keeping `approvals.decide` hidden; spec reviewer `Singer the 2nd` passed and code-quality reviewer `Faraday the 2nd` passed after the unnecessary sort was removed. Fresh focused verification passed `npm.cmd run test -- tests/invariants/mcp-transport.test.ts` (`1` file / `11` tests) and `npm.cmd run test -- tests/unit/agent-handoffs.test.ts tests/unit/agent-runtime.test.ts tests/invariants/mcp-visibility.test.ts tests/invariants/mcp-transport.test.ts tests/unit/sap-odata.test.ts tests/unit/retrieval-tools.test.ts tests/invariants/connector-readiness.test.ts` (`7` files / `67` tests).
- Latest full gate after the R1 source-read hardening: `npm.cmd run verify` passed. Result: ESLint passed, TypeScript passed, Vitest passed (`80` files / `578` tests), Dependency Cruiser passed (`109` modules / `333` dependencies), and `verify:release` passed against live Supabase.
- SAP scope note for future agents: SAP is configured/readable through the HTTP sandbox path and the read-only adapter has metadata-validated GET execution for billing header/items plus R1 SAP-primary read-plan calls. Production `/run` and MCP consume Supabase-backed `recoup_src_sap` evidence rows and fail closed when that evidence context is unavailable. Broader direct frontend-callable SAP entity reads beyond the R1 contract remain source-mapping work unless the corresponding service/entity/key contract is explicitly supplied and tested; do not infer "all SAP tables live-wired" from billing-header/item/R1-plan proof alone.
- Decision-confidence production activation note: reviewer `Dirac the 2nd` returned KEEP-BOUND. The threshold and formula are implemented, but production activation still requires a field-level confidence evidence contract: per-rule required fields, source-corroboration comparisons, rule exactness criteria, production field mappings/join keys/freshness/fail-closed behavior, and confirmation whether `0.40/0.30/0.30` blend weights are R1-locked or runtime-configurable. Do not treat provenance-only metadata as closure.

Closed reviewer gates for the latest backend slices: deployment readiness passed `Jason` and `Heisenberg` after the Vercel output-directory fix; durable Supabase audit-chain persistence passed `Nash` and `Mencius` after the retry and RPC-permission hardening patches; SQLite/local fallback atomicity passed `Hubble` and `McClintock` after the durable-duplicate/direct-service gate fix; bounded R-score/R-drift passed `Mendel` and `Ptolemy` after the 0-100 bounds, zero-baseline, and evidence-ID fixes; MCP advisory visibility passed `Singer` after the read-only Sentinel/Containment service-tool patch; source-normalized Sentinel/R-drift passed `Newton` after source citation and partial-field fail-closed fixes; Behavioral Containment action closure passed reviewer `Ampere` after the local audit-entry assertion was added; post-approval audit receipt visibility passed reviewer `Kuhn` with no Critical/Important findings and the suggested malformed-row sanitizer coverage was added; OpenAI vector-store readiness passed reviewer `Dewey` after citation integrity repair, and re-reviewer `Zeno` returned Ready/Yes; AgentHooks live-runner wiring passed spec reviewer `Linnaeus` after RED tests proved missing runner listeners and missing hook receipt status, reviewer `Hypatia` passed the live-stream auth/output hardening with no findings, and the low unauthenticated POST `/run` coverage note was added afterward; no-new-dependency middleware passed reviewer `Sartre` with no findings, and the low unsafe-correlation-ID coverage note was added afterward; Forensics injected evidence-source seam passed reviewer `Confucius` after default retrieval was re-locked to SAP/docs/TPM and no-live-fetch coverage was added; SAP billing item collection mapping passed reviewer `Arendt` after empty and malformed item collection evidence suppression was added; replacement reviewer `Bacon` cross-checked the historical backend gap list and found no untracked owner-free backend code gap, confirming MCP advisory tools are closed/tested for the read-only advisory scope; final release-input reviewer `Euler` found no Critical issues, confirmed the release gate is DB-backed and hash-validated, and flagged the remaining expected-ranking caveat below as Important. Latest Risk Mesh config reviewer `Cicero` found the SDD function-key mismatch and agent-as-tool validation gap; both were fixed, and re-review returned no remaining blocking issues. Latest owner-free backend reviewer `Goodall` found no Critical or Important gap after the Supabase settlement-source correction; the only note was to preserve the owner-response/full-production distinction without editing the owner-provided response. Follow-up reviewer `Nietzsche` found the R-score source-column coercion and explicit Tools_data schema gaps; both are now fixed. Reviewer `Popper` found two Important gaps, active governed config rows hard-locked to static seed values and production non-SAP retrieval still able to use static prefix-derived evidence; both are fixed and tested with DB-authoritative governed parsing plus Supabase-required `/run`/MCP evidence context. Reviewer `Volta` then found two Important MCP gaps, optional service context allowing static retrieval fallback and omitted capabilities allowing draft-only handlers; both are fixed and tested. Post-fix reviewer `Kierkegaard` found no Critical or Important owner-free backend code gap; the only doc nuance was to state that missing Supabase source context fails closed while present-but-empty source rows yield empty evidence that decision guardrails reject.

Still open / do not overclaim: the release gate owner inputs are closed and DB-backed, release evals compare owner `expectedRanking` when deterministic arbitration produces ranked options, and Harbor Risk Mesh case values, source refs, David credit display facts, Round 2 option values, R-score component rows, the decision-confidence threshold, and the owner-approved seed-42 vector-store lifecycle are now backed by live Supabase/readiness evidence. Vector reviewers `Arendt the 2nd`, `Pasteur the 2nd`, and `Helmholtz the 2nd` passed the package-command/docs hardening. Production confidence gating still needs deterministic confidence-score source evidence. Other remaining production items are broader live O2C mappings beyond the R1 SAP-primary/Supabase-fallback contract, production evidence-corpus expansion beyond seed-42, owner-confirmed embeddings/build-model/SAP sandbox verification flags, optional Winston JSON logging dependency approval for production observability, and unresolved UI/visual quality gates. Crestline is owner-locked to review-only for R1. The latest backend audit artifact does not identify a confirmed owner-free backend code gap.

```
repo root
├─ tokens.json                         ← structured design tokens
├─ tokens.css                          ← CSS custom properties, light-first + scoped dark
├─ O2C_Collections_Design_System_v3.md ← full written design spec
├─ O2C Design System v3.1.dc.html      ← visual showcase / QA reference
├─ RECONCILIATION_LEDGER.md            ← canonical business facts and gold set
├─ INVARIANTS.md                       ← release-blocking contract
├─ Recoup_v2_SDD.md                    ← technical architecture
└─ codex-handoff.md                    ← this file
```

## 1. Recommended frontend stack

- **Next.js App Router + React + TypeScript**
- **Tailwind CSS** mapped to `tokens.json`, with `tokens.css` imported once at the app root
- **Vercel AI SDK** for streamed agent/chat UI where useful
- **Radix UI** primitives for accessible drawer/dialog/menu/tooltip
- **TanStack Table** for virtualized worklists
- **Recharts** or **visx** for DSO, leakage, aging, and forecast charts
- **Phosphor Icons** (`@phosphor-icons/react`), regular weight
- Fonts: `Newsreader`, `IBM Plex Sans`, `IBM Plex Mono`

Next.js is canonical for the Recoup cockpit. Vite may be used only for an isolated token/showcase sandbox, not for the production hackathon app.

## 2. Wire the tokens first

1. Copy `tokens.css` into the Next.js app and import it once in `app/layout.tsx` or the root global CSS.
2. Map `tokens.json` into `tailwind.config.ts` (`theme.extend.colors`, `fontFamily`, `spacing`, `borderRadius`, `boxShadow`).
3. Reference variables everywhere: `background: var(--bg-surface)`, `color: var(--text-primary)`, etc.
4. Dark mode: set `data-theme="dark"` **only** on a monitoring-dashboard root, never on `<html>`.
5. Do not invent colors, fonts, spacing, radii, shadows, or status colors.

## 3. Kickoff prompt

> You are building **Recoup — Revenue-Integrity Cockpit**, an agentic Order-to-Cash product for CPG deduction recovery, credit arbitration, and CFO visibility.
>
> Read these files first, in this order: `AGENTS.md`, `INVARIANTS.md`, `RECONCILIATION_LEDGER.md` §4, `docs/Agentic_O2C_Persona_Journey_v1_2.md` (§4-§8), `Recoup_v2_SDD.md` for technical architecture, `O2C_Collections_Design_System_v3.md`, `tokens.json`, and `tokens.css`.
>
> SDD §11 is superseded for cockpit by `docs/Agentic_O2C_Persona_Journey_v1_2.md` (§4-§8); do not treat SDD §11 as the authoritative cockpit UX spec.
>
> Stack: Next.js App Router + React + TypeScript + Tailwind mapped from `tokens.json` + Vercel AI SDK + Radix UI + TanStack Table + Recharts + @phosphor-icons/react.
>
> Build exactly three surfaces for R1: **Maya Forensics cockpit**, **David Credit/Arbitration cockpit**, and **read-only CFO summary**. O2C-lead and Audit views are deferred/light.
>
> Non-negotiable product rules: no model-computed dollars, no production ERP write-back, no autonomous external action, HITL for every external action, every decision cites records and deterministic basis, and gold-set parity must remain 20 lines / $112,400; valid 7 / $32,600; recovery 13 / $79,800.
>
> Non-negotiable design rules: use tokens only; light-first; dark theme only for monitoring; amounts/IDs use IBM Plex Mono with tabular numerals; status = color + icon + text; no uppercase letter-spaced eyebrow labels; no AI purple/pink gradients; visible focus rings; reduced-motion support.

## 4. Recoup UI build order

1. **App shell** — role-aware nav for Forensics, Credit/Arbitration, CFO Summary; token wiring; responsive frame.
2. **Component primitives** — Button, StatusChip, KpiCard, Table, EvidenceDrawer, ApprovalInboxItem, RiskScore, NextBestActionPanel, Timeline, AuditTrail, Toast.
3. **Maya Forensics cockpit** — 8 scenario cards from S1–S8, ranked queue, valid/recovery totals, evidence dossier, verdict/confidence, draft recovery/Billing packets, approve/modify/reject.
4. **Evidence + HITL drawer** — POD/contract/TPM evidence tabs, record IDs, deterministic basis, amount clamp, draft-only action preview, immutable audit preview.
5. **David Credit/Arbitration cockpit** — Harbor risk drift, DSO 32→51, over-limit $640K order, partial-hold score 51.25 → 55%, ranked Risk Mesh options, draft revised terms.
6. **CFO summary** — read-only gross-to-net, margin protected, recovered/in-flight/projected-prevented leakage, DSO/CEI, what-changed narrative, cited drill-down.
7. **Conversational query surface** — text first, optional voice; answers with cited evidence from the same records shown in the cockpit.
8. **States everywhere** — loading, empty, filtered-empty, error, permission, low-confidence/escalation states.

## 5. Guardrails to repeat to Codex

- Use tokens only; flag undefined values instead of guessing.
- Use tool/service guardrails around every decision-producing and action-producing function tool, not only agent-level input/output guardrails.
- Preserve worklist filters/sort/scroll/selection across navigation.
- Every chart needs a data-table fallback and keyboard-accessible tooltips.
- Every AI suggestion/risk score must show reason codes and source records.
- Drafts are drafts: no autonomous send, no ERP mutation path, no hidden execution button.

## 6. OpenAI UI affordances for judging criterion #4

- **Streamed agent progress** → SSE/Vercel AI SDK event stream renders retrieval, rule checks, verdicts, handoffs, and HITL pauses.
- **Evidence-backed drafting** → model drafts recovery/Billing correspondence, always editable and HITL-gated.
- **Risk explanation** → model summarizes deterministic R-score/partial-hold factors; the code-computed score remains visible.
- **Conversational query** → answers CFO/Maya/David questions with cited record IDs and audit links.

Keep the reasoning visible in the UI. The winning demo should feel like a governed finance product, not a chatbot beside a dashboard.
