# Recoup VS Code Handoff Status

Date: 2026-06-23
Workspace: `C:\Rathish\Root Folder\CFO\Hackathon\Recoup1\Recoup`
Branch: `codex/guardrail-riskmesh-hardening`
Remote: `https://github.com/rathishpadman/Recoup.git`
Remote branch: `origin/codex/guardrail-riskmesh-hardening`
PR URL: not opened for this local session branch

## Current Git Status

- Active work is on local session branch `codex/guardrail-riskmesh-hardening`.
- This section contains historical pushed checkpoints as well as active continuation notes. During the current `/goal` continuation the worktree is dirty; use `git status --short`, this restart section, and fresh command output as the source of truth.
- The Windows EOL verification fix was preserved in commit `7fccd47 Preserve Windows verification newline fixes`.
- The current session's git-prioritization commits are pushed to `origin/codex/guardrail-riskmesh-hardening`; recent notable checkpoints include `e42530b Harden runtime guardrail gates`, `24ba638 Harden cockpit auth proxy boundaries`, `1e0d18a Rework cockpit persona surfaces`, `457737a Document cockpit visual handoff evidence`, `842a0f5 Update handoff with commit checkpoint`, `8953871 Record pushed branch in handoff`, `51bdaf1 Record browser skill setup in handoff`, and `0df63a3 Record browser runtime restart check`.
- Latest UI/UX journey-map evidence checkpoint `e70f856 Document cockpit journey rework mockups` was pushed to `origin/codex/guardrail-riskmesh-hardening`; it includes the deterministic mockup screenbook, exported mockup screenshots, the detailed journey/test-scenario doc, and the updated handoff checkpoint.
- Historical pre-commit verification passed before those commits: `npm.cmd run test:e2e` passed after the Next app-route env-loader fix, and `npm.cmd run verify` passed with ESLint green, TypeScript green, Vitest `67` files / `401` tests green, and Dependency Cruiser green (`97` modules / `249` dependencies). Later backend work adds a release-readiness gate, so old green `verify` entries are not current release proof.
- Safe upstream push completed with no rebase and no force-push. The first push attempt hit a transient `curl 55` connection reset and did not create the remote branch; a checked retry succeeded and set upstream tracking for `origin/codex/guardrail-riskmesh-hardening`.
- Prior pushed-branch notes are historical and no longer describe the active local branch.

## Restart-Ready Handoff - Read First

This file is the restart anchor for the next Codex session after a machine reboot. Do **not** restart broad discovery from scratch.

Current live checkpoint for the 2026-07-01 real-evidence pipeline:

- Active branch: `codex/real-evidence-phased-plan-execution` at `e3e4a55d751cbcb3ea2df4e282c3d4cb6011ac02`.
- Active plan: `docs/superpowers/plans/2026-07-01-real-evidence-phased-plan-with-audit.md`.
- Phase 0 production baseline artifacts are under `docs/audit/real-evidence-baseline/2026-07-01/`.
- Production alias inspected: `https://recoup-self-eta.vercel.app` -> Vercel deployment `dpl_GmRnve57izaS1GndBn2N1WBjTxRv`, status `Ready`.
- Baseline browser capture found 19 HTTP 200 route/state screenshots and zero visible canonical `EVD-*` IDs, zero `RECON-*` receipt IDs, and no loaded POD media/document artifact.
- Expanded route inventory includes `/forensics`, `/forensics/shadcn`, `/run`, `/credit`, `/credit/command`, `/cfo`, `/governance`, `/governance/agents`, `/governance/connectors`, `/governance/evals-finops`, `/governance/memory`, and `/governance/trace`.
- Pre-existing route health notes: `/run` logged `ERR_CONNECTION_REFUSED`; `/governance/connectors` rendered a production Server Components error; `/governance/memory` logged a Server Components production error while still rendering memory content.
- Scoped runtime diff remained empty before implementation for `src`, `cockpit`, `config`, `scripts`, `tests`, `render.yaml`, `package.json`, and `docs/supabase-memory-schema.sql`.
- No Supabase DML, Render deploy, Vercel deploy, or production mode change has been performed in this Phase 0 pass.
- Route inventory correction: use `/governance/trace` and `/governance/evals-finops`; there is no current top-level `/trace` or `/evals-finops` route.
- Implementation guard: before Phase 1 runtime code edits, keep working from this branch/worktree, record any new `HEAD`, and re-run the Phase 0 scoped-diff check. Do not apply Supabase DML, provider env changes, Render deploys, Vercel deploys, or production backfill without explicit human approval.
- Phase 1-5.5 implementation checkpoint: canonical evidence/claim/receipt schema, deterministic evidence materializer, Supabase evidence repository, reconciliation engine, reconciliation receipt repository, promoted Forensics receipt routing, freshness-gated Forensics read-model caching, canonical real-evidence refresh pipeline, and production cutover preflight are implemented locally.
- Phase 4 checkpoint: promoted Supabase Forensics reads use `recoup_deduction_claims` plus `recoup_reconciliation_receipts`; old `recoup_deduction_lines` decision-column reads are isolated in rollback-only helpers. `RECOUP_RECONCILIATION_MODE` defaults to `legacy` rollback to avoid a production outage until Phase 5.5 proves production evidence/receipt rows and a human promotes the mode.
- Phase 5 checkpoint: Express `/forensics` cached read models now compare current source/evidence/receipt freshness fingerprints before returning `x-recoup-read-model-cache: hit`; stale fingerprints rebuild and return `stale`, and unverifiable current source rows fail closed instead of serving an old model. Next `/api/forensics` delegates to backend `/forensics` and no longer serves direct Supabase cache hits before backend freshness checks.
- Post-review freshness hardening checkpoint: reviewer Copernicus found the Phase 5 fingerprint still needed canonical `recoup_evidence_documents` document-hash/retrieved/source/storage/link membership. `src/services/evidenceFreshness.ts` now includes the canonical evidence dataset in the Forensics read-model freshness record IDs, including an explicit `evidence-dataset:absent` marker for legacy/no-dataset states. `tests/unit/source-freshness.test.ts` proves an unchanged reconciliation receipt with a changed POD document content hash marks the cached Forensics model stale.
- Phase 5.5 checkpoint: `npm run refresh:real-evidence` and `npm run preflight:reconciliation-cutover` exist locally. Refresh is manual and fails closed without `RECOUP_REAL_EVIDENCE_REFRESH_APPROVED=approve-real-evidence-refresh`; it is not scheduled in `render.yaml`. The preflight reads only counts/IDs/hashes/timestamps and blocks authoritative promotion unless production has 20 claims, at least 114 evidence documents, 20 receipts, all expected evidence IDs, all expected `RECON-*` receipt IDs, matching content hashes, and a Supabase host matching `RECOUP_PRODUCTION_SUPABASE_PROJECT_REF`.
- Phase 6-6.5 checkpoint: local code now includes SSE invalidation, forwarded backend business-hash invalidation, visible stale/degraded UI wiring, canonical evidence/receipt provenance UI, the S1-S8 reconciliation matrix, mutation/sensitivity coverage, confidence/deterministic-basis assertions, fail-closed missing-evidence matrix, SAP fallback provenance coverage, and e2e/visual/a11y/shared-route hardening scripts.
- Scenario/gold-label hardening checkpoint: cockpit worklist display/API fields now use `workItemId`, `workItemLabel`, and `deductionReason`; the exposed work item ID is source-line-backed and the S1-S8 group key is kept private for grouping. Focused tests assert `scenarioId`/`scenarioLabel` do not appear in the worklist display contract.
- Missing-evidence no-publish checkpoint: `tests/unit/cockpit-api.test.ts` now proves a missing SAP/source-evidence failure returns HTTP 503 for `/forensics/refresh` and the follow-up `GET /forensics` without issuing another `POST /rest/v1/recoup_cockpit_read_models?on_conflict=model_key`; only the initial successful publish remains.
- Local rollback-continuity checkpoint: `tests/unit/reconciliation-rollout.test.ts` now covers `canary -> legacy`, `canary -> shadow`, `authoritative -> legacy`, and `authoritative -> shadow` returning `legacy_rollback` without requiring receipts. This is local continuity proof only; production canary/authoritative smoke remains pending.
- Deployment cutover manifest checkpoint: `.env.example` now documents blank reconciliation cutover controls, and `tests/invariants/deployment-readiness.test.ts` asserts Render/Vercel manifests do not commit `authoritative`/`canary` mode, canary line IDs, or a production Supabase project ref. This is repo safety proof only; live provider env promotion remains pending.
- Cutover-preflight diagnostics checkpoint: `scripts/preflightReconciliationCutover.ts` waits for all three read-only Supabase source-table probes and reports sanitized table/status/code/failure-kind values together when failures occur. Latest local target run on 2026-07-02 passes: `npm.cmd run preflight:reconciliation-cutover` returned 20 claims, 114 evidence documents, 20 reconciliation receipts, 114 evidence hashes, 20 receipt hashes, no missing required evidence/receipt IDs, and no hash mismatches. Local `.env.local` now includes the non-secret production project-ref binding, so `npm.cmd run preflight:reconciliation-cutover -- --target=production` and `npm.cmd run verify:real-evidence -- --target=production` pass by default with the same 20/114/20 proof. Provider env was not changed.
- Cutover-repair planner checkpoint: `scripts/planReconciliationCutoverRepair.ts` plus `npm.cmd run plan:reconciliation-cutover-repair` convert preflight state into a no-mutation, approval-gated repair checklist. Latest dry run exits zero with `noMutation: true`, `preflightStatus: "pass"`, `status: "ready_for_refresh_approval"`, and no repair actions; remaining gates are explicit `refresh:real-evidence` approval, preview/canary/browser/POD-media proof, and re-running production preflight at promotion time.
- Review-only SQL artifact checkpoint: `scripts/generateReconciliationCutoverSchemaRepairSql.ts` plus `npm.cmd run plan:reconciliation-cutover-sql` generate a local review artifact only. It does not connect to Supabase, writes no files, exits code 1, separates read-only verification commands from post-approval mutation/proof commands, and emits only a commented `reviewOnlySqlPlan` so DDL is non-executable by default. `tests/unit/reconciliation-cutover-sql-artifact.test.ts` covers builder safety, no DML/destructive SQL/secret strings, and CLI blocked-exit behavior. Hume and Singer re-reviewed the patched artifact and found no remaining blockers.
- Human approval packet checkpoint: `scripts/planReconciliationCutoverApprovalPacket.ts` plus `npm.cmd run plan:reconciliation-cutover-approval` run the read-only preflight/repair planner and render the current approval packet as Markdown. The command exits zero locally with `Status: ready_for_refresh_approval` and performs no provider mutation. Latest local output has no source-read failures, no blocking preflight error, no repair actions, remaining gates, approval checklist, separated read-only/post-approval commands, and the commented review-only SQL plan. Chandrasekhar and Meitner re-reviewed the earlier patched packet and found no blockers; a fresh reviewer check is requested for this updated state.
- Cutover environment readiness checkpoint: `scripts/checkReconciliationCutoverEnvironmentReadiness.ts` plus `npm.cmd run check:reconciliation-cutover-env` perform a read-only local env readiness check. It emits safe presence/host/status metadata only, blocks non-HTTPS Supabase URLs, blocks malformed or non-HTTP(S) preview URLs without echoing raw URL values, and separates production preflight readiness from refresh/preview/canary approvals. With `RECOUP_PREVIEW_URL` pointed at the new Vercel preview, the checker recognizes preview proof as `ready`; it still exits blocked because `RECOUP_REAL_EVIDENCE_REFRESH_APPROVED` is not approved. Production preflight readiness is ready. Russell and Pauli re-reviewed the patched checker and found no remaining blockers.
- Phase 7 local scaffolding checkpoint: `npm run capture:real-evidence-audit` exists and replays the Phase 0 route inventory against an approved preview or public alias to create screenshots plus a sanitized manifest. Capture manifests now include route status, sanitized console/page error snippets, sanitized origin/path URLs, visible `EVD-*`, visible `RECON-*`, visible content hashes, visible provenance terms, image `complete`, decoded dimensions, response status, content type, positive byte length, safe host/path, and loaded POD media proof. Pending placeholders exist under `docs/audit/real-evidence-post-implementation/2026-07-01/`, `docs/audit/real-evidence-preview/2026-07-01/`, and `docs/audit/real-evidence-comparison/2026-07-01.md`; they are not production proof until captured.
- Phase 7 proof-readiness checkpoint: `scripts/checkRealEvidenceProofReadiness.ts` plus `npm.cmd run check:real-evidence-proof` provide the no-mutation closeout gate. It consumes environment readiness, the Phase 0 baseline manifest, the post-implementation manifest, and the pixel-diff report, and blocks until every Phase 0 live route has post-capture coverage, screenshot name, existing screenshot file, healthy route status, sanitized/no console/page errors, and passing pixel diff. It separately requires every evidence-detail route (`selected case`, `evidence provenance drawer`, `query answer panel`, `approval audit panel`) to carry its own visible `EVD-POD-S3-L1`, visible `RECON-S3-L1`, visible content hash/provenance proof, and decoded POD image or HTTP-verified non-empty POD PDF/link proof.
- Local capture-timeout checkpoint: `scripts/captureRealEvidenceAudit.ts` now has a route-level timeout guard (`RECOUP_CAPTURE_ROUTE_TIMEOUT_MS`, default 60000ms) that records sanitized `captureError` entries instead of hanging, aborts stalled POD/media fetches, and redacts relative/absolute query params, auth headers, email-shaped values, Windows user paths, and JSON/lowercase secret assignments. The earlier local-only runs at `docs/audit/real-evidence-local/2026-07-01-local-proof/manifest.json`, `docs/audit/real-evidence-local/2026-07-01-local-proof-3/manifest.json`, and `docs/audit/real-evidence-local/2026-07-01-local-proof-4/manifest.json` are superseded by `docs/audit/real-evidence-local/2026-07-01-local-proof-5/manifest.json`. The superseding local-only manifest has 19 captures, 19 HTTP 200 captures, zero capture errors, zero console-error routes, screenshots for all 19 routes, `proofScope: "local"`, `releaseProof: false`, and clean local captures for the previously noisy `/forensics`, `/run`, and `/governance/connectors` routes. Each required Maya evidence-detail route (`selected case`, `evidence provenance drawer`, `query answer panel`, `approval audit panel`) shows `EVD-POD-S3-L1`, `EVD-REMIT-S3-L1`, `RECON-S3-L1`, 2-3 visible content hashes, `source_generated` provenance, and a visible POD document link that returned HTTP 200 `application/pdf` with positive byte length (1322 bytes for `EVD-POD-S3-L1`). This remains local-only evidence, not Phase 7 preview/production proof.
- Generated support-document proof checkpoint: after reviewer Volta confirmed the old POD path was metadata plus an HTML safe-viewer link, `scripts/captureRealEvidenceAudit.ts` was tightened so HTML safe-viewer proof is no longer counted as loaded POD media. The user then clarified that prod-ready proof documents can be generated from controlled evidence rows as long as they are not misrepresented as live production-origin files. `cockpit/app/api/forensics/evidence-documents/[evidenceId]/route.ts` now renders deterministic generated PDF artifacts for `source_generated` POD, SAP invoice, and remittance rows from their persisted evidence payloads; `src/services/cockpitModel.ts` derives backend/read-model `storageHref` values for those generated artifacts, and `src/services/evidenceMaterializer.ts` emits storage URIs for generated SAP invoice/remittance/POD records. These artifacts remain explicitly `source_generated`; they are not live SAP/3PL/remittance originals.
- Phase 7 comparison-doc checkpoint: `docs/audit/real-evidence-comparison/2026-07-01.md` now has one explicit pending row per Phase 0 live route, not a grouped shared-surface row. `tests/unit/real-evidence-comparison-doc.test.ts` parses the table by route, asserts the row-local baseline/post screenshot paths, and requires each automated pass column to remain `pending` until post-capture proof exists. Reviewer Galileo found and the controller fixed the first loose version of this test.
- Release-facing documentation honesty checkpoint: `README.md` now separates local `npm.cmd run verify` regression proof from production-release real-evidence proof, and the June Maya QA matrix is labeled historical for the 2026-06-28 production smoke. `tests/unit/real-evidence-doc-honesty.test.ts` requires the current cutover blockers to stay visible: `npm.cmd run check:real-evidence-proof`, `RECOUP_PREVIEW_URL`, `EVD-POD-S3-L1`, `RECON-S3-L1`, and decoded POD image or HTTP-verified non-empty POD PDF/link proof.
- Reviewer checkpoint: Aquinas initially failed Phase 3 S6/S7 coverage and those findings were fixed. James initially failed Phase 4 because old Supabase/cockpit runtime consumers remained, then found an approval-path receipt propagation gap; remediation cut promoted reads to claims/receipts, tightened invariants for `parsed.*` and `settlementLine.*`, added a staged rollback reader, and carries reconciliation through approval preparation.
- Latest real-evidence gates after Phase 6.5/Phase 7 scaffolding: `npm.cmd run test:reconciliation:matrix` passed 5 files / 18 tests; `npm.cmd run test -- tests/unit/realtime-next-routes.test.ts` passed 1 file / 41 tests after the generated-document `storageHref` cache fixture was corrected; `npm.cmd run test -- tests/unit/reconciliation-cutover-preflight.test.ts` passed 1 file / 6 tests; `npm.cmd run test -- tests/unit/reconciliation-cutover-repair-plan.test.ts tests/unit/reconciliation-cutover-preflight.test.ts` passed 2 files / 9 tests for the no-mutation cutover repair planner; `npm.cmd run test -- tests/unit/reconciliation-cutover-env-readiness.test.ts tests/unit/reconciliation-cutover-approval-packet.test.ts tests/unit/reconciliation-cutover-sql-artifact.test.ts tests/unit/reconciliation-cutover-repair-plan.test.ts tests/unit/reconciliation-cutover-preflight.test.ts` passed 5 files / 24 tests for the environment readiness checker, human approval packet, review-only SQL artifact, cutover planner, and preflight; `npm.cmd run test -- tests/unit/real-evidence-proof-readiness.test.ts tests/unit/real-evidence-capture-media-proof.test.ts tests/unit/reconciliation-cutover-approval-packet.test.ts tests/unit/reconciliation-cutover-sql-artifact.test.ts tests/unit/reconciliation-cutover-env-readiness.test.ts` passed 5 files / 23 tests for Phase 7 proof-readiness, capture media proof, approval packet, SQL artifact, and env readiness; `npm.cmd run test -- tests/unit/real-evidence-capture-media-proof.test.ts tests/unit/real-evidence-proof-readiness.test.ts tests/unit/real-evidence-doc-honesty.test.ts tests/unit/evidence-materializer.test.ts tests/unit/real-evidence-comparison-doc.test.ts` passed 5 files / 19 tests after reviewer Goodall's zero-byte PDF/link proof finding and the follow-up doc-honesty updates; `npm.cmd run test -- tests/unit/real-evidence-comparison-doc.test.ts tests/unit/real-evidence-proof-readiness.test.ts tests/unit/real-evidence-capture-media-proof.test.ts` passed 3 files / 9 tests for the route-by-route comparison doc and proof-readiness/capture guards; `npm.cmd run test -- tests/unit/real-evidence-browser-target.test.ts tests/unit/real-evidence-doc-honesty.test.ts tests/unit/real-evidence-comparison-doc.test.ts` passed 3 files / 11 tests for browser-target preflight, sanitized browser diagnostics, release-facing documentation honesty, and strict comparison-doc wording; latest `npm.cmd run test -- tests/unit/real-evidence-doc-honesty.test.ts` passed 1 file / 7 tests after reviewer Erdos's safe-viewer wording fix; `npm.cmd run test -- tests/unit/evidence-materializer.test.ts tests/unit/reconciliation-receipt-basis.test.ts tests/unit/reconciliation-cutover-preflight.test.ts` passed 3 files / 9 tests for local deterministic readiness, cutover-preflight wording, and all-table failure aggregation; `npm.cmd run test -- tests/unit/cockpit.test.ts tests/invariants/no-legacy-decision-columns-runtime.test.ts tests/invariants/no-premerged-rule-input-runtime.test.ts` passed 3 files / 53 tests; `npm.cmd run test -- tests/unit/cockpit-api.test.ts` passed 1 file / 121 tests including signed CFO read-proxy proof for `/evals-finops`; `npm.cmd run test -- tests/unit/reconciliation-rollout.test.ts` passed 1 file / 6 tests for local rollback continuity; `npm.cmd run test -- tests/invariants/deployment-readiness.test.ts` passed 1 file / 10 tests for deployment cutover manifest safety; `npm.cmd run typecheck` and `npm.cmd run lint` passed after the earlier Phase 7 proof-readiness reviewer fixes; `npm.cmd run verify:real-evidence` passed with 20 claims, 114 documents, no missing required evidence IDs, `proofScope=local_materialized_dataset`, `supabasePersistence=not_checked`, and `frontendMediaProof=not_checked`; default local `npm.cmd run verify:real-evidence -- --target=production` now passes with 20 claims, 114 evidence documents, and 20 receipts, without local materializer proof; `npm.cmd run test:e2e:shared-surfaces` passed locally after the evals-finops CFO read-proxy and duplicate-key fixes; `npm.cmd run test:e2e:maya-stale-state` passed locally; latest full `npm.cmd run verify` passed on 2026-07-02 with lint, typecheck, 124 Vitest files / 1038 tests, dependency-cruiser clean with 141 modules / 473 dependencies, and release readiness.
- Final resume verification checkpoint on 2026-07-02: a full `npm.cmd run verify` initially failed because `cockpit/components/maya/evidence-dossier.tsx` constructed a literal `/api/forensics/evidence-documents/*` href inside the prop-driven React boundary. The fix moved the prepared safe evidence route into backend/read-model field `storageHref`, kept the component as a prop consumer, added a cockpit model assertion, and reran the failed invariant slice successfully. After adding the Phase Gate Chronology Audit doc guard, original POD-media classifier guard, generated-PDF route, and reviewer Erdos's safe-viewer wording fix, fresh full `npm.cmd run verify` passed with lint, typecheck, 124 Vitest files / 1038 tests, dependency-cruiser clean with 141 modules / 473 dependencies, and release readiness. This was a boundary cleanup plus chronology/POD-media/documentation guard after `local-proof-4`/`local-proof-5`; no production deploy, provider env change, Supabase DML, or public alias promotion was performed.
- Final protected Preview proof checkpoint on 2026-07-02: the current final Preview tested was `https://recoup-hetylxq9o-hackathonopenai.vercel.app`, served from the preview code deployment for `430565d`. No `--prod` deploy, production alias promotion, provider env change, or Supabase DML was performed. `npm.cmd run test:e2e:shared-surfaces` passed against the final Preview, and `npm.cmd run capture:real-evidence-audit` wrote `docs/audit/real-evidence-preview/2026-07-02-430565d/manifest.json` plus 19 screenshots. The final Preview manifest is preview-scoped and has 19 captures, 19 HTTP 200 statuses, 19 screenshots, zero capture errors, zero console-error routes, visible `EVD-POD-S3-L1`, visible `RECON-S3-L1`, visible hashes/provenance, and HTTP-verified `application/pdf` POD proof on the four required Maya evidence states. The older preview `dpl_E3fhiU1v2fhX5H1ND2kD8geRHybW` and `docs/audit/real-evidence-preview/2026-07-01-preview-2/manifest.json` are superseded historical protected-login evidence, not the current preview proof.
- Final no-mutation release-proof check on 2026-07-02: `npm.cmd run check:real-evidence-proof` still exits 1 by design with `noMutation: true` for production/public-alias closeout. Baseline has 19 captures, production preflight readiness is `ready`, and final protected Preview proof exists, but `RECOUP_REAL_EVIDENCE_REFRESH_APPROVED` is not approved, the approved production/public-alias post-implementation manifest is absent, and the current visual-diff report is not backed by approved public-alias post screenshots. This is the current stop point for independent audit, merge approval, and production-deploy approval.
- Read-only production source-table proof refresh on 2026-07-02: `npm.cmd run verify:real-evidence -- --target=production` and `npm.cmd run preflight:reconciliation-cutover -- --target=production` both passed with 20 claims, 114 evidence documents, 20 reconciliation receipts, all required evidence/receipt IDs present, and no hash mismatches. A bounded read-only Supabase REST probe for `EVD-POD-S3-L1` returned one POD row with three-PL/source-generated provenance, source record `POD-S3-L1`, storage URI present, valid content hash, and retrieved timestamp present. This closes source-table existence proof; protected Preview POD/media proof is also green, while production public-alias proof remains blocked.
- Read-only provider-env check on 2026-07-02: Vercel token and project metadata are present, and the Vercel project env list has no reconciliation cutover keys (`RECOUP_RECONCILIATION_MODE`, `RECOUP_RECONCILIATION_CANARY_LINES`, `RECOUP_REAL_EVIDENCE_REFRESH_APPROVED`, `RECOUP_PREVIEW_URL`). Render MCP auto-selected the only workspace and shows `recoup-api` on branch `main`, but Render env-var reads are not exposed by the current MCP and no local `RENDER_API_KEY` / `RENDER_API_TOKEN` is present. Do not mark live provider-env authoritative-mode proof closed until backend provider env is inspected or promotion remains deliberately unconfigured by an approved release owner.
- Phase gate chronology audit on 2026-07-02: `docs/superpowers/plans/2026-07-01-real-evidence-phased-plan-with-audit.md` now has a `Phase Gate Chronology Audit` table. The final acceptance item remains unchecked because the overall chronology is PARTIAL: Phase 0 through Phase 5 have local phase-gate proof, but Phase 5.5 through Phase 7 still lack preview/provider/canary/post-capture/pixel-diff proof. The self-review wording was softened so it does not claim every phase boundary is already closed.
- Current blockers: the earlier post-Kaspersky `test:e2e:maya-real` `/forensics` 503 blocker is superseded; the current local Maya real-backend gate passed under SAP-degraded conditions with 20 canonical line items, 8 work items, 5 live query work items, and 50 backend trace rows. Local browser hardening now has fresh post-fix passes for `npm.cmd run typecheck`, the 5-file / 23-test real-evidence doc/proof/browser-target unit gate, `npm.cmd run test:e2e:forensics-sse-live-update`, `npm.cmd run test:e2e:maya-stale-state`, `npm.cmd run test:e2e:shared-surfaces`, and `npm.cmd run verify:real-evidence-a11y -- --browsers=chromium`; reviewer Mendel re-reviewed and approved the browser-hardening slice after the final diagnostics, visibility, and contrast fixes. `npm.cmd run verify:real-evidence-a11y -- --browsers=chromium` passes locally across mobile/tablet/desktop Chromium viewports by user instruction to skip cross-browser on this Windows system, but default all-browser `npm.cmd run verify:real-evidence-a11y` remains blocked because Firefox headless launch times out with a Windows graphics framebuffer error; Chromium-only is not release cross-browser proof. `npm.cmd run preflight:reconciliation-cutover` now passes for the local target with 20 claims, 114 evidence documents, 20 receipts, complete required IDs, and matching hashes. Default local production-target preflight and `verify:real-evidence -- --target=production` also pass with the same 20/114/20 proof; provider env was not changed. `npm.cmd run check:reconciliation-cutover-env` recognizes the Vercel preview URL but still blocks on missing refresh approval. Latest local-only capture `docs/audit/real-evidence-local/2026-07-01-local-proof-5/manifest.json` closes the independent audit's Maya evidence-detail visibility and media-load gap locally, and final protected Preview capture `docs/audit/real-evidence-preview/2026-07-02-430565d/manifest.json` repeats the required evidence/POD proof against the tested Preview. Fresh no-mutation `npm.cmd run check:real-evidence-proof` on 2026-07-02 still exits 1 as expected for production closeout: `RECOUP_REAL_EVIDENCE_REFRESH_APPROVED` is not approved, the approved production/public-alias post-implementation manifest is absent, and the current visual-diff report is not backed by approved public-alias post screenshots. Production release proof remains blocked until those gates are run.
- OpenAI API health checkpoint: `.env.local` contains `OPENAI_API_KEY` and `OPENAI_EVIDENCE_VECTOR_STORE_ID` presence only; a separate no-payload .NET `HttpClient` call to `https://api.openai.com/v1/models` returned HTTP 200 with JSON content and 126 model records. No key or response payload was printed.
- Reviewer Bacon found no critical auth/write-access issues in this continuation. The only reviewer finding was closed by tightening `tests/e2e/real-evidence-browser-helpers.ts`: non-2xx/3xx route responses and `/login` redirects now fail explicitly, the current header worklist and mobile row flow are supported, and missing worklist diagnostics include visible button names.
- Next restart step: independent audit/user signoff, then explicitly approved merge of `codex/real-evidence-phased-plan-execution` into `main`. After merge, production deployment remains a separate explicit approval followed by public-alias capture, production `check:real-evidence-proof`, and post-deploy smoke. Do not run production Supabase DML/backfill, enable `authoritative`, change production provider env, capture post-production proof, or run Vercel `--prod` until that approval is in place.

Current live checkpoint after user-approved continuation:

- 2026-06-23 Round 2 owner-input implementation checkpoint: the owner supplied Risk Mesh option values, R-score component scores, and the decision-confidence threshold. Local tests cover default ranked Risk Mesh, source-owned R-score seed SQL, and decision confidence when the threshold row is present. Live Supabase read-back confirmed active `risk_mesh_cases` hash `1e85d61fd4c7faa5614d10bc371eed8a8777dd3d1a40b1b05b1baaab43556863`, `arbitration_eval_labels` hash `246b86ea6db527a4209956412a8e92bb1726dbc0d7124c6953712e88ede22a0d`, `decision_confidence_threshold` hash `a58cd791f61e56c2679c74b0263bb7fded555caae14437b2b1e68a9690c9d697`, and all four Tools_data customer R-score JSON rows. OpenAI evidence vector-store provisioning created the store, uploaded four synthetic dossiers, and wrote only `OPENAI_EVIDENCE_VECTOR_STORE_ID` to `.env.local`.
- 2026-06-23 optional decision-confidence threshold plumbing checkpoint: reviewer `Nietzsche the 2nd` confirmed the safe owner-free path is DB plumbing only, not inventing a threshold or confidence score. Implementer `Pauli the 2nd` added optional `decision_confidence_threshold` support in Supabase `recoup_config`: the release owner-input loader fetches that optional row alongside the four required release rows, missing rows keep Forensics confidence visibly blocked and keep `decision-confidence-threshold` in `openDependencies`, and present rows validate human approval, row hash, and `{ threshold: 0..1 }`. Live migration `allow_optional_decision_confidence_threshold_config_key` widened `recoup_config_key_check` so the optional key can be stored when the owner supplies it, without seeding a value. Spec reviewer `Lovelace the 2nd` passed; code reviewer `Parfit the 2nd` returned Ready, and the two minor maintainability notes were fixed. Historical full verification before Round 2 owner-input implementation: `npm.cmd run verify` passed with ESLint green, TypeScript green, Vitest `79` files / `564` tests, Dependency Cruiser green (`109` modules / `327` dependencies), and live Supabase `verify:release` passed.
- 2026-06-23 final Supabase/static-runtime cleanup checkpoint: after reviewer `Kepler the 2nd` re-audited the live worktree, the remaining production static-value leaks were closed without adding code fallbacks. `recoup_config.risk_mesh_cases` now owns `containmentIntentLabel` plus Harbor risk observation criteria (`criticalAlertType`, `criticalAlertSeverity`, `citedDeductionVerdicts`); the live Supabase row was updated to hash `fb71aa187325b74969ebe90bf7e4553e18567cdf9bb73695c3afe212657f063c`. Live migration `add_rule_input_json_to_recoup_deduction_lines` added and populated `recoup_deduction_lines.rule_input_json`, so Forensics rule facts now come from Supabase rows rather than hard-coded per-rule switches. Sentinel/Containment/Risk Mesh now receive case/customer/intent selectors from governed config/source snapshots, Supabase risk criteria are applied from config, `/memory` no longer fabricates Harbor demo records, approval/session memory no longer writes fake epoch timestamps, Realtime tool manifests no longer hard-code the Harbor case ID, query/CFO citations derive from runtime audit/source records, and the review-only containment action ID is generated from the assessed customer plus cited evidence hash instead of `containment-review:CUST-CRESTLINE:M6`. Historical verification for this checkpoint passed with ESLint green, TypeScript green, Vitest `79` files / `561` tests, Dependency Cruiser green (`109` modules / `327` dependencies), and live Supabase `verify:release` passed.
- 2026-06-23 Supabase-backed runtime evidence/approval/run-control checkpoint: user clarified that runtime values must be stored/read through Supabase DB tables, not static code. The current continuation closed the remaining owner-free backend gaps found by reviewer `Avicenna`: `/forensics` now requires Supabase settlement/source service context, production `retrieval.sap` uses `recoup_src_sap` evidence rows instead of prefix-derived fallback evidence, Containment/Risk Mesh Harbor intent reads a cited source risk observation snapshot instead of the static `LEDGER-HARBOR-DISTRESSED-HONEST` record, cockpit `POST /approval` commits only through the Supabase audit chain and fails closed when durable audit persistence is unavailable, and live Agents SDK run-control no longer embeds agent-tool `maxTurns: 1` or silently defaults `retryCap` to `0`. Focused verification passed: `npm.cmd run test -- tests/unit/enterprise-connectors.test.ts tests/unit/retrieval-tools.test.ts tests/invariants/mcp-visibility.test.ts tests/unit/cockpit-api.test.ts tests/unit/cockpit.test.ts tests/unit/supabase-memory.test.ts tests/unit/live-forensics-stream.test.ts tests/unit/agent-runtime.test.ts tests/invariants/run-control.test.ts` (`9` files / `163` tests).
- 2026-06-22 backend SDD-audit continuation checkpoint: the current session is executing the user-approved `/goal` to complete open SDD backend actions with subagents and reviewer gates. Do not restart the old read-only audit from scratch; use this checkpoint plus current git status as the active source of truth.
- Closed in this continuation so far:
  - SAP retrieval hardening: `.env.local` uses `SAP_ODATA_CLIENT_ID=100` as the SAP Gateway client when `SAP_ODATA_CLIENT` is absent, SAP sandbox HTTP mode is accepted, and `ZUI_BILLINGDOCUMENTFS_0001` is aliased to the live `UI_BILLINGDOCUMENTFS` service path. Live read-only SAP probes confirmed keyed billing headers are retrievable, and numeric `INV-*` records now also plan a GET-only `C_BillingDocumentItemFs` collection read filtered by `BillingDocument`; empty or malformed item collections are suppressed instead of cited. No ERP write path was added.
  - Tool/action guardrails: all draft action paths now fail closed through HITL/action-boundary proposal checks, recovery support-doc evidence, amount clamp validation, deterministic basis, and service/tool guardrails. Recovery/Billing remains draft-only.
  - Retrieval/source depth: `retrieval.bureau` exists, non-SAP evidence source typing includes bureau/remittance/EDI/source-table provenance, and bureau/docs/remittance/EDI/TPM adapters can read synthetic Supabase source tables while preserving canonical/provenance constraints.
  - Audit-local hardening: `src/audit/trail.ts` now clones/freezes audit entries and rejects non-JSON payload shapes that `JSON.stringify` would hide.
  - Agent/runtime wiring: per-agent prompts live in `src/prompts/*.md`, shared decision schemas live in `src/types/decision.ts`, `gpt-5.4-nano` is pinned, POST `/run` streams SSE, and `src/agents/liveForensicsStream.ts` wraps the live OpenAI Agents SDK stream without exposing dollar text or secrets.
  - MCP / agent-to-agent: `riskMesh` has advisory agent-as-tool wiring for Sentinel and Containment positions, `agent_tool_sentinel_position` and `agent_tool_containment_intent_position` are MCP-visible read-only service tools, core/decision/approval internals remain hidden from MCP, and `src/services/conductor.ts` records sanitized AgentHooks audit receipts for start/end/handoff/tool lifecycle events without storing model output text.
  - Supabase live baseline: project `nmwfftudympcvcjtyjbf` has `recoup_config`, `recoup_audit_chain`, `recoup_memory_records`, `recoup_customers`, `recoup_deduction_lines`, synthetic `recoup_src_*` tables, and the Harbor Sentinel Tools_data rows in `customers`, `payments`, `bureau_alerts`, and `deductions_backlog`. Live owner-input migration `store_owner_release_inputs_in_recoup_config` added `run_control`, `release_eval_label_manifest`, `intent_eval_labels`, and `arbitration_eval_labels` rows. Live migration `store_risk_mesh_cases_in_recoup_config` added the governed `risk_mesh_cases` row for Harbor order amount, terms, positions, and partial-hold component scores; follow-up migration `restore_billing_risk_mesh_dimension` restored the SDD function keys (`credit`, `fulfillment`, `billing`, `collections`) while preserving the owner-supplied 0.35/0.30/0.15/0.20 values. Live migration `store_credit_cockpit_display_values_in_risk_mesh_cases` moved David account readout, Sentinel display, and credit action queue business values into the same Supabase-backed row. Live migration `store_sentinel_risk_observation_source_refs` added the DB-backed Harbor source refs (`USCU_S04`, baseline payments `90000036`/`90000060`/`INV-HARB-003`, current payment `90000085`), `seed_harbor_sentinel_tools_data_sources` created/seeded the minimal non-destructive Tools_data source rows, `harden_sentinel_tools_data_source_tables` added repeatable table/grant/RLS hardening, and `align_sentinel_tools_data_text_defaults` added `deductions_backlog.line_ref` plus text-ID defaults for seed-compatible source rows. Live migration `create_remaining_tools_data_readiness_tables` added `pod_records`, `promotions`, `contracts`, `carrier_reports`, `damage_photos`, `remittance_headers`, `remittance_lines`, `billing_requests`, `recovery_packages`, `credit_decisions`, and `immutable_audit_log` with read-only service-role grants/RLS. Read-back confirmed hash `f9bfe35c39d950db1d00fa57476054a5738611a311a43842d99f054c3c57b582`, 1 customer row, 4 payment rows, 1 bureau alert, 4 Harbor invalid/partial deduction rows, `line_ref` present, source table ID defaults set, all expected Tools_data/readiness tables present, and S8/S7 non-SAP evidence retrievable from Supabase. The release rows are approved by `human:rathish-owner` and validated by row hash; the Day-1 governed seed rows are approved by `human:owner-ratified-day-1`.
  - Supabase setup artifact closure: the live Supabase `recoup_config` rows are the release owner-input and governed case-value source of truth; `config/releaseOwnerInputs.ts` validates release row schemas/hashes, `config/governed.ts` validates governed row schemas/hashes/ranges without exact-locking active rows to Day-1 seed values, and `docs/supabase-memory-schema.sql` inserts `human:rathish-owner` plus `run_control`, `release_eval_label_manifest`, `intent_eval_labels`, `arbitration_eval_labels`, and the Day-1 `risk_mesh_cases` row for fresh project setup, including David credit account/Sentinel/action-queue display fields and Harbor Sentinel source refs. The same setup SQL now creates/grants the Tools_data tables used by the Sentinel reader, the settlement source tables `recoup_customers` and `recoup_deduction_lines`, and the full connector/readiness table set. Runtime paths load Supabase rows and fail closed instead of falling back to checked-in values.
  - Governed/release config loaders: Supabase `timestamptz` rows are normalized before strict Zod datetime validation; the base governed loader filters only its eight governed keys, and the release owner-input loader reads the four required release keys plus optional `decision_confidence_threshold` from the same `recoup_config` table so optional release rows do not break the base runtime snapshot.
  - Governed runtime config and source injection: Risk Mesh, Sentinel, Partial Hold, Forensics containment, cockpit read models, approvals, Realtime tool calls, MCP advisory calls, and release readiness now require an injected governed config snapshot instead of reading Day-1 code constants. Active Supabase `recoup_config` rows are runtime authority under structural validation; checked-in Day-1 values are setup/test seed fixtures. Forensics, Risk Mesh/Sentinel service tools, MCP advisory calls, Realtime `query.answer`, release readiness, and cockpit read models also require an injected `SourcePort`. Production HTTP/MCP/CLI entry points construct that source from Supabase `recoup_customers`, `recoup_deduction_lines`, and Harbor Tools_data tables; missing rows return 503/fail-closed instead of constructing `SyntheticSource({ seed: 42 })`. `src/core/` remains pure and uses the neutral `governed-config-snapshot` basis marker without mentioning Supabase or `recoup_config`.
  - Production SAP/docs/TPM/bureau evidence retrieval is now Supabase-backed for `/run` and MCP: `src/services/cockpitApi.ts` and `src/mcp/server.ts` preload required SAP/docs/TPM/bureau evidence from `recoup_src_sap`, `recoup_src_docs`, `recoup_src_tpm`, and `recoup_src_bureau`; `src/services/serviceLayer.ts` routes `retrieval.sap`, `retrieval.docs`, `retrieval.tpm`, and `retrieval.bureau` through those sources and fails closed when required Supabase evidence context is absent. A present source with no matching document returns empty evidence, and decision guardrails reject unsupported decisions. Generic `correspondence` evidence is rejected unless linked record IDs make it decision-safe credit-memo evidence. Live read-back confirmed S8 credit memo evidence and S7 TPM evidence from Supabase.
  - Runtime run-control wiring: `src/middleware/budgets.ts` now accepts an injected DB-loaded `RunControlConfig`; `src/services/conductor.ts` exposes a run-budget controller that enforces owner-configured step, retry, and token budgets without defaults; and `src/services/cockpitApi.ts` loads the Supabase `recoup_config` release-owner `run_control` row for `/healthz` and `/run`. `/run` fails closed with JSON 503 before SSE if the release-owner rows are absent/invalid; the live Agents SDK runner receives `maxTurns` and retry caps from DB-loaded `forensics.stepBudget`/`retryCap`, and explicit SDK usage metadata is recorded against the DB-loaded token budget, so runtime budget values come from Supabase rather than static code.
  - Forensics run metadata cleanup: `runForensicsInvestigation()` no longer lists `run-control-token-budget`, `run-control-step-budget`, or `run-control-retry-cap` as unresolved metadata. Those values are closed only by the Supabase `recoup_config` release-owner rows and the `/healthz`, `/run`, and `verify:release` gates. Optional Supabase plumbing exists for `decision_confidence_threshold`, Round 2 owner input supplied the threshold value, and live read-back confirmed the row/hash; Forensics confidence still needs deterministic confidence-score source evidence before production gating is complete.
  - Deployment readiness: worker `Hume` added `render.yaml`, `vercel.json`, `.env.example` API URL/PORT entries, and `tests/invariants/deployment-readiness.test.ts`. Spec reviewer `Jason` passed. Code reviewer `Heisenberg` found a real Vercel output-directory issue; `Hume` fixed it by adding `outputDirectory: "cockpit/.next"`, and re-review returned Ready/Yes.
  - Durable Supabase audit-chain persistence: worker `Hooke` added the Supabase audit-chain repository/RPC path, DB-tail based approval audit entry construction, and focused tests. Spec reviewer `Gauss` and code reviewer `Turing` initially failed the slice for process-local DB continuity and non-atomic approval/audit finality; the repair now reads the durable DB tail, commits audit row plus final approval memory through `recoup_commit_approval_audit`, retries once on tail mismatch, and records local action consumption only after the RPC succeeds.
  - Audit RPC hardening: reviewer `Nash` caught the real PostgREST mismatch shape (`P0001/audit_tail_mismatch` over HTTP 400); the adapter now maps mismatch by sanitized response body, not 409 only. Reviewer `Mencius` caught the `SECURITY DEFINER` default-execute and raw audit-table insert grant risks; the generated SQL now revokes function execution from `PUBLIC`, `anon`, `authenticated`, and `service_role`, grants `EXECUTE` only back to `service_role`, limits `recoup_audit_chain` to `SELECT` for `service_role`, and restricts the RPC memory-table parameter to the generated table name.
  - SQLite/local approval fallback atomicity: worker `Lorentz` changed the non-Supabase durable-memory path to build the final approval with `auditEntryHash`, append-if-absent the final approval memory record, and only then record process-local audit/action consumption. Spec reviewer `Hubble` passed. Code reviewer `McClintock` initially found that durable duplicates did not close the process-local direct-service gate; the duplicate paths now call `recordPreparedApprovalDecision(...)` before returning 409, and re-review returned Ready/Yes.
  - Post-approval audit receipt visibility: explorer `James` confirmed `/memory` stripped durable approval receipt payloads after `POST /approval`. The controller added RED coverage and patched `src/services/cockpitModel.ts` / `src/services/cockpitApi.ts` so persisted SQLite/Supabase approval records project sanitized `approvalAuditReceipts` with `actionId`, verified human approver, decision, status, audit hash, optional safe reason, and cited action record IDs after refresh. Reviewer `Kuhn` found no Critical/Important issues; the suggested malformed-row sanitizer coverage was added.
  - Bounded R-score/R-drift deterministic core: explorer `Faraday` confirmed the owner-ratified Day-1 weights/triggers exist in `config/governed.ts` while source normalization and full Capability D remain open. `src/core/risk.ts` now computes R-score only from caller-supplied finite `0-100` component values using governed weights, requires cited `recordIds`, evaluates R-drift from supplied observations/cooldown, and rejects zero-baseline/nonzero-current dispute-rate relative-increase cases instead of inventing an infinity policy. Reviewers `Mendel` and `Ptolemy` passed after fixes for 0-100 bounds, evidence IDs, zero-baseline handling, and input cloning.
  - Source-normalized Sentinel/R-drift wiring: explorer `Sagan` confirmed the safe path is adapter/source normalization, not new core math. `src/adapters/source.ts` adds a canonical risk observation snapshot, `src/adapters/synthetic.ts` returns cited Harbor source records plus explicit missing-field markers, and `src/agents/sentinel.ts` now computes R-score/R-drift only when complete source fields are present. Missing or partial source fields fail closed as `blocked_missing_source_fields`; empty source citations are rejected. Reviewer `Newton` passed after the record-ID and partial-field fixes.
  - Behavioral Containment action closure: explorer `Plato` confirmed the remaining D gap was action closure, not the Crestline gaming gate. `src/agents/containment.ts` now derives the single governed gaming candidate from supplied settlement lines and creates a deterministic `containment-review:<customerId>:<evidenceHash>` action from the cited evidence; `runForensicsInvestigation()` returns it as `containmentActions`; `approvals.decide` can find it and audit a verified-human decision. The artifact is deliberately review-only (`contained: false`, `no-external-action-staged`, `pending_human`, no external dispatch) and cites record IDs, behavioral evidence, and the deterministic gaming-gate R-score component readout. Reviewer `Ampere` passed with no Critical/Important findings; the suggested local audit-entry assertion was added.
  - OpenAI evidence vector-store readiness: explorer `Locke` confirmed the safe path is fetch-based async adapter/readiness, not sync `retrieval.docs` integration. `.env.example` and `config/env.ts` now recognize `OPENAI_EVIDENCE_VECTOR_STORE_ID` and expose redacted `openai.fileSearch` readiness without selecting an embeddings model. `src/adapters/openAiVectorStore.ts` implements a read-only OpenAI vector-store search adapter with Zod response validation, bounded `max_num_results`, metadata-only record citations, customer/scenario hit rejection, duplicate-file dedupe, and no OpenAI SDK dependency. It now requires the owner citation metadata contract on every vector hit (`source_table`, `record_id`, `customer_id`, `scenario_type`, `provenance='synthetic'`) and fails closed on missing/malformed/non-synthetic metadata. `src/adapters/docRepo.ts` exposes it only through injected async `retrieveVectorStoreEvidence(...)`; deterministic forensics stays on the existing sync docs retriever. Reviewer `Dewey` found and the controller fixed a metadata-citation issue; re-reviewer `Zeno` returned Ready/Yes; latest reviewer `Poincare` passed the stricter metadata contract with no findings.
  - Forensics injected evidence-source seam: explorer `Herschel` confirmed the safe owner-free path is injected/tested evidence, not env-driven live vector retrieval. `src/agents/forensics.ts` exposes `runForensicsInvestigationWithEvidenceSources(...)`, an opt-in async sibling that can attach injected document evidence while sharing the same deterministic rule, verdict, routing, recovery/Billing draft, and HITL logic as the default run. The default `runForensicsInvestigation(...)` now requires governed config plus a settlement source snapshot; it performs no env-driven live OpenAI/vector reads and no hidden static source construction. `retrieval.bureau` remains standalone and outside the default Forensics path. `src/tools/retrieval/docs.ts` merges/dedupes line-relevant evidence, and `src/services/decisionTools.ts` cites attached evidence document IDs/record IDs on decisions. Reviewer `Boyle` initially rejected the seam for default `retrieval.bureau` scope expansion; the controller removed that default call, added retrieval-surface/no-live-fetch coverage, and re-reviewer `Confucius` passed with no findings. Production vector-store ID/indexing/upload policy and broader live source contracts remain owner-bound.
  - AgentHooks live-runner wiring: auditor `Godel` found that hook receipt helpers existed but were not connected to the actual OpenAI runner path. RED coverage first failed for missing runner listeners and missing hook receipt status. `src/agents/liveForensicsStream.ts` now registers `registerRunHookAuditReceipts(...)` on the runner before `runner.run(...)`, and `src/services/cockpitApi.ts` passes deterministic cited trace record IDs into the live hook path. The live `/run` stream now requires verified human cockpit auth before using a configured OpenAI key, unauthenticated GET and POST `/run` stay deterministic-only, raw model deltas are replaced with a fixed non-decision status, and hook receipts flush before a fail-closed stream status. Hook receipts surface only sanitized lifecycle metadata as non-decision status events. Spec reviewer `Linnaeus` passed, reviewer `Hypatia` passed the auth/output hardening with no findings, and the low unauthenticated POST `/run` coverage note was added afterward.
  - No-new-dependency middleware path: `src/middleware/errors.ts`, `src/middleware/logging.ts`, and `src/middleware/budgets.ts` now provide shared compact JSON body-parse errors, safe correlation ID propagation over `x-recoup-correlation-id`, and DB-injected run-budget status without adding Winston or inventing budget constants. `src/services/cockpitApi.ts` installs the shared error/correlation middleware, exposes the correlation header to cockpit clients, and gates `/run` on Supabase release-owner run-control rows. Reviewer `Sartre` passed the earlier middleware slice with no findings; the suggested unsafe inbound correlation-ID replacement coverage was added afterward.
  - SAP billing item collection mapping: explorer `Pasteur` confirmed the owner-free path was invoice item collection reads from numeric `INV-*` record IDs, not synthetic credit memo/reference keys. `src/adapters/sapOData.ts` now emits a second GET-only `C_BillingDocumentItemFs?$filter=BillingDocument eq '<invoice>'` request when metadata exposes the item set and filter key; `retrieveDeductionCase()` fallback remains invoice-header-only. Reviewer `Arendt` first caught empty/malformed item payloads becoming positive evidence; RED tests were added and the mapper now emits item evidence only when `d.results` contains at least one object row. Current live probes: keyed billing headers and the item filter path are retrievable only with a sandbox-scoped TLS bypass because the HTTPS certificate is expired; HTTP closes the socket; non-billing SAP services currently return `403` with the configured credentials.
  - Release-readiness gate wiring: `evals/releaseReadinessCli.ts` loads `.env`/`.env.local`, reads Supabase `recoup_config`, validates governed config plus release owner-input rows, derives eval predictions from deterministic code, and exits nonzero if a gate fails. Appendix G run-control and expert eval-label inputs are now closed through the live DB rows; no static runtime values are used to pass `verify:release`.
  - Release expected-ranking coverage and ranked-readiness: `evals/releaseReadiness.ts` now includes owner `expectedRanking` in arbitration eval labels when the deterministic arbitration result is `ranked`. The Risk Mesh wrapper is rank-capable when Supabase-shaped `risk_mesh_cases.harbor.arbitrationPositions` rows include source-owned `optionValue` strings: `runRiskMeshClosedLoop` returns the core `ArbitrationResult` union, audits `arbitration.ranked`, keeps hold/terms actions `pending_human`, and query/cockpit read models handle ranked status without blocked-only `reason` leakage. Round 2 owner inputs supplied the option values, local tests cover the default ranked path, and live Supabase read-back confirms the active row has 16 option-value positions.
- Latest verification in this continuation:
  - Historical full proof after the optional decision-confidence threshold plumbing, before Round 2 owner-input implementation: `npm.cmd run verify` passed with ESLint green, TypeScript green, Vitest `79` files / `564` tests green, Dependency Cruiser green (`109` modules / `327` dependencies), and live Supabase `verify:release` passed. Live Supabase changes from the prior checkpoint updated active `recoup_config.risk_mesh_cases` to include `containmentIntentLabel` plus risk observation criteria, and migration `add_rule_input_json_to_recoup_deduction_lines` added/populated `rule_input_json`.
  - Current Round 2 local/live coverage plus R1 source-read hardening: default ranked Risk Mesh, source-owned R-score seed SQL, decision confidence when the supplied threshold is present, live Supabase row/hash read-back for Round 2 config/source rows, OpenAI evidence vector-store provisioning, and bounded frontend/MCP-callable R1 source-read plans. Final aggregate `npm.cmd run verify` passed after these updates: ESLint green, TypeScript green, Vitest `80` files / `578` tests, Dependency Cruiser `109` modules / `333` dependencies, and live Supabase `verify:release` green.
  - `npm.cmd run test -- tests/unit/retrieval-tools.test.ts tests/unit/forensics.test.ts tests/invariants/deduction-evidence-pack.test.ts tests/unit/enterprise-connectors.test.ts tests/unit/cockpit-api.test.ts tests/invariants/governed-config.test.ts tests/unit/supabase-memory.test.ts tests/invariants/connector-readiness.test.ts` passed after the Supabase table-backed runtime slice (`8` files / `152` tests).
  - `npm.cmd run verify` passed after the Supabase table-backed runtime slice: ESLint green, TypeScript green, Vitest `79` files / `553` tests green, Dependency Cruiser green (`109` modules / `326` dependencies), and live Supabase `verify:release` passed.
  - Read-only owner-free backend reviewer `Popper` found two Important gaps after the R-score source-column slice: static Day-1 locks around active governed config rows and static prefix-derived fallback evidence in production non-SAP retrieval. Both are fixed with DB-authoritative governed parsing and Supabase-required `/run`/MCP evidence context.
  - Read-only owner-free backend reviewer `Volta` found two Important MCP gaps after the source-table slice: MCP retrieval could omit service context and fall back to static prefix evidence, and MCP draft-only tools could execute when capabilities were omitted. Both are fixed in `src/mcp/server.ts`: MCP facades default to `requireSupabaseSyntheticEvidence: true`, and draft-only MCP tools require explicit `draft_action` capability before invoking handlers. Focused verification passed (`tests/invariants/mcp-visibility.test.ts`, `tests/invariants/tool-permissions.test.ts`, `tests/unit/retrieval-tools.test.ts`, `tests/unit/cockpit-api.test.ts`, `tests/unit/agent-runtime.test.ts`: `75/75`).
  - Post-fix reviewer `Kierkegaard` found no Critical or Important owner-free backend code gap after the MCP fail-closed repair. The only doc/status nuance was corrected: missing Supabase source context fails closed, while present-but-empty source rows return empty evidence that decision guardrails must reject.
  - `npm.cmd run test -- tests/unit/enterprise-connectors.test.ts tests/invariants/connector-readiness.test.ts tests/invariants/readme-contract.test.ts tests/invariants/mcp-visibility.test.ts tests/invariants/tool-permissions.test.ts tests/unit/retrieval-tools.test.ts tests/unit/supabase-memory.test.ts` passed after the source-table status cleanup (`7` files / `77` tests).
  - Historical `npm.cmd run verify` passed after the MCP fail-closed repair and source-table status cleanup: ESLint green, TypeScript green, Vitest `79` files / `555` tests green, Dependency Cruiser green (`109` modules / `326` dependencies), and live Supabase `verify:release` passed. This is superseded by the current Supabase/static-runtime cleanup proof above.
  - `npm run test -- tests/unit/enterprise-connectors.test.ts tests/unit/forensics.test.ts tests/unit/risk-mesh-run.test.ts tests/unit/query.test.ts tests/unit/cockpit.test.ts tests/unit/cockpit-api.test.ts tests/unit/realtime-session.test.ts tests/evals/release-readiness.test.ts tests/invariants/integration-contract.test.ts tests/invariants/mcp-visibility.test.ts tests/invariants/risk-mesh-service-boundary.test.ts tests/invariants/arbitration-audit.test.ts tests/invariants/no-wrongful-containment-s6.test.ts tests/invariants/action-hitl-all-capabilities.test.ts tests/unit/supabase-memory.test.ts` passed after the Supabase settlement-source correction (`15` files / `189` tests). `npm run lint`, `npm run typecheck`, and `npm run depcruise` also passed after this correction.
  - Historical `npm run verify` passed after the Supabase R-score component-score source-column slice: ESLint green, TypeScript green, Vitest `79` files / `550` tests green, Dependency Cruiser green (`109` modules / `323` dependencies), and live Supabase `verify:release` passed. This is superseded by the current `561`-test gate above.
  - Read-only owner-free backend reviewer `Goodall` found no Critical or Important code gap after the Supabase settlement-source correction. Only doc hygiene remains: the owner-response artifact says the full SDD-completion set is satisfied, but implementation evidence must continue to treat Round 2 live DML/read-back, deterministic confidence-score source evidence, vector-store provisioning, broader O2C contracts, and owner verification flags as source-bound until independently proven. Do not edit the owner-response artifact without owner confirmation.
  - `npm run test -- tests/invariants/deployment-readiness.test.ts` passed (`6/6`).
  - `npm run build:cockpit` passed with Next.js production output including `/run`, `/forensics`, `/credit`, `/credit/command`, `/cfo`, login, and governance routes.
  - `npm run test -- tests/unit/supabase-memory.test.ts tests/unit/supabase-audit-chain.test.ts tests/unit/cockpit-api.test.ts tests/invariants/hash-chain-tamper.test.ts` passed after the audit repair (`4` files / `73` tests).
  - `npm run test -- tests/unit/cockpit-api.test.ts tests/unit/supabase-audit-chain.test.ts tests/unit/supabase-memory.test.ts tests/invariants/hash-chain-tamper.test.ts` passed after the local fallback repair (`4` files / `75` tests).
  - `npm run test -- tests/unit/risk.test.ts tests/unit/risk-mesh-run.test.ts tests/invariants/governed-config.test.ts tests/invariants/runtime-config.test.ts` passed after the bounded R-score/R-drift repair (`4` files / `34` tests).
  - `npm run test -- tests/invariants/integration-contract.test.ts tests/invariants/mcp-visibility.test.ts tests/invariants/tool-permissions.test.ts tests/invariants/tool-whitelist.test.ts` passed after the MCP advisory tool repair (`4` files / `20` tests).
  - `npm run test -- tests/unit/sentinel.test.ts tests/unit/risk-mesh-run.test.ts tests/unit/risk.test.ts tests/invariants/integration-contract.test.ts tests/invariants/mcp-visibility.test.ts tests/unit/cockpit.test.ts tests/unit/cockpit-api.test.ts` passed after the Sentinel source-normalization repair (`7` files / `86` tests).
  - `npm run typecheck` passed after the audit repair.
  - `npm run typecheck` passed after the local fallback repair.
  - `npm run typecheck` passed after the bounded R-score/R-drift repair.
  - `npm run test -- tests/invariants/action-hitl-all-capabilities.test.ts tests/invariants/named-guardrail-surfaces.test.ts` passed after the Behavioral Containment action-closure repair (`2` files / `11` tests).
  - `npm run test -- tests/unit/cockpit.test.ts tests/unit/cockpit-api.test.ts` passed after the post-approval audit receipt read-model repair (`2` files / `66` tests).
  - `npm run lint` and `npm run typecheck` passed after the post-approval audit receipt read-model repair.
  - `npm run verify` passed after the post-approval audit receipt read-model repair: ESLint green, TypeScript green, Vitest `75` files / `462` tests green, and Dependency Cruiser green (`105` modules / `290` dependencies).
  - `npm run test -- tests/invariants/runtime-config.test.ts tests/unit/enterprise-connectors.test.ts` passed after the OpenAI vector-store readiness repair (`2` files / `31` tests).
  - `npm run lint` and `npm run typecheck` passed after the OpenAI vector-store readiness repair.
  - `npm run verify` passed after the OpenAI vector-store readiness repair: ESLint green, TypeScript green, Vitest `75` files / `469` tests green, and Dependency Cruiser green (`106` modules / `293` dependencies).
  - `npm run test -- tests/unit/live-forensics-stream.test.ts tests/unit/conductor-hooks.test.ts tests/unit/cockpit-api.test.ts` passed after AgentHooks live-runner wiring (`3` files / `56` tests).
  - `npm run lint` and `npm run typecheck` passed after AgentHooks live-runner wiring.
  - `npm run verify` passed after AgentHooks live-runner wiring: ESLint green, TypeScript green, Vitest `75` files / `471` tests green, and Dependency Cruiser green (`106` modules / `294` dependencies).
  - `npm run test -- tests/unit/live-forensics-stream.test.ts tests/unit/cockpit-api.test.ts` passed after the live-stream auth/output hardening (`2` files / `57` tests).
  - `npm run lint` and `npm run typecheck` passed after the live-stream auth/output hardening.
  - `npm run verify` passed after the live-stream auth/output hardening: ESLint green, TypeScript green, Vitest `75` files / `475` tests green, and Dependency Cruiser green (`106` modules / `294` dependencies).
  - `npm run test -- tests/unit/middleware.test.ts tests/unit/cockpit-api.test.ts` passed after the no-new-dependency middleware path (`2` files / `56` tests).
  - `npm run lint` and `npm run typecheck` passed after the no-new-dependency middleware path.
  - `npm run verify` passed after the no-new-dependency middleware path: ESLint green, TypeScript green, Vitest `76` files / `481` tests green, and Dependency Cruiser green (`109` modules / `300` dependencies).
  - `npm run test -- tests/unit/retrieval-tools.test.ts tests/unit/forensics.test.ts tests/invariants/deduction-evidence-pack.test.ts` passed after the injected Forensics evidence-source seam (`3` files / `19` tests).
  - `npm run lint` and `npm run typecheck` passed after the injected Forensics evidence-source seam.
  - `npm run verify` passed after the injected Forensics evidence-source seam: ESLint green, TypeScript green, Vitest `76` files / `486` tests green, and Dependency Cruiser green (`109` modules / `300` dependencies).
  - `npm run test -- tests/unit/sap-odata.test.ts` passed after the SAP billing item collection mapping (`1` file / `22` tests).
  - `npm run test -- tests/unit/sap-odata.test.ts tests/invariants/connector-readiness.test.ts tests/invariants/integration-contract.test.ts` passed after the SAP billing item collection mapping (`3` files / `40` tests).
  - `npm.cmd run test -- tests/unit/forensics.test.ts` passed after the stale run-control metadata cleanup (`1` file / `10` tests).
  - `npm run lint` and `npm run typecheck` passed after the SAP billing item collection mapping.
  - `npm run verify` passed after the SAP billing item collection mapping: ESLint green, TypeScript green, Vitest `76` files / `490` tests green, and Dependency Cruiser green (`109` modules / `300` dependencies).
  - `npm.cmd run test -- tests\invariants\readme-contract.test.ts tests\unit\enterprise-connectors.test.ts tests\invariants\runtime-config.test.ts tests\invariants\run-control.test.ts tests\evals\accuracy-bars.test.ts` passed after the owner-answer source-of-truth doc correction (`5` files / `53` tests).
  - `npm.cmd run test -- tests\invariants\release-readiness-verify.test.ts` passed after the release-readiness verify wiring (`1/1`).
  - `npm.cmd run test -- tests\evals\release-readiness.test.ts` passed after the release-readiness helper was added (`2/2`).
  - `npm.cmd run test -- tests\invariants\run-control.test.ts tests\evals\release-readiness.test.ts tests\invariants\governed-config.test.ts tests\unit\supabase-memory.test.ts tests\unit\risk-mesh-run.test.ts tests\invariants\partial-hold-determinism.test.ts tests\invariants\arbitration-audit.test.ts tests\unit\arbitration.test.ts` passed after DB-backed owner input wiring (`8` files / `51` tests).
  - `npm.cmd run verify:release` passed against live Supabase `recoup_config`.
  - Historical `npm.cmd run verify` passed after the Supabase owner-input migration and DB-backed release-readiness wiring: ESLint green, TypeScript green, Vitest `79` files / `504` tests green, Dependency Cruiser green (`110` modules / `304` dependencies), and `verify:release` green. This is superseded by the current gate above.
  - Historical `npm.cmd run verify` passed after the governed runtime-config injection and reviewer follow-up repairs: ESLint green, TypeScript green, Vitest `79` files / `511` tests green, Dependency Cruiser green (`109` modules / `310` dependencies), and `verify:release` green. This is superseded by the current gate above.
  - `npm.cmd run test -- tests\invariants\run-control.test.ts tests\unit\middleware.test.ts tests\unit\cockpit-api.test.ts` passed after DB-backed runtime run-control wiring (`3` files / `65` tests). `npm.cmd run lint` and `npm.cmd run typecheck` also passed afterward.
  - Historical `npm.cmd run verify` passed after DB-backed runtime run-control wiring: ESLint green, TypeScript green, Vitest `79` files / `514` tests green, Dependency Cruiser green (`109` modules / `312` dependencies), and `verify:release` green against live Supabase `recoup_config`. This is superseded by the current gate above.
  - `npm.cmd run test -- tests\unit\cockpit.test.ts` passed after the CFO read model stopped listing the closed `verify-runtime-config-loader` proof as open (`19/19`). `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd run verify:release` also passed; `verify:release` read the live Supabase `recoup_config` rows.
  - `npm.cmd run test -- tests\evals\release-readiness.test.ts tests\unit\supabase-memory.test.ts` passed after wiring ranked arbitration eval comparison and adding checked-in Supabase release owner-input seed rows (`2` files / `31` tests).
  - `npm.cmd run test -- tests\unit\live-forensics-stream.test.ts tests\unit\cockpit-api.test.ts tests\invariants\seed-reproducible.test.ts` passed after wiring live Agents SDK `maxTurns`, retry caps, and explicit usage telemetry to the DB-loaded `forensics` run-control row plus the SDD TypeScript datagen wording cleanup (`3` files / `64` tests).
  - Historical `npm.cmd run verify` passed after the live retry/token budget telemetry, Supabase source-of-truth doc cleanup, and SDD TypeScript datagen wording cleanup. This is superseded by the current Supabase settlement-source gate above: ESLint green, TypeScript green, Vitest `79` files / `548` tests green, Dependency Cruiser green (`109` modules / `323` dependencies), and `verify:release` green against live Supabase `recoup_config`.
  - Historical `npm.cmd run verify` passed after the Supabase Sentinel Tools_data fail-closed repair and setup-SQL hardening: ESLint green, TypeScript green, Vitest `79` files / `533` tests green, Dependency Cruiser green (`109` modules / `318` dependencies), and `verify:release` green against live Supabase `recoup_config`. This is superseded by the current gate above.
  - `git diff --check -- .env.example render.yaml vercel.json tests\invariants\deployment-readiness.test.ts` passed; only a Windows LF-to-CRLF warning for `.env.example` was reported.
- Active backend slice status at this checkpoint: durable Supabase audit-chain persistence, SQLite/local approval fallback atomicity, post-approval audit receipt visibility, bounded R-score/R-drift deterministic core, MCP-visible Sentinel/Containment advisory tools, source-normalized Sentinel/R-drift wiring, Harbor DSO R-drift source normalization, Supabase fallback provenance for R1 G2 credit-memo proof, Behavioral Containment review-only action closure, OpenAI vector-store readiness, Forensics injected evidence-source seam, AgentHooks live-runner wiring/auth-output hardening, the no-new-dependency middleware path, DB-backed governed runtime-config injection, DB-backed runtime run-control gating, and the CFO open-proof read model cleanup are closed with verification evidence; reviewer gates are recorded above where they were run. Do not reopen the Supabase RPC path unless a live Supabase RPC migration/test proves otherwise.
- Replacement reviewer `Bacon` cross-checked the stale backend audit action list after `Maxwell` was closed for no output. `Bacon` found no untracked owner-free backend code gap; MCP agent-to-agent advisory tools are closed/tested for the read-only advisory scope; remaining backend items are owner-bound or the final SDD audit process. Final release-input reviewer `Euler` found no Critical issues, confirmed `verify:release` reads live Supabase rows instead of static runtime values, and flagged that expected arbitration ranking is not yet true weighted option scoring.
- Latest read-only backend reviewer `Raman` found three owner-free follow-ups: release evals ignored `expectedRanking`, checked-in Supabase setup did not seed release owner-input rows, and live Agents SDK `maxTurns` was static. The controller fixed all three locally: ranked arbitration evals now compare `expectedRanking` when ranked outputs exist, `docs/supabase-memory-schema.sql` seeds the non-secret release rows into `recoup_config`, and `/run` passes DB-loaded `forensics.stepBudget` into the live runner. Follow-up reviewer `Descartes` confirmed MCP agent-to-agent is closed for the read-only advisory scope and found two remaining owner-free cleanups; the controller then wired DB-loaded live retry caps plus explicit SDK usage telemetry into the run-budget controller and corrected the SDD datagen wording to TypeScript. Round 2 owner inputs now supply the source-owned `optionValue` depth; live Supabase DML/read-back remains separate proof.
  - Latest controller pass moved Harbor Risk Mesh case facts, David credit business display values, and Harbor Sentinel source refs out of inline code and into live/check-in Supabase `recoup_config.risk_mesh_cases`. The regression tests mutate the Supabase-shaped row and prove order amount, partial-hold scores, action bases, terms, arbitration record IDs, account readout, Sentinel display, and action queue follow the DB row; new Sentinel/API tests prove Supabase Tools_data `customers`/`payments`/`bureau_alerts`/`deductions_backlog` rows drive Harbor R-drift record IDs, require a matching source customer row, require explicit unresolved lien rows, require cited invalid/partial dispute rows before `disputeSpike`, return 503 instead of synthetic fallback when rows are missing, and never use `credit_decisions` or invented R-score components. Latest follow-up slices enforce the OpenAI vector-store owner metadata contract, make Risk Mesh rank-capable for source-owned `optionValue`s, add nullable checked `customers.r_score_component_scores_json` as the strict source field for R-score components, remove exact Day-1 locks from active governed config rows, route production `/run`/MCP SAP/docs/TPM/bureau retrieval through Supabase `recoup_src_*` rows, make MCP retrieval/permission fail closed when service context or explicit `draft_action` capability is absent, and rename connector readiness modes from legacy table-status wording to source-table wording. Focused Vitest is green for Sentinel/API (`6` files / `123` tests), vector metadata (`20` tests), ranked-readiness/query/cockpit/audit (`4` files / `32` tests), the R-score source-column slice (`91/91`), the Supabase table-backed runtime slice (`8` files / `152` tests), the MCP fail-closed repair (`5` files / `75` tests), and the source-table status cleanup (`7` files / `77` tests). Round 2 owner inputs now supply Risk Mesh option values, R-score component scores, and the decision-confidence threshold; local tests cover those paths, while live Supabase DML/read-back and fresh full verify still need separate proof. New `docs/backend-sdd-compliance-audit.md` records the current backend evidence and owner/source-bound gaps.
- Still open / do not claim complete: Round 2 owner values are supplied and live-read-backed, and local coverage exists for default ranked Risk Mesh, source-owned R-score seed SQL, and present-threshold decision confidence. Other open items are deterministic confidence-score source evidence, broader live O2C mappings beyond the R1 SAP-primary/Supabase-fallback contract, vector-store upload/re-indexing lifecycle policy, optional Winston JSON logging dependency approval for production observability, and owner/build-time verification flags. Crestline remains owner-locked to review-only for R1. The latest backend audit artifact does not identify a confirmed owner-free backend code gap.
- Human UI verification is now an explicit required gate before marking any UI/UX surface passed. The cockpit is running for reviewer inspection at `http://localhost:3000`; the API is running at `http://127.0.0.1:4317`.
- Browser skill status: the bundled browser plugin skill was copied into the user skill directory at `C:\Users\rathi\.codex\skills\control-in-app-browser\SKILL.md`, with a local note pointing at `C:\Users\rathi\.codex\plugins\cache\openai-bundled\browser\26.611.62324\scripts\browser-client.mjs`.
- 2026-06-22 10:05 +05:30 restart check: the user skill file and bundled `browser-client.mjs` both exist, and `[plugins."browser@openai-bundled"] enabled = true` is present in `C:\Users\rathi\.codex\config.toml`. The browser runtime was still not exposed because `[features] js_repl` was set to `false`; that local Codex feature flag has now been changed to `true`. This current session cannot dynamically gain the missing tool, so restart Codex once more before expecting the Browser skill to control the in-app browser. Until that restart/tool exposure, Playwright remains the fallback verification path.
- 2026-06-22 10:08 +05:30 post-restart check: `js_repl = true` and the browser plugin is still enabled in `C:\Users\rathi\.codex\config.toml`, but tool discovery still did not expose the browser-control JS runtime. A broader discovery pass exposed only the subagent tool, not the in-app browser runtime. Browser skill files are installed, but Browser remains unavailable in this Codex session; use Playwright fallback unless a later Codex/extension restart exposes the missing runtime.
- 2026-06-22 10:36 +05:30 Browser control green check: the Browser runtime is now exposed and successfully bootstrapped against the current bundled plugin client at `C:\Users\rathi\.codex\plugins\cache\openai-bundled\browser\26.616.51431\scripts\browser-client.mjs`. Root cause of the first bootstrap failure was the stale `26.611.62324` local note/path after the bundled browser plugin updated. Live Browser verification opened `http://localhost:3000/login`, observed title `Recoup Forensics Cockpit`, and found exactly one `input[name="loginId"]`; Browser can now replace Playwright fallback for in-app visual checks when available.
- 2026-06-22 10:50 +05:30 UI/UX journey-map restart: user rejected the current cockpit quality for Login, Maya, David, and CFO and requested a simpler hackathon-grade product journey. New active discovery scope is Browser-driven button-by-button mapping, simple ImageGen/visual mockups for target journeys, SAP OData and Supabase-backed source status shown as online in a collapsible/source-health surface, interactive persona worklists rather than static cards, detailed test scenarios before implementation, and human verification before marking UI/UX passed. Read-only subagents launched for UI route mapping, source-status investigation, and independent risk review.
- 2026-06-22 11:30 +05:30 UI/UX correction checkpoint: user clarified to ask for guidance/options when an assumption is unclear, rather than assuming a dependency is unavailable. Initial ImageGen outputs were rejected as not good enough because they contained garbled text/fake-looking values; deterministic HTML/CSS mockups were created instead at `mockups/recoup-simple-journey-rework.html` and `mockups/recoup-simple-journey-rework.css`, exported to `mockups/rework-screenshots/`. Login mockup was corrected to remove SAP/Supabase/source-health labels; connector/source health now belongs only inside authenticated persona/governance surfaces. User also clarified that "simple" persona pages must still include compact analytics cards; the reworked Maya, David, and CFO mockups now include analytics bands while avoiding invented dollar values. No app-code implementation has started for this correction slice yet.
- 2026-06-22 11:34 +05:30 status checkpoint: tracked repo state is synced with `origin/codex/guardrail-riskmesh-hardening` at `d8908c0 Record cockpit journey mockup checkpoint`. No app-code implementation has started after the deterministic mockup/journey-map checkpoint. Current live in-app browser tab remains on `http://127.0.0.1:3000/cfo`. Local untracked scratch remains intentionally uncommitted: rejected ImageGen drafts, `.superpowers/`, `.planning/`, and `output/browser-journey/`.
- Fresh David `/credit` desktop visual reviewer `Godel` scored the post-Risk-Mesh-overlap screenshot `3/5 FAIL`. The old Risk Mesh collision is resolved, but the middle Partial hold workbench / Proactive terms action packet still read too generated, with cramped score copy, text-heavy action rows, and raw IDs too prominent.
- A narrow RED/GREEN fix landed locally for David `/credit`: `tests/invariants/cockpit-no-business-logic.test.ts` now protects unclipped score-basis copy, compact `credit-action-decision-row` structure, and low-emphasis `credit-provenance-strip` record IDs. The RED failure was observed, then focused verification passed: `npm.cmd run test -- tests\invariants\cockpit-no-business-logic.test.ts` (`23/23`), `npm.cmd run typecheck`, and `npm.cmd run test -- tests\invariants\cockpit-no-business-logic.test.ts tests\unit\cockpit.test.ts` (`39/39`).
- The first full `npm.cmd run test:e2e` retry after this fix failed after ~174s at `input[name="loginId"]` because Next served a 500 dev error for `./config/env.ts` importing `./governed.js` through `cockpit/app/api/approval/route.ts`. Root cause: Next/Turbopack could not resolve the NodeNext `.js` source import in the app-route graph after approval-route compilation. Fix: app API routes now import `loadLocalRuntimeEnvFiles` from new `config/localRuntimeEnv.ts`, while `config/env.ts` re-exports that loader for Node runtime callers. RED coverage was added in `tests/invariants/cockpit-route-architecture.test.ts`; route/runtime verification passed: `npm.cmd run test -- tests\invariants\cockpit-route-architecture.test.ts tests\invariants\runtime-config.test.ts tests\unit\realtime-next-routes.test.ts` (`32/32`) and `npm.cmd run typecheck`.
- The stale Next dev server had to be restarted after the module-boundary fix. After restart, `/login` returned HTTP `200` with `input[name="loginId"]`, and a fresh full `npm.cmd run test:e2e` passed, refreshing `output/playwright/e2e/*`. The pass took ~296s because the script captures the full responsive matrix with repeated fresh login contexts; logs show repeated `/login` and `/run` renders around 3s each.
- A selector-specificity issue then appeared in the quick David screenshot: the older `.credit-action-table > div` grid rule overrode the new compact decision-row grid. A RED regression tightened the CSS selector requirement, and the fix now uses `.credit-arbitration-workstation .credit-action-table > .credit-action-decision-row`. `npm.cmd run test -- tests\invariants\cockpit-no-business-logic.test.ts` passed (`23/23`), and the refreshed `output/playwright/e2e/david-credit-1440.png` shows compact horizontal action rows.
- Do not mark David `/credit` visually closed yet. It still needs human inspection on the live server, refreshed e2e screenshot evidence, and a fresh reviewer or explicit human acceptance at `>=4/5`.
- Maya `/forensics` remains closed for the active desktop gate (`4.0/5 PASS`); Maya `/run` remains open (`3.7/5 FAIL`); CFO `/cfo` remains closed for the active desktop gate (`4.1/5 PASS`). Overall cockpit is not final-complete.

Fresh verification after the latest David `/credit` Risk Mesh CSS/test change:

- `npm.cmd run test:e2e` passed on port `3044` and refreshed `output/playwright/e2e/*`.
- `npm.cmd run verify` passed after that change: ESLint green, TypeScript green, Vitest `67` files / `399` tests green, Dependency Cruiser green (`96` modules / `247` dependencies).
- The latest `david-credit-1440.png` was visually inspected after e2e refresh. The Risk Mesh overlap fix is present, but David `/credit` is **not reviewer-closed** yet; it needs a fresh desktop-only visual reviewer score before moving from open P0 to closed.

Latest runtime screenshots to inspect first:

- `output/playwright/e2e/maya-forensics-1440.png`
- `output/playwright/e2e/maya-run-1440.png`
- `output/playwright/e2e/david-credit-1440.png`
- `output/playwright/e2e/david-command-1440.png`
- `output/playwright/e2e/cfo-1440.png`
- `output/playwright/e2e/login-1440.png`
- `output/playwright/e2e/governance-agents-1440.png`
- `output/playwright/e2e/governance-connectors-1440.png`
- `output/playwright/e2e/governance-memory-1440.png`
- `output/playwright/e2e/governance-trace-1440.png`

Fresh-session startup protocol:

1. Read only this top handoff block, `AGENTS.md`, `INVARIANTS.md`, `docs/superpowers/plans/2026-06-21-cockpit-imagegen-comparison.md`, and `docs/superpowers/plans/2026-06-21-full-solution-sdd-review-checklist.md` before acting. Do not load the full SDD unless a specific pending item names a section.
2. Use subagents intentionally: one writer per disjoint write scope, read-only planners/reviewers in parallel, then a separate visual/spec/code reviewer. Do not let a subagent run silently: default wait `60s`, extend to `120s` only with useful activity, then interrupt for a `30s` checkpoint and close/restart/take over.
3. Do not read or print `.env.local`. Auth details are in Supabase tables and runtime env loaders; use the existing API/session paths rather than exposing secrets.
4. Mobile is deferred. Desktop `1440x900` is the current critical visual gate.
5. Never mark a visual route complete below `4/5`. Historical passes are superseded by the latest desktop-only reviewer when scores conflict.
6. Preserve backend/read-model wiring. React routes may format data but must not invent dollars, scores, statuses, record IDs, or decision facts.

Priority order for the next session:

| Priority | Item | Current state | Next action |
|---|---|---|---|
| P0 | David `/credit` desktop visual | `Beauvoir the 2nd` scored pre-fix screenshot `3.2/5 FAIL`; `Lagrange the 2nd` added RED invariant and CSS fix; e2e and component verify were green before release-readiness enforcement. | Spawn a fresh desktop-only visual reviewer on `output/playwright/e2e/david-credit-1440.png`. If score is still `<4`, continue only the Risk Mesh/hierarchy slice. |
| P1 | Maya `/run` desktop visual | `3.7/5 FAIL`; raw `ToolStatusRail` / `MultimodalDock`, snake-case feed labels, cramped rail copy. | Use `Leibniz the 2nd` plan: update `ToolStatusRail`, `/run` sidecar label, `MultimodalDock` label, `run-stream.tsx` label fallback, and route-scoped CSS. |
| P1 | Login/governance desktop visual | Login `3.3/5 FAIL`; governance `3.1-3.4/5 FAIL`. | Use `Hooke the 2nd` plan: remove raw `ToolStatusRail`, demote schema/table substrate, soften memory keys/chips, shorten hash-heavy trace rows while preserving evidence access. |
| P1 | Full-solution SDD line-by-line review | Still pending and broader than persona UI. | Continue from the checklist; do not scope it only to Maya/David/CFO persona pages. |
| P1/P2 | Business capability gaps | Round 2 owner inputs supplied Risk Mesh option values, R-score component scores, and the decision-confidence threshold; local tests cover default ranked Risk Mesh, source-owned R-score seed SQL, and present-threshold confidence. Live Supabase read-back confirms those rows, and the OpenAI evidence vector store is provisioned. Deterministic confidence-score source evidence, vector-store refresh lifecycle policy, and owner policy/order data for any Crestline hold/freeze/partial-hold beyond review-only remain open. | Keep remaining claims visibly blocked/deferred unless source/policy evidence exists; do not invent Appendix G constants or R-score transforms. Use `docs/backend-sdd-compliance-audit.md` as the current backend audit anchor. |

## Active 2026-06-21 Cockpit UI Revamp Status

Current active goal addition: close the desktop-first Recoup v1.2 cockpit as a premium SaaS multi-agent product, using the ImageGen cue boards as the visual target rather than the rough screenbook. Mobile is explicitly deferred for this pass.

Scope correction captured during the active session:

- ImageGen visual comparison is **all-persona**, not Maya-only: Maya, David credit, David D5, and CFO runtime screenshots must each be compared against their matching ImageGen cue before the cockpit visual pass is done.
- Backend/read-model wiring is **all-view**, not Maya-only: every created cockpit view/component should render operational/business facts from API/read-model fields unless the element is explicitly demo chrome and labelled as such.
- SDD review is **full-solution**, not persona-only: the checklist must cover capabilities A/B/C/D, invariants, agents/tools/services, audit/HITL, MCP, memory, eval/verify, cockpit, OpenAI usage, and demo readiness. Persona v1.2 is one subsection of that broader review.

Important new standing instruction:

- `AGENTS.md` now includes section `5.1 Cockpit UI anti-slop standard`.
- That section captures the user's Catalog of Tells as a permanent cockpit review gate: avoid generic AI-dashboard typography, colors, card-everything layouts, nested cards, raw backend enum labels, decorative motion, fake stats, and uniform generated spacing.
- Future subagents working on Maya, David credit, David D5, or CFO should be given that AGENTS section plus the relevant ImageGen cue.

User-critical feedback to preserve:

- The UI must not look like an LLM-generated dashboard.
- The sidebars must feel like real product rails: broad IA, icon-led navigation, compact identity/org footer, and no nested sidebar cards/chips/pills that look generated.
- The ImageGen cues are not merely inspiration; runtime screens must converge on their topology and density.
- Do not use ImageGen raster output as SVG icons. Use a disciplined vector icon system from the existing icon library for crisp, accessible product navigation.
- Do not create static React-only mock UI. Persona screens should render from API/read-model fields; UI may format labels, but no dollars/scores/status facts should be invented in React.

Current cue sources:

- `mockups/imagegen/maya-forensics-journey.png`
- `mockups/imagegen/david-credit-arbitration.png`
- `mockups/imagegen/david-command-center-dark.png`
- `mockups/imagegen/cfo-executive-readout.png`

Tracked review artifacts added during this pass:

- `docs/superpowers/plans/2026-06-21-cockpit-imagegen-comparison.md`: all-persona ImageGen comparison with current visual scores and P0/P1 visual backlog.
- `docs/superpowers/plans/2026-06-21-full-solution-sdd-review-checklist.md`: full-solution SDD/backend checklist with P0/P1/P2 items across HITL, audit, Realtime, Risk Mesh, containment, CFO close, and verification.

Active workflow gate status:

- The desktop cockpit visual program is **not final-complete**. Latest desktop-only reviewer `Beauvoir the 2nd` scored Maya `/forensics` `4.0/5 PASS`, David D5 `/credit/command` `4.4/5 PASS`, and CFO `/cfo` `4.1/5 PASS`; reopened Maya `/run` `3.7/5 FAIL`, `/login` `3.3/5 FAIL`, and governance routes `3.1-3.4/5 FAIL`; and reopened David `/credit` at `3.2/5 FAIL` before `Lagrange the 2nd` landed the Risk Mesh overlap fix. David now needs fresh desktop re-review against `output/playwright/e2e/david-credit-1440.png`. Current durable rule: a score below `4/5` is a failed visual gate, never a completed visual-audit gate; `1/5`, `2/5`, and `3/5` must stay fail/pending. Mobile is deferred for this pass.
- Subagent-driven workflow is now the required execution mode for remaining P0/P1 slices: implementer subagent -> spec-compliance reviewer -> code-quality reviewer -> focused tests -> visual audit -> handoff update. Stale or slow subagents must be terminated or replaced rather than waited on indefinitely. Default wait is `60s`; extend to `120s` only when an agent is visibly active, has produced useful partials, or is running tests; then interrupt for a `30s` checkpoint before close/restart/takeover.
- Workflow verifier subagent `Sagan` reviewed coverage and returned **partial/not complete**. Follow-up SDD/backend reviewer `Fermat` confirmed the highest-risk remaining gaps were Realtime demo-session auth, process-local approval finality, role-principal mapping, static audit chips, and missing Crestline containment handoff. Realtime query proxy auth is now patched for Maya-only demo sessions. Durable approval finality is now closed for the configured cockpit API durable-memory path after spec reviewer `Wegener` and code-quality reviewer `Laplace` passed the slice. Role-principal mapping is now closed for the bounded demo-proxy scope: direct API auth remains exact principal+token, demo proxy identities are role-derived with request-bound HMAC proof, approval allows Maya/David only, Realtime remains Maya-only, CFO approval is rejected, and final reviewer `James` found no Critical/Important/Minor issues. Run-control/eval release readiness now has an explicit failing aggregate gate: blocked run control, blocked eval labels, or failing metrics produce release `fail` instead of being treated as acceptable. Real audit-hash surfacing is now closed for `/credit` and `/cfo`; Governance Trace fidelity is now closed for deterministic Risk Mesh audit entries; CFO proof/status display labels are now model-owned; David `/credit` toolbar, Account-360 detail/summary rows, Sentinel detail, approval record-strip, action queue summary, partial-hold, and terms-packet labels are now model-owned. Crestline/M6 now has a cited risk-review readout plus a HITL-visible containment-review action closure; bounded R-score/R-drift core, source-normalized Sentinel/R-drift wiring, the strict Supabase R-score component-score field/read path, post-approval audit receipt visibility, OpenAI vector-store metadata enforcement/readiness, DB-backed release/run-control gates, and live retry/token budget telemetry are also closed. Remaining backend gaps are Round 2 live Supabase DML/read-back or main-thread verify proof for supplied option/component/threshold rows, deterministic confidence-score source evidence, production vector-store ID/upload/indexing, and owner policy/order data for any Crestline action beyond review-only.
- David HITL implementation was handled by implementer subagent `Huygens`. Spec reviewer `Dalton` passed the initial service/UI wiring, code reviewer `Mendel` found a real browser-session auth gap, and fix worker `Anscombe` added session-cookie approval auth plus guard fixes. Reviewer `Avicenna` then caught a shared-auth side effect; the final David approval patch kept demo-session approval auth scoped to `/api/approval`. A later bounded patch allows Maya-only demo sessions for the two Realtime query proxies and explicitly rejects David/CFO.
- David visual convergence used the subagent workflow. Visual worker `Rawls` was shut down after a non-useful wait window. Reviewer `Parfit` scored the first refreshed screenshot `3/5 FAIL`; reviewer `Hegel` scored a local David-only pass; stricter all-persona reviewer `Descartes` later reopened David at `2/5 FAIL`; reviewer `Halley` scored the first topology remediation `3/5 FAIL`; reviewer `Bohr` scored the refreshed `/credit` screenshot `4/5 PASS`; all-persona auditor `Bernoulli` reopened David `/credit` at `3/5 FAIL`; worker `Carver` then remediated David and reviewer `Meitner` scored it `4/5 PASS`.
- Current live subagent workflow: route/persona inventory explorer `Confucius` completed and confirmed Maya/David/CFO were API/read-model backed while D5 was missing at that time; UI auditor `Descartes` completed and reopened David plus all remaining visual gates; focused Maya reviewer `Aristotle` scored the remediated Maya `/forensics` screenshot `4/5 PASS`; backend worker `McClintock` verified David hold/terms approvals already pass through governed REST/service wiring; D5 worker `Arendt` added `/credit/command` and fixed its typecheck issue; docs worker `Kepler` added the anti-slop workflow rules; visual reviewers `Halley`, `Gibbs`, and `Beauvoir` scored earlier surfaces `3/5 FAIL`; reviewer `Bohr` scored David `/credit` `4/5 PASS`; reviewer `Chandrasekhar` scored D5 `/credit/command` `4/5 PASS`; CFO reviewer `Averroes` scored the first board-pack pass `3/5 FAIL`; CFO reviewer `Peirce` scored the remediated `/cfo` screenshot `4/5 PASS`; run-copy worker `Feynman` was closed after a one-second stale wait while its useful file changes had already landed; reviewer `Curie` scored the refreshed `/run` screenshot `4/5 PASS`; all-persona auditor `Bernoulli` reopened David `/credit` and D5 `/credit/command` at `3/5 FAIL` and identified login plus governance routes as `2/5 FAIL` / not screenshot-gated; David reviewer `Meitner` then scored `/credit` `4/5 PASS`; D5 reviewer `Raman` scored the fixed-grid `/credit/command` screenshot `4/5 PASS`; login/governance worker `Goodall` was interrupted and closed after a bounded wait, returning partial markup only; main integration completed the shared CSS/data wiring; visual reviewer `Copernicus` scored login, agents, connectors, and trace `4/5 PASS`, initially held memory at `3/5 FAIL`, then scored the curated memory rewrite `4/5 PASS`; code reviewer `Planck` found two Important issues (hidden memory citations and malformed ARIA table roles), both were fixed and `Planck` re-reviewed Ready/Yes.
- Backend/read-model reviewer `Ramanujan` rechecked all persona views: `/forensics` and `/run` pass data wiring; `/credit`, `/credit/command`, and `/cfo` were partial because some display facts, thresholds, generated IDs, command-centre rows, or assurance/basis labels still lived in route code/read-model demo shape. Since then, D5 command-centre rows are model-owned, CFO assurance/proof/status labels are model-owned, and `/credit` readout toolbar, Account-360 detail/summary rows, Sentinel detail rows/record-strip label, approval record-strip labels, action queue summary, partial-hold score/split/criteria labels, and terms-packet labels are model-owned. Current `/credit` residual route text is route chrome/table headers/buttons/aria, not business facts. Do not claim the overall goal complete until the remaining SDD gaps and final verify close.
- Latest explicit subagent workflow after the user's repeated workflow concern: explorer `Kepler the 2nd` found the exact real-hash plumbing gap, backend reviewer `Carson the 2nd` audited all-view read-model wiring and flagged remaining static/provenance gaps, visual reviewer `Halley the 2nd` confirmed no documented sub-`4/5` completion but reopened P1 premium caveats, implementer `Epicurus the 2nd` completed the real audit-hash slice with red-test evidence, spec reviewer `Sagan the 2nd` passed it, and code-quality reviewer `Hypatia the 2nd` returned Ready/Yes with no Critical or Important issues. All those agents were closed after use.
- Latest 2026-06-22 parallel workflow: implementation worker `Cicero the 2nd` fixed the sidebar active-route bug and Maya logout suppression in `cockpit/app/cockpit-shell.tsx` with an invariant assertion; focused verification passed (`npm.cmd run typecheck`, `tests/invariants/cockpit-no-business-logic.test.ts` `21/21`). Read-only SDD/status reviewer `Mencius the 2nd` corrected the stale checklist at that checkpoint before the final rerun: Crestline/M6 was partial thin-real, Capability D was not complete, production R-score/R-drift and run/eval gates remained open, and full SDD line-by-line review was still pending. Since then, bounded R-score/R-drift core, source-normalized Sentinel/R-drift wiring, the strict Supabase R-score component-score field/read path, and review-only Behavioral Containment action closure are closed, while live Supabase DML/read-back for the supplied R-score component values remains open. Visual auditor `Linnaeus the 2nd` scored Maya `/forensics` `3.5/5 FAIL` and identified sidebar/footer clipping, sidecar clipping, squeezed worklist queue, and card-everything density as blockers. A focused controller pass then fixed sidebar density/footer visibility, model-owned compact queue labels, worklist grid width, sidecar compactness, and screenshot capture reliability. Fresh reviewer `Faraday the 2nd` scored the recaptured Maya `/forensics` desktop screenshot `4/5 PASS`; residual polish is productizing raw component labels like `ToolStatusRail` / `MultimodalDoc`. After D5/test lint fixes, fresh broad e2e and `npm.cmd run verify` both passed.
- Latest explicit parallel reviewer wave after the user's subagent concern: SDD reviewer `Copernicus the 2nd` confirmed then-open full-solution gaps in R-score/R-drift execution, Behavioral Containment, production arbitration calibration, release/runtime control wiring, and post-approval audit display. The bounded R-score/R-drift deterministic core, source-normalized Sentinel/R-drift wiring, strict Supabase R-score component-score field/read path, review-only Behavioral Containment action closure, post-approval audit receipt visibility, OpenAI vector-store readiness, DB-backed release eval labels, and DB-backed runtime run-control gate have since been closed; live Supabase DML/read-back for the supplied R-score component values remains open. Backend/read-model auditor `Kant the 2nd` confirmed major surfaces are API/read-model backed through `cockpit-data.ts` and `src/services/cockpitModel.ts`, with residual centralized demo fixtures, component-local display overlays, and D5 discoverability as concerns. Visual reviewer `Feynman the 2nd` mixed mobile into scores after mobile was deferred, so that output is future mobile backlog only. Corrected desktop-only visual reviewer `Beauvoir the 2nd` reopened Maya `/run`, David `/credit`, `/login`, and governance routes below `4/5`. All four agents were closed after use.
- Durable approval finality slice details: `/approval` now prepares the approval through service-layer validation, claims insert-only durable finality as `approval:<actionId>` before `approvals.decide` consumes process-local finality when SQLite/Supabase memory is configured, maps duplicate claims to `409`, maps durable claim/write outages to `503`, persists the service audit hash back to the approval record, and preserves normal memory upsert behavior for non-finality records. Verification: `npm.cmd run typecheck` passed; `npm.cmd run test -- tests\unit\cockpit-api.test.ts` passed (`32/32` at that point; now `37/37` after role/auth tests); targeted backend/invariant suite passed (`13` files / `142` tests). Residual: if the post-service audit-hash enrichment write fails after a durable claim succeeds, the client sees `503` while process-local finality is already consumed; treat as an operational hardening follow-up, not a blocker for this bounded slice.
- Role-principal mapping slice details: new `config/cockpitHumanPrincipals.ts` centralizes `maya -> human:maya-lead`, `david -> human:david-lead`, and `cfo -> human:cfo-lead`. Next proxy routes attach request-bound proof headers (`purpose`, method/path, body hash, issuedAt, nonce, role, principal) after verifying signed demo sessions. The API rejects direct role-derived principals unless they match the configured direct principal, accepts signed proxy principals only for route allowlists, consumes proxy nonces to block replay, rejects body tampering, and requires `RECOUP_DEMO_SESSION_SECRET` rather than falling back to `RECOUP_COCKPIT_AUTH_TOKEN`. Verification: typecheck passed and targeted backend/auth/invariant suite passed (`13` files / `142` tests). Reviewer history: `Hooke` failed the first broadening attempt; `Godel` passed the intermediate spec; `Archimedes` found replay/static-secret issues; both fixes are now implemented/tested; final reviewer `James` passed the request-bound proxy-proof slice with no Critical/Important/Minor issues.
- Maya `/run` visual remediation was previously scored `4/5 PASS` by reviewer `Curie`, but latest desktop-only reviewer `Beauvoir the 2nd` reopened it at `3.7/5 FAIL` for raw `ToolStatusRail` / `MultimodalDock` labels, snake-case feed labels, and cramped rail copy. Treat `/run` as open P1 desktop polish until a fresh desktop reviewer scores `>=4/5`.

Current runtime screenshot evidence:

- `output/playwright/desktop-review/maya-forensics-1440.png`
- `output/playwright/desktop-review/david-credit-1440.png`
- `output/playwright/desktop-review/cfo-1440.png`
- Full e2e screenshots also exist under `output/playwright/e2e/`, including `maya-run-1440.png`, `david-command-1440.png`, `login-1440.png`, `governance-agents-1440.png`, `governance-connectors-1440.png`, `governance-memory-1440.png`, and `governance-trace-1440.png`.

Latest Maya desktop capture:

- Path: `output/playwright/e2e/maya-forensics-1440.png`
- Viewport: `1440x900`
- Latest verification: `npm.cmd run typecheck` passed; focused cockpit/invariant contract tests passed after the Maya source/journey work (`4` files / `80` tests); after the sidebar/logout slice, `tests/invariants/cockpit-no-business-logic.test.ts` passed (`21/21`); after the latest layout/queue pass, `npm.cmd run test -- tests\unit\cockpit.test.ts tests\invariants\cockpit-v12-contract.test.ts tests\invariants\cockpit-no-business-logic.test.ts` passed (`3` files / `42` tests). A narrow Maya-only Playwright desktop capture succeeded and refreshed `output/playwright/e2e/maya-forensics-1440.png` after asserting the `Recoup deduction forensics` H1.
- Current visual state: the screen now follows the ImageGen topology more closely with `ToolStatusRail`, dense product nav with visible Maya/footer/sign-out, Maya journey chip, scenario-led worklist with model-owned compact queue labels (`Billing` / `Review`), central evidence dossier, six-step Maya journey, and right `MultimodalDoc` sidecar with readable recovery draft gate. Reviewer `Faraday the 2nd` scored this screenshot `4/5 PASS`. The previous all-route `npm.cmd run test:e2e` timeout was resolved by stopping stale repo-owned API watchers and rerunning on a fresh port. Fresh broad e2e now passed and refreshed `output/playwright/e2e/*` screenshots.

Latest Maya `/run` desktop capture:

- Path: `output/playwright/e2e/maya-run-1440.png`
- Viewport: `1440x900`
- Latest verification before reviewer: `npm.cmd run typecheck` passed, focused cockpit/realtime tests passed (`39/39`), and `npm.cmd run test:e2e` passed with refreshed screenshots.
- Visual state: `/run` has a product-like source rail, dense module sidebar, scenario worklist, active recovery dossier, named MultimodalDock subagents, guarded Realtime evidence query, and audit checkpoint rail. Reviewer `Curie` scored it `4/5 PASS`, but latest desktop-only reviewer `Beauvoir the 2nd` reopened it at `3.7/5 FAIL`; fix raw labels and rail/feed copy before re-closing.

Latest David desktop capture:

- Path: `output/playwright/e2e/david-credit-1440.png`
- Viewport: `1440x900`
- Visual state: latest remediation reshaped the page into the ImageGen arbitration topology: compact Account-360 header, slimmer Sentinel warning rail, release workbench, proactive terms action packet with command buttons, David action queue, and topology-like Risk Mesh with owner/link/status panel. David approval REST was verified by `McClintock` (`tests/unit/cockpit-api.test.ts`, 28 tests passed). Reviewer `Meitner` previously scored the refreshed `/credit` screenshot `4/5 PASS`, but latest desktop-only reviewer `Beauvoir the 2nd` reopened it at `3.2/5 FAIL` because the Risk Mesh supervisor graphic overlaps the center value/name and hierarchy is weaker than the cue. Treat this as the top visual P0.

Latest David D5 desktop capture:

- Path: `output/playwright/e2e/david-command-1440.png`
- Viewport: `1440x900`
- Visual state: `/credit/command` now exists as a backend-wired dark D5 command centre using `fetchCreditModel`, scoped CSS module styling, and e2e screenshot capture. It includes command sidebar, tool status rail, monitor tape, stat strip, exposure board, live bureau feed, behavioral signal rails, active arbitrations, re-underwriting, Risk Mesh queue, audit status, and system feed. Reviewers `Gibbs` and `Beauvoir` both scored earlier screenshots `3/5 FAIL`; reviewer `Chandrasekhar` scored a later pass `4/5 PASS`; all-persona auditor `Bernoulli` reopened D5 at `3/5 FAIL`, then `Meitner` exposed a CSS grid bug that made the body render blank at `2/5 FAIL`. The latest fixed-grid pass uses explicit grid placement, removed primary `Pending ERP`, refreshed e2e screenshots, and reviewer `Raman` scored D5 `4/5 PASS`. D5 visual audit is closed for the active desktop persona route.

Latest CFO desktop capture:

- Path: `output/playwright/e2e/cfo-1440.png`
- Viewport: `1440x900`
- Visual state: board-pack remediation now renders report metadata, CFO board metric ledger, model-backed AuditVerifyChip, proof posture table, open dependency table, change ledger, AI insight, and provenance footer from the CFO read model. First reviewer `Averroes` scored the intermediate pass `3/5 FAIL`; after raw-hash/ID copy was removed and hierarchy tightened, reviewer `Peirce` scored `/cfo` `4/5 PASS`. Residual polish is non-blocking: the page is still more austere/internal than the cue.

Latest login/governance desktop captures:

- Paths: `output/playwright/e2e/login-1440.png`, `output/playwright/e2e/governance-agents-1440.png`, `output/playwright/e2e/governance-connectors-1440.png`, `output/playwright/e2e/governance-memory-1440.png`, and `output/playwright/e2e/governance-trace-1440.png`.
- Viewport: `1440x900`.
- Visual state: `/login` renders a product entry workstation with a governed identity rail, role-scoped persona selector, access-boundary ledger, and connector read-model `ToolStatusRail`. Governance routes render command strips, icon navigation, source/proof tiles, compact structured data rows, side proof rails, and a curated memory evidence surface with citations kept reachable. Reviewer `Copernicus` previously scored these `4/5 PASS`, but latest desktop-only reviewer `Beauvoir the 2nd` reopened `/login` at `3.3/5 FAIL` and governance routes at `3.1-3.4/5 FAIL` for whitespace, raw component labels, schema/table substrate, raw memory keys/chips, hash-heavy primary surfaces, and diagnostic feel.

Current implementation slice:

- `AGENTS.md`: added cockpit anti-slop standard from the user's Catalog of Tells.
- `src/agents/riskMesh.ts`: exposes existing partial-hold scores/weights through the Harbor context so UI charts can render real backend data.
- `src/services/cockpitModel.ts`: expanded Maya, David, and CFO read models with display-ready but deterministic fields: scenario-level worklist, source tiles, MultimodalDock rows, approval action labels, Account-360 detail/summary rows, Sentinel signals, criteria rows, human labels, CFO board metrics, dependency table rows, provenance, and latest David `/credit` readout/Sentinel/action-queue/partial-hold/terms display labels.
- `cockpit/app/cockpit-data.ts`: mirrors the expanded API contracts, including the latest David `/credit` Account-360 detail/summary rows plus readout/Sentinel/action-queue/partial-hold/terms label fields.
- `cockpit/app/cockpit-shell.tsx`: persona module rail is now the single product rail; module rows with real routes are actual links and filtered through `session.allowedRoutes`.
- `cockpit/app/premium-components.tsx`: source rail user-facing label changed from literal `ToolStatusRail` to `Source readiness`; graph uses human function labels; source rail and MultimodalDock rows now consume read-model data instead of local static arrays/timestamps.
- `cockpit/app/forensics/page.tsx`: Maya source rail moved above KPIs; scenario-level worklist, evidence-centered workbench, model-backed dock, and model-backed approval controls added; approval now appears after evidence to match the anti-rubber-stamp journey.
- `cockpit/app/forensics/page.tsx`: latest Maya remediation reduced squeezed worklist columns, moved AuditVerify above the dock, and added a real journey-node spine.
- `cockpit/app/credit/page.tsx`: David now renders Account-360, Sentinel alert, score criteria table, Risk Mesh graph, and Action packet; no React-side dollar/score arithmetic. The route now consumes model-owned readout toolbar labels, Account-360 detail/summary rows, Sentinel detail/record-strip labels, approval record-strip labels, action queue summary, partial-hold score/split/criteria labels, and terms-packet labels.
- `src/services/cockpitModel.ts`, `cockpit/app/cockpit-data.ts`, and `cockpit/app/credit/page.tsx`: latest audit-hash slice exposes real Risk Mesh `chainHeadHash`, `arbitrationHash`, and `entryHashes`; CFO `provenance.auditHash` now uses the real Risk Mesh chain-head entry hash instead of the previous 12-character summary digest; Credit passes the model hash directly to `AuditVerifyChip`.
- `cockpit/app/credit/command/page.tsx` and `cockpit/app/credit/command/command.module.css`: D5 dark command centre route now renders from the credit read model with scoped styling and e2e screenshot coverage.
- `cockpit/app/cfo/page.tsx`: CFO now renders report metadata, board KPI ledger, model-backed AuditVerifyChip, proof posture table, dependency table, change ledger, AI insight, and provenance footer from `CfoSummaryCockpitModel`; no top connector rail, no primary raw hashes/record IDs, and no route-local executive label helpers or proof-count construction.
- `cockpit/app/login/page.tsx` and `cockpit/app/login/login-form.tsx`: login now renders a product entry workstation backed by `fetchConnectorReadinessModel`, with role-scoped persona selection, access-boundary proof, and a source-status rail instead of a centered generic auth card.
- `cockpit/app/governance/agents/page.tsx`, `cockpit/app/governance/connectors/page.tsx`, `cockpit/app/governance/memory/page.tsx`, `cockpit/app/governance/trace/page.tsx`, and `cockpit/app/governance/governance-nav.tsx`: governance routes now render dense URL-backed control surfaces from the existing read models; memory was curated away from raw schema/chip-cloud presentation while preserving all cited record IDs.
- `cockpit/app/styles.css`: desktop density pass, integrated Maya workbench, removal of thick colored accent bars/inset rails, and persona-specific David/CFO classes. Large shared stylesheet diff existed from the broader active revamp; avoid unrelated churn.
- `cockpit/app/styles.css`: latest hardening converts the Maya source rail from card strip to compact row instrumentation, removes global/heading tracking, removes gradients/pseudo accent bars/inset rails, shortens source state noise, flattens nested card selectors, and replaces David's score orb with a score ledger.
- `cockpit/app/styles.css`: latest Maya remediation separates the workbench into three panes, restores compact source proof detail, improves the product rail density, allows key worklist/evidence text to wrap, removes sidecar internal scrolling, and compacts the action inbox.
- `tests/unit/cockpit.test.ts`: asserts expanded read-model fields.
- `tests/invariants/cockpit-no-business-logic.test.ts`: updated route-nav, scenario worklist, model-backed approval controls, evidence-before-approval, and hard anti-generated-UI invariants. It now blocks 2px+ directional rails, inset rails, pseudo accent bars, uppercase/tracking-wide tells, non-zero letter spacing, gradients, orb naming, nested card selectors, and spacious card padding/min-height regressions across cockpit CSS/markup.
- `tests/invariants/cockpit-v12-premium-components.test.ts`: updated premium contract for Source readiness, model-backed dock/source tiles, and David/CFO persona layout classes.
- `tests/e2e/cockpit-premium-e2e.ts`: updated runtime assertions for Account-360, Action packet, CFO board metric ledger, CFO provenance footer, and D5 screenshots.

Subagent audit results captured:

- Historical visual anti-slop subagent: scored the then-current screenshots Maya `2/5`, David `1/5`, CFO `2/5`; flagged generic KPI strips, nested/card-everything composition, raw proof/debug strings, and David's score/graph trope. Later reviewer-scored remediation closed Maya `/forensics`, Maya `/run`, David `/credit`, D5 `/credit/command`, and CFO `/cfo` at `4/5`.
- CSS/invariant subagent: confirmed no active uppercase/tracking/gradient/glass violations, but flagged `forensics-kpi-strip`, `cfo-kpi-strip`, `executive-strip`, and `graph-score-ring` as remaining generated-dashboard tells plus regex gaps.
- Historical full-solution SDD/wiring subagent result: highest-risk gap was David HITL approval wiring. It also flagged Maya evidence-open gating as copy-only, the dock as read-model-backed rather than live Realtime, missing Crestline containment handoff, missing David D5 at that time, and CFO AuditVerify/drilldown gaps. David HITL, D5, CFO visual/readout, Maya-only Realtime proxy auth, bounded cockpit API durable approval finality, local role-principal mapping, Credit/CFO real audit-hash surfacing, deterministic Governance Trace audit-entry fidelity, login persona read-model ownership, Governance Memory provenance honesty, bounded R-score/R-drift core, source-normalized Sentinel/R-drift wiring, strict Supabase R-score component-score field/read path, review-only Crestline containment action closure, post-approval audit receipt visibility, DB-backed release eval labels, and DB-backed runtime run-control gating have since landed. Live Supabase DML/read-back for the supplied R-score component values and broader read-model ownership remain open.
- Follow-up SDD/backend reviewer `Fermat`: David HITL local browser scope is now closed, but P0/P1 gaps remain: approval finality stored only in a process-local `Set`, role-specific human principal mapping incomplete, Crestline containment read model missing, and some audit chips not backed by real hashes. The SDD/config arbitration-weight wording contradiction is closed: SDD §5.7 / Appendix G now document owner-ratified Day-1 governed config seed values while keeping production calibration VERIFY-PROD. Realtime proxy auth for `/run` is now widened only for Maya demo-session cookies on both query routes; David/CFO demo cookies remain rejected.

Corrections applied after those audits:

- Runtime class families `forensics-kpi-strip`, `cfo-kpi-strip`, `executive-strip`, and `graph-score-ring` were removed and replaced with `scenario-metric-ledger`, `board-metric-ledger`, `control-ledger`, and `arbitration-score-cell`.
- SDD/config arbitration-weight wording is reconciled: the SDD no longer calls the Day-1 arbitration P&L weights placeholders or pending expert input, and it preserves the rule that Codex must never invent or change expert-owned constants. Production arbitration-weight calibration remains VERIFY-PROD.
- `tests/invariants/cockpit-no-business-logic.test.ts` now blocks the removed class families, `score-ring`/`score-orb`, broader pseudo-element accent bars, inset rails, and camelCase uppercase/tracking style props.
- Source readiness now emits seven product sources in cue order: SAP OData, TPM, 3PL POD, Bureau, Remittance / EDI, Contract Repo, MCP. The 3PL POD source is explicitly synthetic-labelled rather than shown as live.
- `ToolStatusRail` now renders source tiles with Phosphor icons instead of letter-only source marks.

Recent verification during the active revamp:

- `npm.cmd run lint` passed after the David/CFO read-model-backed revamp.
- `npm.cmd run typecheck` passed after the David/CFO read-model-backed revamp.
- `npm.cmd run test -- tests\invariants\cockpit-v12-premium-components.test.ts tests\invariants\cockpit-no-business-logic.test.ts tests\unit\cockpit.test.ts` passed after the David/CFO read-model-backed revamp.
- `npm.cmd run test:e2e` passed after the David/CFO read-model-backed revamp and wrote screenshots to `output/playwright/e2e/`.
- `npm.cmd run verify` passed earlier after the rail/orb/anti-tell hardening: `66` test files, `349` tests, and no dependency-cruiser violations. This is **historical evidence**, not the current final gate.
- After the subagent-driven anti-tell patch, `npm.cmd run typecheck` passed and `npm.cmd run test -- tests\invariants\cockpit-no-business-logic.test.ts tests\invariants\cockpit-v12-premium-components.test.ts tests\unit\cockpit.test.ts` passed (`25` tests).
- After the login fallback and dock policy-label fix, `npm.cmd run test:e2e` passed and wrote refreshed screenshots to `output/playwright/e2e`.
- Historical full repo gate after the David Risk Mesh CSS/test fix: `npm.cmd run verify` passed with ESLint, TypeScript, Vitest (`67` files / `399` tests), and Dependency Cruiser (`96` modules / `247` dependencies). This is superseded by the fresh `79` files / `555` tests verify recorded in the current backend checkpoint above.
- Login e2e root cause and fix: `npm.cmd run test:e2e` initially exposed a hydration race where the login form submitted as `GET /login?loginId=...&password=...` before the client handler mounted. The login form now has a server POST fallback to `/api/demo-login`, and the API route accepts both JSON and formData, redirecting on form POST with the signed session cookie. After adding the `voice/text citation parity` policy label to the dock read model, `npm.cmd run test:e2e` passed and wrote refreshed screenshots to `output/playwright/e2e`.
- David HITL focused verification after reviewer fixes: `npm.cmd run lint` passed, `npm.cmd run typecheck` passed, and `npm.cmd run test -- tests\unit\realtime-next-routes.test.ts tests\invariants\cockpit-no-business-logic.test.ts tests\unit\cockpit-api.test.ts` passed (`51` tests).
- Historical full repo gate after David HITL reviewer loop: `npm.cmd run verify` passed with `66` test files, `354` tests, and no dependency-cruiser violations; this is superseded by later full verify records above.
- David visual focused verification after the latest reviewer-driven pass: `npm.cmd run typecheck` passed, `npm.cmd run test -- tests\invariants\cockpit-no-business-logic.test.ts tests\invariants\cockpit-v12-premium-components.test.ts tests\unit\cockpit.test.ts` passed (`26` tests), and `npm.cmd run test:e2e` passed with refreshed screenshots under `output/playwright/e2e/`.
- Historical full repo verification after the David HITL/visual patch: `npm.cmd run verify` passed with ESLint green, TypeScript green, Vitest `66` files / `354` tests green, and Dependency Cruiser green (`94` modules, `232` dependencies). This is no longer a final visual gate because the stricter all-persona reviewer reopened David.
- Maya visual slice verification after the latest anti-tell/remediation patch: `npm.cmd run typecheck` passed, `npm.cmd run test -- tests\invariants\cockpit-no-business-logic.test.ts tests\invariants\cockpit-v12-premium-components.test.ts tests\unit\cockpit.test.ts` passed (`26` tests), `npm.cmd run test:e2e` passed with refreshed screenshots under `output/playwright/e2e/`, and reviewer `Aristotle` scored Maya `/forensics` `4/5 PASS`.
- David/D5 visual remediation verification after the latest reviewer-driven passes: `npm.cmd run typecheck` passed; focused cockpit/D5 tests passed (`27/27`); `RECOUP_E2E_COCKPIT_PORT=3025 npm.cmd run test:e2e` passed and captures `output/playwright/e2e/david-credit-1440.png`, `output/playwright/e2e/david-command-1440.png`, `output/playwright/e2e/login-1440.png`, and governance screenshots. Reviewer `Meitner` passed David at `4/5`; reviewer `Raman` passed D5 at `4/5`.
- CFO board-pack remediation verification: `npm.cmd run typecheck` passed; focused cockpit tests passed (`26/26`); `npm.cmd run test:e2e` passed and refreshed `output/playwright/e2e/cfo-1440.png`. Reviewer `Averroes` scored the first remediation `3/5 FAIL`; reviewer `Peirce` scored the final remediated CFO screenshot `4/5 PASS`.
- Maya `/run` remediation verification: `npm.cmd run typecheck` passed; focused cockpit/realtime/invariant tests passed (`39/39`); `npm.cmd run test:e2e` passed and refreshed `output/playwright/e2e/maya-run-1440.png`. Reviewer `Curie` scored the final remediated `/run` screenshot `4/5 PASS`.
- Durable approval finality and role-principal verification: `npm.cmd run typecheck` passed; `npm.cmd run test -- tests\unit\realtime-next-routes.test.ts tests\unit\cockpit-api.test.ts tests\invariants\cockpit-role-auth.test.ts tests\unit\realtime-session.test.ts tests\unit\realtime-browser-session.test.ts tests\unit\supabase-memory.test.ts tests\unit\sqlite-memory.test.ts tests\unit\runtime-memory.test.ts tests\invariants\cockpit-no-business-logic.test.ts tests\invariants\action-hitl-all-capabilities.test.ts tests\invariants\integration-contract.test.ts tests\invariants\sod.test.ts tests\invariants\memory-contract.test.ts` passed (`13` files / `142` tests). Spec reviewer `Wegener`: PASS for durable finality. Code-quality reviewer `Laplace`: Ready/Yes for durable finality. Role-principal reviewer `James`: Ready/Yes with no Critical/Important/Minor issues after tests covered body tamper, nonce replay, no bearer-token fallback, direct CFO rejection, and CFO proxy rejection.
- Run-control/eval release-gate verification: added `buildReleaseReadinessReport` in `evals/harness.ts`; blocked run control, blocked eval labels, and failing metrics now produce release `fail`, while all-pass inputs produce release `pass`. `npm.cmd run test -- tests\evals\accuracy-bars.test.ts tests\invariants\run-control.test.ts` passed (`2` files / `20` tests), `npm.cmd run typecheck` passed, and reviewer `Darwin` returned Ready/Yes with no Critical or Important issues. This does not invent Appendix G run budgets or missing intent/arbitration labels; it makes their absence release-blocking.
- Login/governance visual remediation verification: `npm.cmd run typecheck` passed; `npm.cmd run test -- tests\unit\cockpit.test.ts tests\invariants\cockpit-no-business-logic.test.ts tests\invariants\cockpit-v12-premium-components.test.ts` passed (`3` files / `26` tests); a narrow Playwright desktop screenshot pass refreshed `/login` and `/governance/{agents,connectors,memory,trace}` at `1440x900`. Reviewer `Copernicus` scored all five routes `4/5 PASS` after the memory rewrite. Reviewer `Planck` initially returned Ready/No for hidden memory citations and malformed ARIA table roles; fixes preserved all citations, removed malformed roles, derived connector operation summary from the read model, and `Planck` re-reviewed Ready/Yes.
- Real audit-hash surfacing verification: implementer `Epicurus the 2nd` first made `tests/unit/cockpit.test.ts` fail because `model.audit.chainHeadHash` was undefined and CFO still emitted the 12-character `b7ec95ec90dc`; after the fix, `npm.cmd run test -- tests\unit\cockpit.test.ts tests\invariants\integration-contract.test.ts tests\invariants\cockpit-no-business-logic.test.ts` passed (`3` files / `31` tests), and `npm.cmd run typecheck` passed. Spec reviewer `Sagan the 2nd` passed; code reviewer `Hypatia the 2nd` found no Critical or Important issues. Minor residual: `riskMeshAuditHashes()` assumes the `arbitration.blocked` entry taxonomy and tests mirror the deterministic Risk Mesh run.
- Credit approval citation verification: implementer `Meitner the 2nd` first made focused tests fail because `approvalInbox[].recordIds` was undefined and the credit page did not render `RecordStrip` from `item.recordIds`; after the fix, hold/terms approval rows carry `run.holdAction.recordIds` and `run.termsAction.recordIds`, and the David action packet renders visible per-action citations beside HITL controls. Spec reviewer `Boole the 2nd` initially failed the broad dirty diff as overreach, then passed after corrected slice scoping. Code reviewer `Huygens the 2nd` found an Important layout/visibility issue because action-table code chips were hidden; the fix moved citations into `credit-action-basis`, removed the scoped hidden-code rule, added a regression assertion, and code re-reviewer `Noether the 2nd` returned Ready/Yes. Verification: `npm.cmd run test -- tests\unit\cockpit.test.ts tests\invariants\cockpit-no-business-logic.test.ts` passed (`2` files / `24` tests), `npm.cmd run typecheck` passed, and `RECOUP_E2E_COCKPIT_PORT=3025 npm.cmd run test:e2e` passed with refreshed screenshots.

Immediate next steps:

1. P0/P1 backend: Maya-only Realtime query proxy auth, bounded cockpit API durable approval finality, post-approval audit receipt visibility, OpenAI vector-store metadata enforcement/readiness, role-derived demo proxy principal mapping, explicit release-failing aggregation for blocked run-control/eval states, DB-backed release eval labels, DB-backed runtime run-control gating, DB-backed live retry/token budget telemetry, Credit/CFO real audit-hash surfacing, credit approval row citations, deterministic Governance Trace audit-entry rows, login persona read-model ownership, Governance Memory provenance honesty, D5 command-centre read-model ownership, CFO executive proof/status labels, David `/credit` toolbar/Sentinel/action-queue/partial-hold/terms/Account-360 label ownership, Crestline/M6 thin-real risk-review readout, bounded R-score/R-drift core, source-normalized Sentinel/R-drift wiring, the Supabase R-score component-score source column/read path, and review-only Behavioral Containment action closure are patched/tested/reviewed or locally verified. Round 2 owner inputs supplied Risk Mesh option values, R-score component scores, and the decision-confidence threshold; local tests cover the default ranked Risk Mesh, source-owned R-score seed SQL, and present-threshold decision confidence paths. Remaining backend gaps are live Supabase DML/read-back or main-thread verify proof for those supplied rows, deterministic confidence-score source evidence, production vector-store ID/upload/indexing, owner policy/order data for any Crestline hold/freeze/partial-hold beyond review-only, and final all-view SDD/release verification.

Latest login persona ownership slice: worker `Schrodinger the 2nd` produced the expected RED tests for missing `buildLoginModel`, missing `GET /login`, and client-hardcoded personas. The implementation added `config/cockpitDemoProfiles.ts`, `buildLoginModel()`, `GET /login`, `fetchLoginModel()`, and `LoginForm` persona props while preserving Supabase-backed `/api/demo-login` credential verification. Focused verification passed: `npm.cmd run test -- tests\unit\cockpit.test.ts tests\unit\cockpit-api.test.ts tests\unit\cockpit-demo-auth.test.ts tests\invariants\cockpit-no-business-logic.test.ts tests\invariants\cockpit-v12-contract.test.ts` (`75/75`) and `npm.cmd run typecheck`. Spec reviewer `Nash the 2nd` passed; code-quality reviewer `Kierkegaard the 2nd` returned Ready/Yes with no Critical or Important findings.

Latest Governance Memory provenance slice: explorer `Franklin the 2nd` confirmed `/memory` could serve Supabase, SQLite, or deterministic fallback records without exposing which source was used. Worker `Parfit the 2nd` began the RED unit coverage; the controller completed API and UI/invariant RED tests, producing 5 expected failures for missing `backend`, `sourceMode`, and `provenance`. The implementation adds model-level memory metadata: Supabase/SQLite return `runtime_persisted` / `persisted_runtime_memory`, while no durable backend returns `in_memory_fallback` / `deterministic_demo_fallback` / `deterministic_demo_memory`. Governance Memory renders those fields with explicit fallback wording. Focused verification passed: `npm.cmd run test -- tests\unit\cockpit.test.ts tests\unit\cockpit-api.test.ts tests\invariants\cockpit-v12-contract.test.ts` (`54/54`) and `npm.cmd run typecheck`. Spec reviewer `Ramanujan the 2nd` passed; stalled code reviewer `Dalton the 2nd` was terminated and replacement reviewer `Lovelace the 2nd` returned Ready/Yes with no Critical or Important findings.
Latest David D5 command-centre read-model ownership slice: worker `Chandrasekhar the 2nd` added RED coverage and moved the command status rail, stats, exposure rows, feed rows, signal rows, audit rows, and market tape into `CreditCockpitModel.commandCenter`; `/credit/command` now consumes `model.commandCenter` instead of route-local command row builders. The controller corrected one worker test expectation that guessed a deterministic criterion value instead of mirroring the model. Focused verification passed: `npm.cmd run test -- tests\unit\cockpit.test.ts tests\invariants\cockpit-v12-contract.test.ts tests\invariants\cockpit-no-business-logic.test.ts tests\unit\cockpit-api.test.ts` (`73/73`) and `npm.cmd run typecheck`. Spec reviewer `Anscombe the 2nd` returned partial after timeout and was closed; replacement reviewer `Kuhn the 2nd` returned PASS. Code reviewer `Averroes the 2nd` returned partial after timeout and was closed; replacement reviewer `Gibbs the 2nd` returned Ready/Yes with no Critical or Important blockers.
Latest CFO executive proof/status label ownership slice: worker `Russell the 2nd` first returned partial with no edits, then continued with RED tests for missing CFO model-owned labels and route helper removal. The slice moves CFO `valueLabel`, `supportLabel`, `basisLabel`, `recordCountLabel`, provenance labels, `assurance`, and `readoutStatusLabels` into `CfoSummaryCockpitModel`; `/cfo` now renders those fields directly and no longer defines `executiveMetadataValue`, `executiveBasis`, `recordCountLabel`, or `humanize`. The controller also fixed the old route-helper copy bugs for `human approval approval` and the unreachable `synthetic seed 42 plus` label. Focused verification passed: `npm.cmd run test -- tests\unit\cockpit.test.ts tests\invariants\cockpit-no-business-logic.test.ts tests\invariants\cockpit-v12-contract.test.ts tests\unit\cockpit-api.test.ts` (`75/75`) and `npm.cmd run typecheck`. Spec reviewer `Erdos the 2nd` returned PASS. Code reviewer `Socrates the 2nd` found one Important route-local toolbar proof-count issue; after `readoutStatusLabels` fixed it, replacement reviewer `Avicenna the 2nd` returned Ready/Yes with no Critical or Important blockers.
Latest David `/credit` toolbar/Sentinel/action-queue label ownership slice: worker `Harvey the 2nd` was terminated after no checkpoint/final output; replacement worker `Lorentz the 2nd` observed RED failures for missing `model.readoutStatusLabels`, missing route model fields, and route-composed action record labels, then stalled after useful partials. The controller completed the narrow implementation: `CreditCockpitModel` now owns `readoutStatusLabels`, `sentinel.detailRows`, `sentinel.recordStripLabel`, `approvalInbox[].recordStripLabel`, and `actionQueueSummaryLabel`; `/credit` renders those fields directly; and `cockpit-data.ts` mirrors the API contract. Focused verification passed: `npm.cmd run test -- tests\unit\cockpit.test.ts tests\invariants\cockpit-no-business-logic.test.ts tests\invariants\cockpit-v12-contract.test.ts tests\unit\cockpit-api.test.ts` (`77/77`) and `npm.cmd run typecheck`. Spec reviewer `Jason the 2nd` returned PASS. Code reviewers `Pascal the 2nd` and `Aristotle the 2nd` were terminated after missing checkpoint/final windows; final reviewer `Locke the 2nd` returned Ready with no Critical or Important findings. Remaining David `/credit` route-local facts are partial-hold score/split/criteria and terms-packet labels, intentionally left for the next slice.
Latest David `/credit` partial-hold/terms label ownership slice: worker `Raman the 2nd` was terminated after no checkpoint/final output, but left useful RED tests requiring `model.partialHold.scoreReadout`, `releaseReadout`, `splitRows`, `ledgerRows`, `criteriaHeaders`, and `model.termProposal` packet/command labels. The controller observed RED, moved those fields into `CreditCockpitModel`, mirrored them in `cockpit-data.ts`, and updated `/credit` to render from the model. Focused verification passed: `npm.cmd run test -- tests\unit\cockpit.test.ts tests\invariants\cockpit-no-business-logic.test.ts tests\invariants\cockpit-v12-contract.test.ts tests\unit\cockpit-api.test.ts` (`77/77`) and `npm.cmd run typecheck`. Spec reviewer `Newton the 2nd` returned PASS. Code reviewer `Arendt the 2nd` found two Important issues; the controller fixed raw top-summary score/ratio usage and narrowed over-broad invariant string bans, then re-ran verification (`77/77` + typecheck). Re-reviewer `James the 2nd` returned Ready with no Critical or Important findings. Remaining David `/credit` route-local facts are account identifier/stat labels and route chrome.
2. P1 review: continue the full-solution SDD checklist line-by-line; do not scope it only to persona screens. The owner-input migration and DB-backed release-readiness wiring were superseded by later backend gates; the latest recorded full backend proof before Round 2 owner-input implementation is the optional decision-confidence threshold gate with `79` files / `564` tests, Dependency Cruiser `109` modules / `327` dependencies, and live Supabase `verify:release`. Do not call the Round 2 docs/config state fully verified until the main thread runs and records a fresh full gate.
3. P0/P1 visual polish: latest desktop-only reviewer `Beauvoir the 2nd` scored David `/credit` `3.2/5 FAIL` before the Risk Mesh overlap fix; `Lagrange the 2nd` landed the fix and historical e2e/component verify were green before release-readiness enforcement, so David needs re-review before closure. Maya `/run` remains `3.7/5 FAIL`, `/login` remains `3.3/5 FAIL`, and governance routes remain `3.1-3.4/5 FAIL`. Maya `/forensics`, D5, and CFO remain desktop-pass. Mobile findings from `Feynman the 2nd` are future backlog only because mobile is deferred.

## Last Historical Full Verification Evidence

Last known full repo gate passed before the current David HITL reviewer workflow:

```powershell
npm.cmd run verify
```

Result:

- ESLint passed.
- TypeScript typecheck passed.
- Vitest passed: `66` test files, `349` tests.
- Dependency Cruiser passed with no dependency violations.

Note: this historical full verify was later superseded by component gates and then by release-readiness enforcement. The latest recorded full verify before Round 2 owner-input implementation passed with Supabase-backed `verify:release`; this docs-only update does not claim a fresh full verify.

## High-Level Build Status

Recoup is now a deterministic, agent-ready O2C recovery cockpit for the NorthBay demo story. The implemented build keeps the repo contract intact:

- Code computes dollar amounts.
- Decisions cite record IDs and deterministic basis.
- SAP remains read-only.
- External actions remain human-approved and draft-only.
- Expert-owned constants are not invented.

## Completed Work

### Supabase And Durable Memory

- Supabase/Postgres is wired as the primary shared durable memory path when `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are configured.
- SQLite remains the local/offline fallback when `RECOUP_MEMORY_DB_PATH` is configured.
- In-memory store remains for test/demo harness paths without durable config.
- Memory categories are typed and scoped: session, workflow, case, transaction, evidence, approval, audit, connector, artifact, compaction, and agent-handoff state.
- Supabase schema SQL is captured in `docs/supabase-memory-schema.sql`.
- Postgres `timestamptz` values are normalized to Recoup ISO memory timestamps before Zod validation.
- Tests cover schema SQL, REST upsert/list behavior, timestamp normalization, runtime backend selection, and SQLite fallback.

Key files:

- `src/memory/supabaseStore.ts`
- `src/memory/sqliteStore.ts`
- `src/memory/runtime.ts`
- `src/memory/schema.ts`
- `src/memory/session.ts`
- `tests/unit/supabase-memory.test.ts`
- `tests/unit/sqlite-memory.test.ts`
- `tests/unit/runtime-memory.test.ts`
- `tests/invariants/memory-contract.test.ts`

### SAP OData

- SAP OData adapter remains read-only.
- No ERP write-back path was added.
- OAuth and Gateway Basic auth are supported for read-only metadata/GET access.
- Scope and tenant are optional when the SAP endpoint does not require them.
- `docs/Tools_data` invoice reads are mapped to `ZUI_BILLINGDOCUMENTFS_0001/C_BillingDocumentFs`.
- `SAP_ODATA_CLIENT` or `SAP_ODATA_CLIENT_ID` supplies `sap-client` for metadata/read GETs when the SAP endpoint requires it.
- Non-numeric synthetic `INV-*` IDs fail closed instead of becoming live SAP reads.
- POD, credit memo, and duplicate-claim proof are no longer emitted as SAP fallback evidence.
- Historical Basic-auth metadata proof was captured for `FCOM_COSTCENTER_SRV/$metadata`; the current demo billing baseline is the `ZUI_BILLINGDOCUMENTFS_0001` invoice header plus `C_BillingDocumentItemFs` item collection path.
- The historical cost-center metadata proof exposed `CostCenterSet` and `F4_CostCenterSet`.
- SAP credential details are not serialized in runtime config output.
- The SAP skill is installed under `skills/sap-odata-access/SKILL.md`.

Key files:

- `src/adapters/sapOData.ts`
- `src/tools/retrieval/sap.ts`
- `skills/sap-odata-access/SKILL.md`
- `tests/unit/sap-odata.test.ts`
- `tests/invariants/no-erp-writeback.test.ts`
- `tests/invariants/integration-contract.test.ts`

### OpenAI Realtime Query Guard

- `gpt-realtime-2` is pinned behind a server-issued client-secret route.
- `POST /query/realtime-client-secret` requires verified cockpit human auth.
- The verified human principal is used as the `OpenAI-Safety-Identifier`.
- Realtime issuance gates only on `OPENAI_API_KEY`; unrelated SAP/Supabase config parsing cannot block it.
- Local live proof returned HTTP `200`, status `issued`, transport `webrtc`, and audit record `OPENAI-REALTIME-POLICY`.
- No OpenAI key or client secret value was printed.
- Browser voice/text remains a controlled demo surface wired through the guarded Realtime browser helper; production live-query policy and identity remain approval dependencies.

Important caveat:

- The local Windows/Node HTTPS path exposed `UNABLE_TO_GET_ISSUER_CERT_LOCALLY`.
- The live proof used one process-scoped TLS bypass only.
- Production/demo machines should fix the trusted CA chain rather than use TLS bypass.

Key files:

- `src/services/realtimeSession.ts`
- `src/services/cockpitApi.ts`
- `cockpit/app/realtime-query-controls.tsx`
- `cockpit/app/api/query/realtime-client-secret/route.ts`
- `tests/unit/realtime-session.test.ts`
- `tests/unit/cockpit-api.test.ts`

### Cockpit UI And API

- Cockpit has Forensics, Credit/Arbitration, CFO Summary, Agent Operations, memory, and trace bands.
- First viewport desktop SaaS polish was applied against the O2C design system.
- Mobile navigation wraps instead of clipping.
- Mobile worklist table is compact enough to keep amount cells visible.
- Approval controls are HITL-gated and route through the local API.
- Realtime client-secret controls are present but still a controlled demo affordance.
- Playwright/installed Chrome visual QA verified desktop `1440x900` and mobile `390x844`: nonblank render, no framework overlay, no console errors, and interaction focus.

Key files:

- `cockpit/app/page.tsx`
- `cockpit/app/styles.css`
- `cockpit/app/approval-controls.tsx`
- `cockpit/app/realtime-query-controls.tsx`
- `src/services/cockpitApi.ts`
- `src/services/cockpitModel.ts`
- `tests/unit/cockpit.test.ts`
- `tests/unit/cockpit-api.test.ts`
- `tests/invariants/cockpit-no-business-logic.test.ts`

### Agent Harness And Communication

- Offline-safe agent roster is declared for Forensics, Recovery Drafter, Risk Mesh, Sentinel, Containment, and Conversational Query.
- Forensics to Recovery handoff is represented in the implemented S4 boundary.
- Handoff graph and Zod handoff packets are implemented.
- Handoff packets carry `recordIds`.
- Query remains deterministic/offline-safe for answers and cites deterministic Harbor state.
- Risk Mesh/Sentinel/Containment remain deterministic harness modules. Owner-ratified Day-1 tunables are captured in `config/governed.ts` and Supabase `recoup_config` rows; older notes reference `CODEX_BUILD_ANSWERS.md`, but that file is not present in this checkout. The DB-backed governed config loader/injection and DB-backed runtime run-control gate are implemented; VERIFY-PROD calibration remains open for production operating policy.

Key files:

- `src/agents/agentRuntime.ts`
- `src/agents/forensics.ts`
- `src/agents/handoffGraph.ts`
- `src/agents/messages.ts`
- `src/agents/query.ts`
- `src/agents/riskMesh.ts`
- `tests/unit/agent-runtime.test.ts`
- `tests/unit/agent-handoffs.test.ts`
- `tests/unit/query.test.ts`

### MCP, Permissions, And Service Boundary

- MCP server uses `StreamableHTTPServerTransport`.
- `/mcp` is protected when MCP auth is configured.
- Tool metadata includes risk class, side-effect class, and MCP/internal visibility.
- Read-only MCP clients are denied draft action calls before handler execution.
- Approval, core compute, and decision tools remain internal and are not MCP-visible.
- Service tools are namespaced, typed, and Zod-validated.

Key files:

- `src/mcp/server.ts`
- `src/services/serviceLayer.ts`
- `src/services/permissionEngine.ts`
- `tests/invariants/mcp-transport.test.ts`
- `tests/invariants/mcp-visibility.test.ts`
- `tests/invariants/tool-permissions.test.ts`
- `tests/invariants/tool-whitelist.test.ts`

### Audit And Judge Evidence

- README now includes the demo narrative, OpenAI capability map, memory, skills, handoffs, permissions, and claim-to-code-to-test table.
- Independent audit evidence is captured outside chat in `docs/independent-audit-log.md`.
- Broader architecture review and open recommendations are captured in `docs/architecture-review-and-recommendations.md`.
- Judge-visible evidence includes Supabase, SAP, Realtime, MCP, UI, and connector-readiness status.

Key files:

- `README.md`
- `docs/independent-audit-log.md`
- `docs/architecture-review-and-recommendations.md`

## Skills Used During The Build

### Codex/Superpowers Process Skills

- `superpowers:using-superpowers`
- `superpowers:test-driven-development`
- `superpowers:systematic-debugging`
- `superpowers:verification-before-completion`
- `superpowers:writing-plans`
- `superpowers:executing-plans`
- `superpowers:dispatching-parallel-agents`
- `superpowers:subagent-driven-development`
- `superpowers:requesting-code-review`
- `superpowers:finishing-a-development-branch`

### Domain/Implementation Skills

- `build-web-apps:supabase-postgres-best-practices`
- `build-web-apps:frontend-testing-debugging`
- `build-web-apps:frontend-app-builder`
- `build-web-apps:react-best-practices`
- `openai-docs`

### Local Recoup Skills Added

- `skills/recoup-evidence-pack/SKILL.md`
- `skills/recoup-recovery-drafting/SKILL.md`
- `skills/recoup-risk-arbitration/SKILL.md`
- `skills/recoup-query-answering/SKILL.md`
- `skills/sap-odata-access/SKILL.md`

### Subagent / Independent Review Work

- Memory/Supabase review.
- SAP connector review.
- UI/UX cockpit visual review.
- Realtime client-secret/security review.
- Architecture and harness review.

## Pending Items

### Expert-Owned Tunables

Owner-ratified Day-1 tunables are captured in `config/governed.ts` and emitted as `recoup_config` v1 seed rows in `docs/supabase-memory-schema.sql`; older notes reference `CODEX_BUILD_ANSWERS.md`, but that file is not present in this checkout. Runtime service entry points now load governed values and run-control budgets from Supabase `recoup_config` and fail closed when the required DB rows are missing or invalid. Round 2 owner inputs supplied Risk Mesh option values, R-score component scores, and the decision-confidence threshold; remaining production calibration, live DML/read-back, and owner `[VERIFY]` flags must not be guessed:

- Live Supabase DML/read-back or main-thread verify proof for the supplied Risk Mesh option values, R-score component scores, and decision-confidence threshold.
- Deterministic confidence-score source evidence for production confidence gating.
- Production calibration/normalization for drift and gaming beyond the owner-ratified Day-1 governed rows and supplied Round 2 values.
- Embeddings model id.
- Codex build-model id.
- SAP sandbox instance.

### SAP / O2C Live Depth

- SAP read-only Gateway metadata proof exists, and the demo billing path has invoice header plus `C_BillingDocumentItemFs` billing-item collection reads.
- Live outbound-delivery, credit-memo/reference, and broader non-billing O2C services still need reachable endpoints.
- Need metadata-backed entity sets, key fields, and sample payloads for O2C evidence mapping beyond invoice header/item reads.
- ERP write-back remains out of scope.
- SAP must stay read-only.

### Realtime / Query

- Server-side Realtime client-secret path is live-proved.
- Browser voice/text session is wired through the guarded Realtime browser helper; production live-query policy remains an approval dependency.
- Live answers must remain constrained to deterministic query tools and cited Recoup records.
- Local Node/Windows CA trust should be fixed to avoid TLS bypass during demos.

### Supabase / Memory Scale Path

- Supabase runtime memory is wired and tested.
- SQLite fallback exists.
- Replay-grade audit/memory rehydration remains a scale-path item.
- Production RLS/auth principal model should be hardened beyond service-role-only hackathon access.

### MCP / Enterprise Deployment

- MCP StreamableHTTP auth and RBAC baseline are implemented.
- Secure MCP Tunnel/private-network hardening remains a scale-path item.
- Production IdP/OAuth/KMS decisions remain open.

### Connector Strategy

SAP remains the only live external adapter. Production `/run` and MCP consume Supabase-backed docs/TPM/bureau source-table evidence for R1, while broader live non-SAP contracts remain deferred behind canonical source ports:

- Bureau feed.
- Remittance.
- EDI.
- Document repository.
- TPM.

Implemented foundation:

- Added Supabase source-table definitions for bureau, docs, consolidated remittance/EDI, TPM, and the broader connector/readiness table set used by frontend source calls.
- Added Supabase synthetic table-reading adapters for bureau, docs, consolidated remittance/EDI, and TPM. These read with GET-only service-role REST calls, enforce `provenance = synthetic`, and map rows to canonical cited evidence.
- Production `/run` and MCP now preload required SAP/docs/TPM/bureau evidence from Supabase `recoup_src_sap`, `recoup_src_docs`, `recoup_src_tpm`, and `recoup_src_bureau` rows and fail closed when the Supabase evidence context is unavailable; static source data is test/setup-only.
- Connector readiness reports non-SAP sources as `ready_synthetic` only after a Supabase schema probe verifies the `docs/Tools_data` tables.
- Credentials without a successful probe report `blocked_schema_required`, not ready.
- Readiness blocks on missing/not-exposed `Tools_data` tables and unsafe shadow statuses such as `SENT_TO_SAP`, `SUBMITTED_TO_PORTAL`, or `SAP_STAGE_WRITE`.
- `docs/Tools_data/seed_data.sql` remains human-approval only because it truncates tables and seeds action/decision/audit-like rows.
- `recoup_src_remittance` is the shared source table for remittance and EDI-remittance via `transaction_set`.

Remaining implementation:

- Broader production non-SAP contracts, data ownership, and refresh policy remain deferred even though `/run` and MCP now consume Supabase source rows for docs/TPM/bureau.
- Wire the optional OpenAI vector-store evidence reader into a production service boundary only after the production vector-store ID and indexing/upload process are approved; the required metadata attribute policy is already enforced by `src/adapters/openAiVectorStore.ts`.
- Keep `provenance = synthetic` and semi-trusted connector state visible in any seeded connector records.
- Preserve the source-swap boundary so live contracts can replace synthetic adapters later without changing core/services.
- Keep real non-SAP live contracts deferred to VERIFY-V3.

### Cockpit Product Depth

- First-viewport desktop/mobile QA baseline is resolved.
- Deeper drilldowns and expanded workflows remain planned.
- Final O2C design review is still needed for expanded flows beyond the first viewport.

### Git / Repo Hygiene

- Local branch `codex/guardrail-riskmesh-hardening` contains the preserved EOL verification fix and current guardrail/Risk Mesh hardening work.
- This session branch has not been pushed.
- Open a PR only after `npm.cmd run verify` is green on the final diff. This docs-only handoff update does not claim a fresh full verify after the Round 2 owner-input progress.

## How To Run In VS Code

Open the repo folder:

```powershell
cd "C:\Rathish\Root Folder\CFO\Hackathon\Recoup1\Recoup"
code .
```

Install dependencies if needed:

```powershell
npm.cmd install
```

Run the full guard:

```powershell
npm.cmd run verify
```

Run the API:

```powershell
npm.cmd run dev:api
```

Run the cockpit:

```powershell
npm.cmd run dev:cockpit
```

Default API URL:

```text
http://127.0.0.1:4317
```

Open the Next.js URL printed by `dev:cockpit`.

## Demo Flow

The live story is a two-persona demo:

- Maya Patel is the Deduction Forensics analyst: she works the recovery queue, evidence pack, draft action, approval controls, run trace, and guarded Realtime query.
- David Kim is the Credit / Arbitration lead: he reviews Harbor through Sentinel, Risk Mesh arbitration, deterministic partial-hold split, draft terms, and pending human approvals.
- CFO Summary remains the read-only executive close.

1. Start on the Forensics queue and show recovery and billing-prevention drafts.
2. Open evidence pack and record IDs for a recovery line.
3. Use approval controls to show human approval and audit hash output.
4. Ask the guarded Realtime query why Harbor is blocked and show cited-or-blocked output.
5. Move to Credit/Arbitration and explain Harbor's governed partial-hold split.
6. Show Agent Operations: MCP surface, typed memory categories, handoff graph, and trace timeline.
7. Close with CFO Summary and explicitly call out open VERIFY-PROD calibration dependencies.

## Safety Reminders

- Do not print `.env.local` values.
- Do not commit `.env.local`; it is ignored.
- Use `npm.cmd`, not `npm`, in this Windows workspace.
- Node 25 is accepted for this hackathon, though the repo runtime target remains Node 22+ TypeScript.
- Do not add ERP write-back.
- Do not invent domain constants.
- For any rule, guard, score, memory, schema, or decision-producing change, use tests-first.
