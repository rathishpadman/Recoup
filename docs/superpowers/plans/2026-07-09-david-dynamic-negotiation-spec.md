# David Option 1 — Dynamic Fulfilment & Terms Negotiation — Phase-wise Spec

**Owner decisions (2026-07-09):** counter-offers arrive over **live Resend email** (reuse Maya's email integration + a new inbound webhook); **the LLM drafts candidate deal structures** which the deterministic engine prices/ranks/**rejects** (LLM never emits dollars); the workbench **extends the David v2 dossier** (no new route). Economic feeds (3PL inventory, cost of capital, POS sell-through) are **simulated Supabase tables** because those integrations aren't available.

> Execution protocol: same as the David v2 plan — per task, reviewer subagent → RED (tdd-guide) → GREEN → parallel `code-reviewer` + `security-reviewer` → boundary gate → conventional commit. No `--no-verify`. Branch off freshly-fetched `origin/main` in a clean worktree.

---

## 0. Why this design (the invariants that constrain it)

The literal "agent autonomously invents deals and negotiates over email" collides with the platform's governance promise. This spec keeps the *experience* (dynamic, optimized, specific tranche deals) while honoring the invariants that make Recoup demo-credible:

| Invariant | Consequence for this feature |
|---|---|
| I-24 deterministic + replayable | Every dollar/ratio/objective is **code-computed** (Decimal), reproducible from stored inputs + seed. LLM output is *structure parameters*, never money. |
| no-float-money (`no-float-money.test.ts`) | All money math via `src/types/money.ts`. |
| I-8 proposer ≠ approver · I-20 HITL on external actions | Every outbound counter-proposal to Harbor is **draft-only → David-approved → sent**. No autonomous send. |
| I-26 no production ERP write-back | Ship/release/credit-master changes stay staged drafts; only email to Harbor is a real external action, and only after approval. |
| I-30 provenance honesty | Simulated feeds (3PL/cost-of-capital/POS) carry a **synthetic badge**; real email carries live provenance. No "Connected" without a loaded source. |
| I-21 hash-chained decisions | Each negotiation round + approval is appended to the audit chain. |

**Architecture in one line:** simulated economic data (Supabase) → **deterministic deal-space optimizer** (reuses `core/partialHold.ts` + `proposeTerms`; new `core/dealExpectedValue.ts` for EV) → **LLM drafts candidate structures** the optimizer prices/ranks/rejects → David approves → **Resend email** to Harbor → **inbound webhook** parses Harbor's counter → next round.

---

## 1. New Supabase tables (all seeded; simulated feeds badged synthetic)

```sql
-- Trigger: a pending order that may breach the limit (the $640K-over-$500K beat)
create table if not exists credit_orders (
  order_id text primary key, account_id text not null references credit_accounts(account_id),
  order_amount numeric not null, requested_ship_date date not null,
  status text not null check (status in ('pending','negotiating','partially_released','closed')) default 'pending');

-- Simulated economic feeds (synthetic-badged in UI)
create table if not exists sim_3pl_inventory (
  sku text not null, account_id text not null references credit_accounts(account_id),
  warehouse text not null, on_hand_units integer not null, unit_cost numeric not null,
  daily_holding_cost_rate numeric not null, primary key (sku, warehouse));
create table if not exists sim_cost_of_capital (
  as_of_date date primary key, annual_cost_of_capital_rate numeric not null,
  inventory_holding_annual_rate numeric not null);
create table if not exists sim_pos_sellthrough (
  account_id text not null references credit_accounts(account_id), period text not null,
  units_sold integer not null, sell_through_rate numeric not null, primary key (account_id, period));

-- Negotiation state machine + audit
create table if not exists credit_deal_scenarios (
  scenario_id text primary key, order_id text not null references credit_orders(order_id),
  seed integer not null, candidate_json jsonb not null, objective_value numeric not null,
  ranked_position integer not null,
  optimizer_run_id text not null,                     -- groups one optimizer run
  source_hash text not null, policy_hash text not null,  -- exact replay: feeds + policy at run time
  source_record_ids jsonb not null,                   -- cited rows the objective consumed
  created_at timestamptz not null default now(),
  unique(order_id, source_hash, policy_hash, seed, ranked_position)); -- dedupe only true re-runs
create table if not exists credit_negotiation_rounds (
  round_id text primary key, order_id text not null references credit_orders(order_id),
  round_no integer not null, our_proposal_json jsonb not null, their_counter_json jsonb,
  status text not null check (status in ('drafted','sent','countered','accepted','rejected','withdrawn')),
  audit_entry_hash text, created_at timestamptz not null default now(),
  unique(order_id, round_no));                         -- one row per order/round
-- Outbound send ledger: correlation + body-hash proof + idempotency (columns the plan's claims need)
create table if not exists credit_negotiation_sends (
  send_id text primary key, round_id text not null references credit_negotiation_rounds(round_id),
  action_id text not null, reply_to_token text not null,
  approved_body_hash text not null, sent_body_hash text,   -- must match on send
  provider_email_id text,                                  -- Resend send id from /emails response
  idempotency_key text not null unique,                    -- dedupe re-approvals / Resend retries
  status text not null check (status in ('approved','sent','failed')) default 'approved',
  created_at timestamptz not null default now());
create table if not exists credit_counter_offers (
  offer_id text primary key, order_id text not null references credit_orders(order_id),
  round_no integer not null, terms_json jsonb not null,
  source text not null check (source in ('email','manual')), inbound_message_ref text,
  inbound_email_id text, inbound_message_id text, cited_spans_json jsonb,
  parsed_by text, received_at timestamptz not null default now(),
  unique(inbound_message_ref),                          -- webhook-replay dedupe (NULL for manual = allowed)
  unique(inbound_email_id));                            -- Resend received-email id dedupe (NULL for manual = allowed)
-- Vendor AP contact = sender-auth key for inbound (Harbor's demo address = your Gmail, seeded from env)
create table if not exists credit_account_contacts (
  account_id text not null references credit_accounts(account_id),
  contact_email text not null, contact_name text not null, role text not null default 'ap',
  primary key (account_id, role),
  unique(account_id, role));
-- Governed policy bounds for the deal grammar (expert-owned, editable without code)
create table if not exists credit_negotiation_policy (key text primary key, value numeric not null);
```

`credit_negotiation_policy` seeds the **deal-grammar bounds** (expert-owned policy, like the P&L weights): `min_deposit_pct`, `max_deposit_pct`, `max_tranches`, `max_collateral_ratio`, `max_financing_spread_bps`, `min_release_pct`, `max_release_pct`, `default_prob_by_verdict_*`. Seed script `scripts/seedNegotiationDataset.ts`; verify counts.

> **BLOCKING INPUT (owner) — no invented values.** Every number in `credit_negotiation_policy` (bounds, default probabilities, collateral limits, financing spreads, release bounds) is a governed constant, and the repo forbids invented thresholds/weights/probabilities. **The owner requested a research-backed applicable policy on 2026-07-09; the table below is the P0.0 policy candidate.** Execution may use it only after owner acceptance or explicit override. Capture accepted figures in the seed script; the builder fails closed if any policy key is absent.

**P0.0 research-backed policy candidate (owner acceptance required):**

Research basis: use current U.S. prime as the financing anchor (Fed H.15 bank prime loan rate is 6.75% as of 2026-07-08), trade-credit mitigation norms that tighten terms through deposits/collateral/insurance for high-risk buyers, and current corporate/high-yield default context. These values are intentionally conservative for a **simulated David negotiation demo**, not a production underwriting policy.

| Policy key | Proposed value | Unit | Rationale / guardrail |
|---|---:|---|---|
| `min_deposit_pct` | `0` | percent | Allows no-deposit offers only when the deterministic score/risk economics justify it. |
| `max_deposit_pct` | `60` | percent | Keeps LLM/optimizer offers commercially plausible while allowing a strong cash-in-advance lever for HIGH risk. |
| `max_tranches` | `3` | count | Limits complexity: initial release + up to two triggered follow-on releases. |
| `max_collateral_ratio` | `1.25` | ratio | Caps collateral/security requests at 125% of the financed/released risk balance. |
| `max_financing_spread_bps` | `600` | basis points | Allows risk-based pricing up to prime + 600 bps; enough for HIGH risk without creating predatory demo terms. |
| `min_release_pct` | `10` | percent | Prevents meaningless offers below 10% fulfilment unless the engine chooses no deal/fail-closed. |
| `max_release_pct` | `85` | percent | Keeps a back-order/hold lever available for over-limit/high-risk negotiation; no full release without separate policy approval. |
| `default_prob_by_verdict_clear` | `0.005` | annual probability | CLEAR account baseline for expected-default-loss math. |
| `default_prob_by_verdict_watch` | `0.015` | annual probability | WATCH account baseline, above CLEAR but below stressed/high-yield context. |
| `default_prob_by_verdict_elevated` | `0.050` | annual probability | ELEVATED baseline, aligned to stressed speculative-risk range. |
| `default_prob_by_verdict_high` | `0.120` | annual probability | HIGH baseline, conservative stress for over-limit + disputed/unsupported exposure. |

Implementation note: treat `default_prob_by_verdict_*` as **annualized** PDs and convert to the candidate horizon in code (`1 - (1 - annualPd)^(days/365)`) before expected-default-loss math. Do not let the LLM see or change these constants; it can only propose bounded structure parameters.

**Policy Q&A / vector search:** `credit_negotiation_policy` remains the authoritative exact-value store. Vector search is approved only for **policy-related questions and rationale retrieval**: index the policy research notes, owner-approval note, and policy rationale chunks with record IDs + policy version/hash so David copilot can answer "why is this bound 60%?" with citations. The engine must never retrieve active numeric policy values from vector search; it reads exact rows from `credit_negotiation_policy` and fails closed on missing/conflicting values. If vector-retrieved rationale disagrees with the structured policy row, show `Policy rationale conflict` and route to human review.

**Deal grammar (the bounded space the optimizer + LLM operate in):**
`{ depositPct, tranches:[{pct, trigger:{kind:'date'|'pos_sellthrough', value}}], collateralRatio, financingSpreadBps }` — every field bounded by `credit_negotiation_policy`. A candidate outside bounds is rejected by the engine, never rendered.

---

## Phase 0 — Surface the existing engines behind "Simulate alternatives" (deterministic what-if)

**Goal:** make the currently-disabled "Simulate alternatives" button real, reusing built code, before any negotiation. De-risks the `/credit/command` retirement (which would otherwise orphan `partialHold`/`proposeTerms`).

**Backend:** new pure `src/services/creditSimulationModel.ts` wrapping `computePartialHold` (`core/partialHold.ts`) + `proposeTerms` (`tools/actions/proposeTerms.ts`); read-only `POST /credit/v2/simulate` (Express) taking `{accountId, weightOverrides?, scoreOverrides?}` → recomputed `{compositeScore, releaseRatioPercent, amountSplit, terms}` with cited basis. No approval, no external action.

**UI (extend David dossier):** enable "Simulate alternatives" → opens a `Sheet` with weight/score sliders (bounded), live-recomputed release/back-order split + terms + a **sensitivity line** ("bureau-risk weight would need to fall from X to Y before full release"). All labels pre-formatted by backend.

**Success criteria:**
- Button no longer disabled; adjusting an input returns a recomputed split from `computePartialHold` (not a constant).
- Same inputs → same output (deterministic); money via Decimal; no approval/external call fires.
- `/credit/command` engines now have a live consumer in v2.

**Test scenarios:**
- *Unit:* `computePartialHold` wrapper golden values; bounds clamp; Decimal money; fail-closed on missing scores.
- *Browser/e2e:* open Harbor dossier → "Simulate alternatives" → move a weight → split + sensitivity line update; assert **no** `/api/approval` or email network call fires on open or on simulate.
- *LLM:* n/a (Phase 0 is pure-deterministic).

---

## Phase 1 — Deterministic deal-space optimizer over simulated economic data

**Goal:** replace single-menu-pick with a code-computed optimizer that ranks candidate deal structures by **expected value of the relationship** across a seeded scenario grid.

**Backend:** `src/services/dealOptimizer.ts` (pure, Decimal):
- Inputs (all from Supabase): order (`credit_orders`), account verdict/exposure (existing `credit_*`), `sim_3pl_inventory`, `sim_cost_of_capital`, `sim_pos_sellthrough`, grammar bounds.
- Objective per candidate = `revenueCaptured − costOfCapital(financedBalance, days) − holdingCost(unshippedUnits, days) − expectedDefaultLoss(verdict, exposure)`; default probability from `credit_negotiation_policy.default_prob_by_verdict_*`; EV aggregated across a **fixed-seed** scenario grid (sell-through ± bands).
- **New helper — do NOT claim reuse of `reconstructExpectedPosition`.** `core/expected.ts` only handles three deduction shapes (`contracted-delivery`, `promo-accrual`, `contract-sla`) and is **not** a generic scenario-grid EV aggregator. Add a new deterministic **`src/core/dealExpectedValue.ts`** (pure, Decimal, seeded) that owns the objective + grid aggregation, with its own golden tests. Reference `expected.ts` for the Decimal/Money idioms only.
- Output: ranked candidates persisted to `credit_deal_scenarios` (with seed) for replay/audit.
- `GET /credit/v2/orders/:orderId/deals` returns ranked candidates + objective + basis.

**UI:** in the Harbor dossier, a "Fulfilment & Terms Negotiation" workbench: order-vs-limit breach banner, ranked candidate cards (deposit/tranches/triggers/collateral + EV + cited basis), "why this ranks first" sensitivity readout. Synthetic badges on 3PL/cost-of-capital/POS.

**Success criteria:**
- Candidates are ranked deterministically; **re-running with the same `seed` + `source_hash` + `policy_hash` reproduces the exact ranking + objective values** (I-24), persisted in `credit_deal_scenarios` with `optimizer_run_id` + `source_record_ids`. A changed feed/policy row yields a new `source_hash`/`policy_hash` (new run, not a silent overwrite) — this is what makes the hash-chain evidence real.
- Every candidate's dollars are Decimal-computed and cite the source rows; out-of-grammar candidates never appear.
- Fail-closed (503/Contract gap) if any required sim table is empty.

**Test scenarios:**
- *Unit:* golden EV for a known order+feeds; determinism (same seed → identical ranking); grammar-bounds rejection; Decimal money; missing-feed fail-closed; monotonicity checks (higher cost-of-capital → lower EV for financed-heavy deals).
- *Browser/e2e:* Harbor breach → workbench renders ranked candidates with EV + basis + synthetic badges; assert values match a fixture computed from seeded rows; no write/approval call on load.
- *LLM:* n/a (Phase 1 is deterministic).

---

## Phase 2 — LLM drafts candidate structures (engine prices, ranks, and rejects)

**Goal (owner choice):** let the LLM *propose* creative deal shapes; the deterministic engine remains the sole source of dollars and the gatekeeper.

**Backend:** extend the existing David live-agent path (`creditRiskQuerySession` pattern) with a `credit_negotiation.draft_structures` tool. Flow:
1. LLM (OpenAI Agents SDK, live trace) emits **structure parameters only** as strict JSON matching the deal-grammar schema (Zod-validated) — `depositPct`, `tranches[]`, triggers, `collateralRatio`, `financingSpreadBps`. **No dollar amounts in LLM output.**
2. `dealOptimizer` (Phase 1) validates bounds, **prices** each LLM candidate, ranks it against the deterministic candidates, and **rejects** any that break grammar/risk bounds (returns a visible rejection reason).
3. Response carries model-execution metadata (mode `live_openai_agents`, agents, handoffs, tokens, run id) + citations to the account/order/feed records, same as David copilot; raw model text suppressed.

**UI:** "Ask the copilot to draft an option" → LLM-proposed structures appear **priced by the engine**, interleaved with deterministic candidates, each tagged `agent-drafted` vs `engine-generated`; rejected drafts show the rejection reason (grammar/risk). Free-text still routes through `/api/credit/query`.

**Success criteria:**
- LLM output is **schema-valid structure params with zero dollar figures**; all pricing comes from the engine.
- An LLM structure that violates bounds is **rejected with a cited reason** and never rendered as approvable.
- The *priced* result for a given structure is identical regardless of LLM phrasing (determinism downstream of the LLM).
- Every LLM claim carries citations (I-29-style parity); model-execution drawer shows tokens/agents/handoffs.

**Test scenarios:**
- *Unit:* schema rejects any LLM JSON containing money; engine prices an agent-drafted structure identically to an equivalent hand-authored one; bounds-violation → rejection object.
- *LLM/eval (offline harness, seeded prompts):*
  - *Grammar adherence:* over N sampled generations, 100% parse to the grammar schema; **0 dollar tokens** in raw output (regex + numeric-field assertion).
  - *No-invention:* assert the LLM never returns `objectiveValue`/amounts; those exist only post-engine.
  - *Citation-scope:* every narrated rationale cites an existing record/scenario id; injected "cite a record outside the packet" prompt must refuse/Contract-gap (reuse the forensics citation-scope guard pattern).
  - *Rejection path:* a prompt nudging an out-of-bounds deal (e.g., 95% deposit) → engine rejects, UI shows reason.
  - *Determinism downstream:* same structure via 5 different phrasings → identical priced ranking.
- *Browser/e2e:* copilot "draft an aggressive tranche deal" → priced candidate appears with `agent-drafted` tag + citations + model-execution drawer; an out-of-bounds ask renders a rejection card; assert no approval/email call fires.

---

## Phase 3 — Live email negotiation loop (Resend send + inbound webhook, HITL every round)

**Goal:** close the loop over **real email**, reusing Maya's proven send path, with a human approving every outbound counter.

**Email infra (confirmed on `north-bay.dev`, 2026-07-09):** Resend sending **and** receiving are verified. Receiving MX is on the **root `@` (catch-all)** — every address at `north-bay.dev` routes to Resend inbound — so plus-addressing needs no extra config. DNS is done; the remaining setup is app-side (webhook registration + signing secret + the ported route).
> Current proof boundary: treat the preceding Resend sentence as domain/DNS/catch-all readiness, not completed app-side proof. Do not claim the email loop is verified until the Resend `email.received` webhook is registered, signing/rate-limit/retry env is configured, and live webhook/browser e2e passes.
- **Addresses:** send **From** `deals@north-bay.dev`; **Reply-To** `deals+<orderId>-r<roundNo>@north-bay.dev` (the catch-all captures the plus-token).
- **Harbor contact:** the owner's Gmail, seeded into `credit_account_contacts` **from env** (`HARBOR_AP_CONTACT_EMAIL`), never committed — it is both the send target and the inbound sender-auth key.

**Outbound.** David selects a candidate → **draft** counter-proposal email → **David approves** (`actionId: "credit-v2:negotiation:<orderId>:<roundNo>"`) → send via a **new `sendNegotiationEmail` primitive** → receipt + audit hash; **write a `credit_negotiation_sends` row** (`approved_body_hash`/`sent_body_hash` must match, `provider_email_id` from Resend's send response, `idempotency_key`); round persisted to `credit_negotiation_rounds` (status `sent`). From `deals@north-bay.dev` + Reply-To plus-token + subject token `[Recoup Deal <orderId> · Round <n>]`. Do **not** rely on an outbound RFC `Message-ID` from Resend send; store inbound `email_id` + `message_id` on `credit_counter_offers`, then use the latest inbound `message_id` for subsequent `In-Reply-To` / `References` headers.

**Approval storage boundary (owner clarification, 2026-07-09):** the durable communication record is **not** `recoup_memory_records`. Approval memory may store only hash/pointer proof (`approvedBodyHash`, `approvedSubjectHash`, `approvedDraftRecordId`, recipient config key/hash, audit hash, action/source IDs). The approved email body and subject live in `credit_negotiation_rounds.our_proposal_json`; the send route must read that typed row back, verify hashes against the approval receipt, then reserve `credit_negotiation_sends` before calling Resend. If the typed draft row is missing, stale, or hash-mismatched, fail closed and do not call the provider.

**Round idempotency boundary (reviewer fix, 2026-07-09):** duplicate-send prevention is immutable per approved round/action, not per mutable outbound provider body. `credit_negotiation_sends.action_id` is unique; `sendNegotiationEmail` checks existing rows by `actionId` before checking provider idempotency key. Client-supplied `lastInboundMessageId` must never allow the same approved round to reserve a second outbox row or call Resend twice. Persisted send status is normalized to the schema enum (`pending|sent|failed`); raw provider events must not be written into the constrained `status` column.

> **Send-primitive gap (not just the route).** `RecoupEmailDraft` is `{actionId, body, lineId, recipientGroup, subject}`, `EmailRecipientGroup` is `billing|recovery` only, recipients come from env config, and `ResendEmailProviderBody` has **no `reply_to` and no custom `headers`** (`emailGateway.ts:14,3,23`). So `sendResendEmail` cannot address Harbor or set Reply-To / `In-Reply-To`. **Add a `sendNegotiationEmail(input)` in `emailGateway.ts`** that reuses the idempotent send-ledger + body-hash + delivery/error handling but accepts explicit `to`, `from`, `replyTo`, and `headers`. Do **not** widen the `billing|recovery` enum on the Maya path. (`emailGateway.ts` already imports `createHmac`/`timingSafeEqual`, so hashing is in-house.)

> **Two backend gaps the review surfaced — do NOT under-scope these (both need their own RED tests):**
>
> **(a) `/api/email` is Maya/forensics-shaped — you cannot just "add David access."** The route's `emailSendSchema` is `.strict()` and requires `lineId` + `recipientGroup: enum("billing","recovery")`, is gated `allowDemoSessionRoles: ["maya"]`, and **re-fetches `/forensics/work-items/:lineId`** before send (`cockpit/app/api/email/route.ts:23,66,146`). Negotiation needs order/account/round semantics, a Harbor recipient, Reply-To plus-token, and inbound Message-ID storage — none of which fit that schema. **Create a dedicated `POST /api/credit/negotiation/email` route** that calls `sendNegotiationEmail` and reuses the `email-draft-dialog` UI pattern. Reuse `emailGateway` hashing/idempotency/error-handling internals, **not `sendResendEmail` directly** and not the forensics route. The new route is david-gated, with its own send/status schema + tests. Generalizing the existing route is an option but higher-risk to Maya; prefer a new route.
>
> **(b) `credit-v2:negotiation:*` is swallowed by the existing generic resolver.** `findPendingAction` (`serviceLayer.ts:599`) enters `if (actionId.startsWith("credit-v2:"))`, searches only account `packet.actionId`, and **throws `Action not found` at :606** — it never falls through. So a negotiation id lands there and fails. **Add a `startsWith("credit-v2:negotiation:")` branch BEFORE the generic `credit-v2:` check**, resolving the action by deterministically rebuilding the negotiation round/deal (recordIds + basis, draft-only). RED test (must fail on current code): `prepareApprovalDecision({actionId:"credit-v2:negotiation:HAR-ORD-001:r1"})` currently throws `Action not found`; after the fix it resolves to a negotiation action; the generic `credit-v2:<accountId>` path still resolves unchanged.

**Inbound (new — port the Relay client's `app/api/webhooks/resend/route.ts`):** `cockpit/app/api/credit/negotiation/inbound/route.ts`:
1. **Register the webhook** in Resend (Configuration/Webhooks) for `email.received` → this deployed route; put the signing secret in env (`RESEND_INBOUND_SIGNING_SECRET`). *DNS alone does not deliver events — this step is required.*
2. **Verify the webhook signature over the RAW body** before any parsing (Resend requires raw-body verification; don't let the App Router parse JSON first); bad signature → 401, nothing written. **Recoup has no `svix` package and the plan keeps "no new deps": implement Resend's svix-compatible scheme with `node:crypto`**, correctly:
   - **Secret:** the `whsec_…` value — strip the `whsec_` prefix and **base64-decode the remainder** to get the raw HMAC key (do not HMAC with the literal string).
   - **Signed content:** `${svix-id}.${svix-timestamp}.${rawBody}`; HMAC-SHA256 → base64.
   - **Header:** `svix-signature` holds **space-separated, versioned** entries like `v1,<base64> v1,<base64>`. Split, take each `v1,…`, and accept if **any** matches (constant-time via `timingSafeEqual`).
   - **Replay guard:** reject if `svix-timestamp` is outside a tolerance window (e.g. ±5 min).
   - Ship **test vectors** (known secret + payload → expected `v1` signature; tampered-body and stale-timestamp negatives). Adding the `svix` package is the alternative **only with explicit owner approval**.
3. **Correlate:** extract the token from the `to` plus-address (primary), fall back to subject token, then use inbound `message_id` continuity for later rounds where available. Unknown/closed round → drop (fail-closed). Store both Resend's inbound `email_id` and the inbound RFC `message_id`; later outbound replies set `In-Reply-To` / `References` from that stored inbound `message_id`.
4. **Sender-auth:** `from` must equal `credit_account_contacts.contact_email` for that order's account, else drop. Catch-all means arbitrary spam hits this route — rate-limit and silently drop unmatched.
5. **Fetch body** via Resend's Received Emails API (`email.received` omits it), with retry; on fetch failure, store metadata and retry later, do **not** advance the round.
6. **LLM extracts cited spans + intent ONLY — never dollar values** (I-1). The LLM returns which grammar fields the counter touches and the exact quoted source spans; **code then deterministically extracts any dollar/number from the exact source text** (regex over the raw message) and validates against the grammar. If a required amount can't be extracted verbatim from the message → **route to human review**, do not let the model supply it. Insert `credit_counter_offers` (`source='email'`, storing the cited spans) + advance `credit_negotiation_rounds` to `countered`.

A manual "paste counter" UI field feeds the same table (`source='manual'`) — the always-works fallback if live email spam-filters or lags on stage.

**Loop:** new counter → `dealOptimizer` re-runs against it → new ranked counter-proposal (Phase 1/2) → David approves → send. Continues until `accepted`/`rejected`/`withdrawn`.

**Fresh-test reset switch (owner addendum, 2026-07-09):** add a David-visible reset control in the negotiation workbench for local/human QA. It calls a dedicated `POST /api/credit/negotiation/reset` route and resets only the selected order's email-negotiation communication artifacts: `credit_counter_offers`, `credit_negotiation_inbound_emails`, `credit_negotiation_sends`, `credit_negotiation_rounds`, plus matching `approval_records` whose action IDs start `credit-v2:negotiation:<orderId>:`. It must not delete credit source rows, policy rows, deal input rows, normal `credit-v2:<accountId>` approvals, or any ERP-facing state. The route is David-authenticated, Supabase-backed, fail-closed, and intended only to prepare a clean email-negotiation test.

**Success criteria:**
- No email sends without an explicit David approval (I-20/I-8); duplicate approval → **idempotent** (no double-send, ledger dedup); sent body hash == approved draft hash.
- Inbound webhook is signature-verified, fail-closed on bad signature; parsed counter is grammar-valid and cited to the message; parsing never invents amounts.
- Each round hash-chained to audit; `credit_orders.status` transitions correctly; opening the workbench sends **no** email/approval request (read-only until explicit action).
- Provenance: email channel shows **live**; economic feeds still **synthetic**.
- Reset switch clears only the selected order's communication/negotiation approval artifacts, then the workbench can be re-tested from a fresh state without disturbing Maya, David credit source data, policy rows, or generic credit approvals.

**Test scenarios:**
- *Unit:* actionId resolver for `credit-v2:negotiation:*`; idempotent send ledger (second approve → same receipt, no new send); inbound signature verification (bad sig → 401, no row written); parser rejects money-invention / out-of-grammar counter.
- *LLM/eval:* inbound-parse harness over sampled counter-offer emails → 100% grammar-valid extraction, 0 invented amounts, every field cited to the message; adversarial email ("also raise my limit to $5M") → parsed as out-of-scope, not silently actioned.
- *Browser/e2e (real backend + Supabase, seeded Harbor breach):*
  1. Harbor breach → workbench → ranked candidates (Phase 1/2).
  2. Select candidate → **Draft counter** → review dialog shows body + cited basis.
  3. Approve → assert **`POST /api/credit/negotiation/email`** fires **once**, receipt + audit hash render, round = `sent`, `credit_negotiation_sends` row written; re-approve → **no second send** (idempotency_key dedupe).
  4. Simulate inbound counter (webhook POST or manual paste) → new round `countered` appears; re-optimized counter renders.
  5. Assert opening the workbench / viewing a round fires **no** send/approval call (HITL discipline).
  6. Provenance: email `live`, 3PL/cost-of-capital/POS `synthetic` badges.
  7. Reset communication → assert only selected order communication rows + negotiation approval receipts are removed; normal David account approval receipts remain.
- *Regression:* Maya email path + David v2 + Maya containment unaffected (`npm run test`, `test:e2e:shared-surfaces`).

---

## Phase 4 (optional) — Continuous re-optimization & close-out

Sentinel-style re-underwrite Harbor after each round (reuse `agents/sentinel.ts`); on `accepted`, stage the (draft-only) release/back-order + credit-master change packet through the existing `/api/approval` path (no ERP write, I-26); CFO summary reflects captured revenue vs risk-priced balance. Success = accepted deal produces a hash-chained, replayable settlement draft; no autonomous execution.

---

## Cross-cutting requirements

- **Reuse over rebuild:** `computePartialHold`, `proposeTerms`, `emailGateway` hashing/idempotency/error-handling internals via the new `sendNegotiationEmail`, `email-draft-dialog`, `creditRiskQuerySession`, `findPendingAction`, David dossier components, `david-verdict-tokens`. **Reference the Decimal/Money idioms in `core/expected.ts` but do NOT reuse it for negotiation EV** (deduction-only; use the new `core/dealExpectedValue.ts`). **New deps: none** — inbound signature check is a local `node:crypto` HMAC verifier (svix package only with owner approval).
- **Boundary:** `cockpit/components/david/*` stays UI-only (no money math, no `src/` imports) — all figures arrive as pre-formatted labels + a provenance matrix row per new field (extend `david-v2-provenance-matrix.md`).
- **David dossier drawer defaults:** the **Signals in** section must be expanded by default for David negotiation/account detail review. Agent assessment, Outcome, and Action may remain collapsed by default unless a task-specific e2e asserts otherwise.
- **Policy query retrieval:** policy-related copilot questions use vector search over policy rationale/approval documents for explanation only; all executable policy constants still come from exact `credit_negotiation_policy` rows.
- **Gates before any prod movement:** `npm run verify` (lint, typecheck, full vitest, depcruise, release-readiness) green; `test:e2e:david-v2` extended; new `test:e2e:david-negotiation`; LLM eval harness green; David visual-review ≥4.5/5.
- **Security:** inbound webhook signature-verified + rate-limited; email send david/maya-gated; no secrets in code; seeded sim data only (no real customer PII).

## Troubleshooting / performance watchpoints

- **Prod `/credit` slow loading:** if `https://recoup-self-eta.vercel.app/credit` takes more than 5 seconds on a warmed browser session, stop the release path and troubleshoot before continuing. Reuse the Maya slow-load/cache troubleshooting pattern from `docs/superpowers/plans/2026-07-07-maya-prod-movement.md` (cache key/versioning, explicit refresh, first-hit/warm-hit probes, Vercel-vs-Render ownership split) and `docs/superpowers/plans/2026-07-07-david-credit-risk-review-v2-plan.md` Task 1-5.5 (prod Maya cache regression troubleshooting record). Apply the Maya lesson explicitly: measure demo-login/auth latency separately from post-login data hydration, check `x-recoup-read-model-cache: hit | miss | refresh` style route evidence when the route supports it, and verify the public alias is serving the intended Vercel route code before blaming Render/Supabase. Reproduce with browser timing + direct API timing; compare Vercel route time, Next proxy time, Render `/credit/v2` time, Supabase fetch time, and any cache/read-model headers. Check browser console/network waterfall, Vercel function logs, Render `recoup-api` logs, and direct `curl`/Playwright timings for `/credit`, `/api/credit/query`, and `${RECOUP_API_URL}/credit/v2`. Confirm Maya cache behavior is not regressed while fixing David load time.

## Execution status (2026-07-09, `codex/david-dynamic-negotiation`)

- Current update: the inbound catch-all rate-limit path is now TDD-green and hardened for production defaults. Red tests covered repeated signed catch-all spam, sender alias sharding across different catch-all recipients, and missing production rate-limit env. Green behavior: the default production route fails closed without `RESEND_INBOUND_RATE_LIMIT_MAX_EVENTS` + `RESEND_INBOUND_RATE_LIMIT_WINDOW_MS`, while injected test stores can still run without production env; rate-limit buckets now cover catch-all, sender, and sender-domain. Green checks: `npm run test -- tests/unit/credit-negotiation-inbound.test.ts` (1 file / 14 tests), focused negotiation adjacent set (6 files / 29 tests), `npm run lint`, and `npm run typecheck`. Live deployed env proof, live Resend webhook proof, and full browser e2e remain pending.
- Current retry update: failed inbound body fetches now have a cron-callable, secret-guarded retry route plus a worker that reads durable failed metadata, refetches the Resend body, reuses the span-grounded exact-policy parser, inserts a durable counter row first, and only then updates metadata to `fetched` and advances grammar-valid counters. Reviewer hardening fixed the fetched-without-counter terminal-state risk: duplicate detection checks `credit_counter_offers` before treating an inbound as duplicate, fetched metadata without a counter is replayed, parse/policy failures leave failed metadata retryable, closed/stale rounds skip without provider fetch, unauthorized sender skips without provider fetch, and Supabase sender-auth now filters `credit_account_contacts.role = 'ap'`. Green checks: `npm run test -- tests/unit/credit-negotiation-inbound-retry.test.ts` (1 file / 6 tests), `npm run test -- tests/unit/credit-negotiation-inbound.test.ts` (1 file / 14 tests), focused negotiation adjacent set (6 files / 29 tests), `npm run lint`, and `npm run typecheck`. Live cron/provider proof and deployed env wiring remain pending.
- Current live-LLM proof: local `.env.local` has `OPENAI_API_KEY`; a live OpenAI Responses extractor probe returned provider status 200 with usage 216 input / 82 output / 298 total tokens, 4 cited spans, `intent=counter_offer`, and deterministic parser status `grammar_valid` for a synthetic Harbor counter. The probe did not print raw model output or secrets. This supersedes the older "real-provider inbound LLM proof pending" wording below for the extractor path only; live Resend webhook/provider proof remains pending.
- Current policy-vector update: P0.1a is complete locally. David copilot policy questions now call the dedicated OpenAI policy vector-store reader for rationale/citation only, while executable policy values stay anchored to exact `credit_negotiation_policy` rows. The reader filters results to the requested `policy_key`, `policy_version`, and `policy_hash`; same-key value conflicts remain visible through `valueConflict` and route to human review. Reviewer fix: vector citation content now strips `value_text` / `valueText` metadata lines before API/UI exposure, while attribute-level `value_text` remains available only for conflict detection. Green checks: RED then GREEN `npm run test -- tests/unit/credit-negotiation-policy-vector.test.ts`; RED then GREEN `npm run test -- tests/unit/credit-v2-api.test.ts`; RED then GREEN `npm run test -- tests/unit/david-copilot-dock.test.ts`; focused set `npm run test -- tests/unit/credit-v2-api.test.ts tests/unit/david-copilot-dock.test.ts tests/unit/credit-negotiation-policy-vector.test.ts tests/unit/credit-negotiation-policy.test.ts` (4 files / 28 tests); `npm run typecheck`; `npm run lint`; `npm run test:e2e:david-v2` (4 account live queries plus Harbor draft-query panel and Harbor policy-vector rationale panel; audit hash `cd55437532bf3156ce5c3b07ec46f20b40e1c2d42e214ac957d737a6d760d5dc`; screenshots `output\playwright\david-v2`); and full `npm run verify`.
- Fresh policy-vector readiness checkpoint (2026-07-10): added the no-mutation operator gate `npm run check:credit-negotiation-policy-vector` (`scripts/checkCreditNegotiationPolicyVectorReadiness.ts`) with RED/GREEN coverage in `tests/unit/credit-negotiation-policy-vector-readiness.test.ts`. The checker loads local runtime env, lists only required env variable names as present/missing, searches the configured OpenAI policy vector store for every executable policy key, and only passes when each hit matches policy version 1 and policy hash `748b6a452224a294113f0560747db867f51340a957a83722ccbf2854d56b9e16`. Current run returned `ready_for_policy_vector_search` for all 11 policy keys and rationale record IDs, with no secret values or executable policy values emitted. Green checks: RED missing-script test, then `npm run test -- tests/unit/credit-negotiation-policy-vector-readiness.test.ts` (1 file / 3 tests), and live no-mutation `npm run check:credit-negotiation-policy-vector`.
- Fresh P0 policy-readiness checkpoint (2026-07-10): added the no-mutation operator gate `npm run check:credit-negotiation-policy` (`scripts/checkCreditNegotiationPolicyReadiness.ts`) and tightened the executable policy parser so generic `human:*` markers are rejected; active rows must carry the exact owner acceptance marker used by the seed artifact. Current run wrote `output/credit-negotiation-policy-readiness.json` and returned `ready_for_policy_seed` with 11 executable keys, policy version 1, and policy hash `748b6a452224a294113f0560747db867f51340a957a83722ccbf2854d56b9e16`. Green checks: RED then GREEN `npm run test -- tests/unit/credit-negotiation-policy.test.ts tests/unit/credit-negotiation-policy-readiness.test.ts` (2 files / 10 tests); focused policy/optimizer regression `npm run test -- tests/unit/credit-negotiation-policy.test.ts tests/unit/credit-negotiation-policy-readiness.test.ts tests/unit/credit-negotiation-policy-vector.test.ts tests/unit/deal-optimizer.test.ts tests/unit/credit-negotiation-counter-parser.test.ts` (5 files / 25 tests); `npm run lint`; `npm run typecheck`; `git diff --check` (CRLF warnings only); and full `npm run verify` passed (`lint`, `typecheck`, Vitest 160 files / 1328 tests, depcruise 154 modules / 533 dependencies, release readiness). This proves the local seed artifact is self-consistent and exact-marker gated; it does not bypass any explicit owner/prod approval gate.
- Current P2.2 update: added offline deterministic LLM draft-structure eval harness `evals/creditNegotiationDraftEval.ts` plus TDD coverage `tests/unit/credit-negotiation-llm-eval.test.ts`. The harness evaluates captured model outputs for grammar adherence, no model-emitted dollars/objectives, selected-evidence citation scope, engine rejection for out-of-policy structures, downstream deterministic pricing across 5 equivalent prompt phrasings, and raw-output suppression in the eval report. Green checks: RED missing-module test, then `npm run test -- tests/unit/credit-negotiation-llm-eval.test.ts` (1 file / 2 tests), `npm run test -- tests/unit/credit-negotiation-llm-eval.test.ts tests/unit/credit-negotiation-drafts.test.ts tests/unit/deal-optimizer.test.ts` (3 files / 11 tests), `npm run lint`, `npm run typecheck`, and full `npm run verify`.
- Current P2.3 update: David copilot now renders backend `negotiationDraft` results in a visible agent-drafted / engine-priced panel with deterministic candidate pricing or policy rejection reasons. Backend retry now re-prompts draft-intent live queries when the first trace omits the negotiation draft tool, and the prompt explicitly requires draft-tool `recordIds` to include the selected account, order ID, and `credit_orders:<orderId>` source row. The draft tool now allows already-selected account evidence IDs alongside governed order rows while still rejecting foreign scope. Green checks: RED then GREEN `npm run test -- tests/unit/david-copilot-dock.test.ts`, RED then GREEN `npm run test -- tests/unit/credit-v2-api.test.ts`, RED then GREEN `npm run test -- tests/unit/credit-negotiation-drafts.test.ts`, focused set `npm run test -- tests/unit/credit-negotiation-drafts.test.ts tests/unit/credit-v2-api.test.ts tests/unit/david-copilot-dock.test.ts` (3 files / 23 tests), `npm run typecheck`, `npm run test:e2e:david-v2` (4 account live queries plus Harbor draft-query panel; audit hash `a1ce6fc00b537fd4b91aa9be94790aa0af1a95182da4ed6ad80c93d4cea9fb8e`; screenshots `output\playwright\david-v2`), and full `npm run verify`.
- Current P3.4 re-optimization update: grammar-valid complete `credit_counter_offers` rows are now loaded from Supabase and converted into deterministic `counter-offer:<counterOfferId>` deal candidates. The optimizer prices them with `dealExpectedValue` code, cites `credit_counter_offers:<counterOfferId>`, and the existing `/credit/v2/orders/:orderId/deals` scenario persistence path writes ranked counter-offer candidates into `credit_deal_scenarios`. Reviewer hardening is complete: malformed complete counter-offers are rejected without failing the optimizer, incomplete grammar-valid counters are rejected and unpriced, human-review counters remain ignored/unpriced, replay `sourceHash`/citations are scoped to the selected order's inventory and POS rows, and failed `credit_negotiation_rounds` reads now fail closed while an empty rounds table remains valid. Local route-level full-loop proof is now added: `tests/unit/credit-negotiation-full-loop.test.ts` approves a top-candidate grounded draft, sends it once through the default Supabase outbox/Resend seam, proves duplicate send idempotency, records a David-authenticated manual counter without fake email metadata, reloads persisted Supabase rows, and re-optimizes into a cited `counter-offer:<counterOfferId>` candidate. Green checks: RED then GREEN `npm run test -- tests/unit/deal-optimizer.test.ts tests/unit/credit-v2-api.test.ts` (2 files / 28 tests), affected-file regression `npm run test -- tests/unit/credit-risk-source.test.ts tests/unit/cockpit-api.test.ts` (2 files / 139 tests), focused full-loop regression `npm run test -- tests/unit/credit-negotiation-full-loop.test.ts tests/unit/credit-v2-approval.test.ts tests/unit/credit-negotiation-email-route.test.ts tests/unit/credit-negotiation-manual-inbound.test.ts tests/unit/deal-optimizer.test.ts tests/unit/credit-v2-api.test.ts` (6 files / 48 tests), `npm run lint`, `npm run typecheck`, full `npm run verify` (`lint`, `typecheck`, `test` 155 files / 1315 tests, depcruise 153 modules / 531 dependencies, release readiness), and `git diff --check` (exit 0; CRLF warnings only). Live Resend webhook/provider proof and browser full-loop proof remain pending.
- Remote Supabase P3 schema update: applied additive migration `david_negotiation_p3_communication_tables` to project `nmwfftudympcvcjtyjbf` with only the missing communication/contact DDL, not the existing source-table seeds/policy rows. Read-back passed: `credit_negotiation_rounds`, `credit_negotiation_sends`, `credit_counter_offers`, `credit_negotiation_inbound_emails`, and `credit_account_contacts` all return HTTP 200 with zero rows; `loadDealOptimizerSourceRows` reads remote policy/source rows (`policyRows=11`, `orders=1`, `inventory=2`, `costOfCapital=1`, `posSellthrough=2`, `candidateStructures=3`, `counterOffers=0`). Supabase security advisor after DDL reports the new P3 tables as RLS-enabled with no policies, which is expected for service-role-only backend access, and still reports pre-existing `rls_disabled_in_public` errors on the older public credit source tables. Do not auto-enable RLS on those older tables without explicit policy design because it can break current source reads.
- Remote `credit_deal_scenarios` forward-schema fix: David browser e2e exposed a stale remote table shape where `credit_deal_scenarios` lacked `candidate_json`, `objective_value`, `ranked_position`, and `seed`, while its legacy `candidate_id` / `payload_json` columns were still active. Added RED schema/writer tests, updated the migration artifact with additive `alter table ... add column if not exists ...` clauses, and made the scenario writer persist both the new contract (`candidate_json`, objective/rank/seed hashes) and legacy compatibility (`candidate_id`, `payload_json`). Applied additive migration `david_credit_deal_scenarios_forward_columns`; direct Supabase scenario POST returned 201; direct backend `/credit/v2/orders/ORD-HARBOR-6534/deals` returned 200 with `max-release-85`; `npm run test:e2e:david-v2` passed with four live queries (token usages 1555 / 1431 / 1683 / 1441), audit hash `2c3c2bfc16b00538b31a2d681ccf75876c0f0362a51dac2b9da3c81447c18d4a`, screenshots `output\playwright\david-v2`.
- Current verification checkpoint: after the P3.4 prerequisite round-state slice, P0.1a policy-vector UI/e2e slice, P2.2/P2.3 eval + copilot work, reviewer hardening on rate-limit + retry lifecycle + policy-vector filtering, the `credit_deal_scenarios` persistence/forward-schema slice, P3.4 counter-offer re-optimization hardening, and the local route-level full-loop regression, focused checks are green and full `npm run verify` passed (`lint`, `typecheck`, `test` 155 files / 1316 tests, depcruise 153 modules / 531 dependencies, release readiness). `git diff --check` reports no whitespace errors (CRLF warnings only).
- Fresh Maya regression checkpoint (2026-07-10): after clearing an existing same-directory Next dev-server lock and rerunning cleanly, `npm run test:e2e:maya-real` passed. Browser proof: loading shell visible 688ms under delayed backend, source force-refresh covered canonical line IDs `S1-L1` through `S8-L1`, canonical browse saw 8 work items / 20 lines, and 5 live query scenarios produced 50 backend trace rows. Source readiness still prints `SAP OData: Probe failed` while TPM, 3PL POD, Bureau, Remittance/EDI, Contract Repo, and MCP are ready/proxy-connected; keep SAP direct-read health as an explicit pre-prod risk if SAP proof is required.
- Done locally with focused tests: policy exact-row parser/fail-closed guard (P0.1), Harbor simulation endpoint foundation (P0.2), deterministic EV/optimizer core plus `credit_deal_scenarios` API persistence (P1.2), Supabase simulated-source tables seeded for Harbor (P1.1 source tables; artifact: `docs/superpowers/plans/2026-07-09-david-credit-negotiation-supabase-migration.sql`), deal workbench endpoint/UI + browser e2e (P1.3), governed `credit_negotiation.draft_structures` MCP/live-query backend wiring (P2.1), and the first P3 email-negotiation backend slices: negotiation action resolver, `sendNegotiationEmail` primitive, dedicated David send route, server-side stored approval receipt verification, required durable send ledger, Supabase send ledger adapter, inbound signature/correlation route, durable inbound metadata table/adapter, Supabase round/contact/counter-offer adapter, retryable fail-closed body-fetch behavior, span-grounded counter parser, selected-order reset route/control, and P3 persistence schema artifact.
- Latest verification passed after the manual-paste + reviewer-fix + default live extractor slice: RED then GREEN `npm run test -- tests/unit/credit-negotiation-manual-inbound.test.ts` (1 file / 5 tests); policy-bounds/retry/message-id/subject-fallback/default-extractor RED then GREEN coverage in `npm run test -- tests/unit/credit-negotiation-inbound.test.ts` (1 file / 10 tests); focused negotiation regression `npm run test -- tests/invariants/david-credit-v2-route.test.ts tests/unit/credit-negotiation-counter-parser.test.ts tests/unit/credit-negotiation-manual-inbound.test.ts tests/unit/credit-negotiation-inbound.test.ts tests/unit/credit-negotiation-email-route.test.ts tests/unit/credit-negotiation-reset-route.test.ts tests/unit/credit-negotiation-email.test.ts` (7 files / 42 tests); `npm run lint`; `npm run typecheck`; and full `npm run test` (149 files / 1277 tests). Previous green gates still recorded for this branch: `npm run verify` (lint, typecheck, 148 files / 1268 tests, depcruise 151 modules / 523 dependencies, release readiness); `npm run test:e2e:david-v2` (4 David live LLM queries with `live_openai_agents`, token usages 1547/1423/1627/1425, audit hash `e49356ebf1cc7b4efa8de18ca52c3870811d98f5dfc2f3f54f4f57ddb1905295`, screenshots `output\playwright\david-v2`); `npm run test:e2e:shared-surfaces`; prior post-storage `npm run test:e2e:maya-real` (8 work items / 20 lines, 5 live query scenarios, 50 backend trace rows); and `git diff --check` (CRLF warnings only, no whitespace errors).
- Partially done: workbench uses backend-priced labels and synthetic badges. The deal optimizer remains pure/deterministic, and the `/credit/v2/orders/:orderId/deals` API now persists ranked deterministic outputs to `credit_deal_scenarios` with `scenario_id`, `candidate_json`, objective value, ranked position, `optimizer_run_id`, `source_hash`, `policy_hash`, and cited source record IDs. It also re-optimizes from complete grammar-valid `credit_counter_offers` rows as code-priced counter-offer candidates while rejecting malformed/incomplete grammar-valid counters and leaving human-review counters unpriced. P3.1/P3.2/P3.3 now have unit-tested infrastructure and Supabase REST adapters, including a David-authenticated manual-paste fallback that writes `credit_counter_offers(source='manual')`, does not fake email metadata, and advances the round only after span-grounded, exact-policy-bounded parsing. Signed inbound now retries a prior failed body-fetch row instead of treating it as duplicate, preserves RFC `message_id` on `credit_counter_offers` for reply continuity, supports the documented `[Recoup Deal <orderId> · Round <n>]` subject token fallback, and uses a default OpenAI Responses structured-output extractor when a test extractor is not injected. Current P3.4 prerequisite update: `loadCreditRiskRows` now reads durable `credit_negotiation_rounds` rows with fail-closed transport/schema handling; Harbor orders expose `currentRound`, `latestSentRound`, and `nextRound`; the workbench manual counter round defaults from `latestSentRound` instead of a hard-coded 1 and renders a round summary. Latest full `npm run verify` passed (154 files / 1312 tests, depcruise clean, release readiness passed). Still pending: live Resend webhook proof, live cron/provider proof, and full draft-to-send-to-inbound browser e2e.
- Superseding reviewer-hardening note: durable retry queue and rate-limit are no longer pending at the unit-tested backend layer. Signed inbound now retries prior failed/fetched-without-counter body-fetch rows safely, the retry route is cron-callable and secret-guarded, production route invocation fails closed without configured rate-limit env, and AP sender-auth is role-filtered. Current local env check: `.env.local` has `RESEND_API_KEY` plus generic Maya email vars, but does not have `CREDIT_NEGOTIATION_FROM_EMAIL`, `HARBOR_AP_CONTACT_EMAIL`, `RESEND_INBOUND_SIGNING_SECRET`, `RESEND_INBOUND_RATE_LIMIT_MAX_EVENTS`, `RESEND_INBOUND_RATE_LIMIT_WINDOW_MS`, `CREDIT_NEGOTIATION_INBOUND_RETRY_SECRET`, `CREDIT_NEGOTIATION_INBOUND_RETRY_LIMIT`, or `RECOUP_CREDIT_NEGOTIATION_RESET_ENABLED`; live send/webhook/retry/reset proof cannot be completed locally until those are configured. Still pending: live Resend webhook proof, live cron/provider proof, and full draft-to-send-to-inbound browser e2e.
- Live-email provider readiness check (refreshed 2026-07-10): Vercel project env currently has the generic Recoup/Supabase/OpenAI/Maya email keys, but not the David-specific keys `CREDIT_NEGOTIATION_FROM_EMAIL`, `HARBOR_AP_CONTACT_EMAIL`, `RESEND_INBOUND_SIGNING_SECRET`, `RESEND_INBOUND_RATE_LIMIT_MAX_EVENTS`, `RESEND_INBOUND_RATE_LIMIT_WINDOW_MS`, `CREDIT_NEGOTIATION_INBOUND_RETRY_SECRET`, `CREDIT_NEGOTIATION_INBOUND_RETRY_LIMIT`, or `RECOUP_CREDIT_NEGOTIATION_RESET_ENABLED` in prod/preview. Read-only Resend webhook listing returned one webhook with `email.received` included, but its endpoint is `https://relay-mail-client.vercel.app/api/webhooks/resend`, not the Recoup David route. `.env.example` now lists the David-specific env placeholders so the remaining provider configuration is explicit, but no secret/default values are committed. Therefore P3.0/P3.0a remain open: the Recoup route is not yet registered as the receiving endpoint, the Recoup signing secret is not yet present in Vercel env, and `HARBOR_AP_CONTACT_EMAIL` is still not seeded/configured. Do not mark live inbound complete from DNS/catch-all readiness alone.
- Fresh David browser checkpoint (2026-07-10): `npm run test:e2e:david-v2` passed against `http://127.0.0.1:4317` with audit hash `9c10d6bfffb0f5c527b1c830617daaddd57d26cdb18c2ad90164cda1783c8c55`. The suite explicitly asserts `david-signals-in` is expanded by default while `david-assessment-timeline`, `david-verdict-banner`, and `david-action-packet` are collapsed by default. Four David live-query checks passed in `live_openai_agents` mode with token usages 1538 / 1420 / 1673 / 1436 and screenshots in `output\playwright\david-v2`.
- Fresh shared/verify checkpoint (2026-07-10): after the X.1 visual remediation, `npm run test:e2e:shared-surfaces` passed on a clean rerun, then full `npm run verify` passed: `lint`, `typecheck`, Vitest 157 files / 1318 tests, depcruise 153 modules / 531 dependencies with no violations, and release readiness green.
- X.1 visual remediation checkpoint (2026-07-10): David dossier chrome was flattened, a backend-derived workflow command strip was added, action-packet rows were changed to decision-ledger rows, and the David decision flow was made responsive (`data-layout="responsive-workflow"`; no `min-w-[760px]` rail). RED/GREEN coverage added in `tests/unit/david-account-dossier.test.ts` and `tests/unit/david-decision-flow.test.ts`. Latest `npm run test:e2e:david-v2` passed against `http://127.0.0.1:4317` with audit hash `72d0a10a8b8c9b4457885c93f8917c4de433d9892cd5b63eb4a9d01f9b15eac6`, refreshed desktop/tablet/mobile screenshots, and four David live-query checks in `live_openai_agents` mode with token usages 1548 / 1432 / 1693 / 1427. Independent reviewer scored **4.55/5 PASS**; mobile decision flow no longer clips/overflows.
- Fresh P3 browser fail-closed checkpoint (2026-07-10): added the named `npm run test:e2e:david-negotiation` gate (`tests/e2e/david-negotiation-e2e.ts`) after first verifying the script was missing. The browser test logs in as David, opens Harbor's workbench, proves opening it fires no approval/email/inbound side-effect routes, verifies the backend-derived optimizer/provenance panel, proves manual counter before a sent round returns `dropped_unmatched`, proves email send before approval/live config fails closed (`503` in current local env), and proves reset is gated (`403` when local reset env is disabled). This exposed and fixed a real Next app-route bundle issue by converting negotiation parser/extractor/config imports in `src/services/creditNegotiationCounterExtractor.ts`, `src/services/creditNegotiationCounterParser.ts`, `src/services/creditNegotiationPolicy.ts`, `config/models.ts`, and `config/governed.ts` from unresolved local `.js` imports to `.ts`. Green checks: `npm run test:e2e:david-negotiation`; `npm run test -- tests/unit/credit-negotiation-full-loop.test.ts tests/unit/credit-negotiation-inbound.test.ts tests/unit/credit-negotiation-manual-inbound.test.ts tests/unit/credit-negotiation-email-route.test.ts` (4 files / 32 tests); `npm run typecheck`; `npm run lint`; full `npm run verify` (`lint`, `typecheck`, Vitest 157 files / 1318 tests, depcruise 153 modules / 531 dependencies, release readiness); `npm run test:e2e:david-v2` after the import fix (four live David LLM queries, token usages 1535 / 1421 / 1689 / 1428, audit hash `57169f12283132b216fcdacaab5b6a90026730b7e26eda02d3e1e40c256167c6`); and `npm run test:e2e:shared-surfaces`. This is browser fail-closed proof only; live Resend send/webhook proof remains pending.
- Fresh P3.0 contact seed checkpoint (2026-07-10): added tested env-driven Harbor AP contact seeding via `src/services/creditNegotiationContactSeed.ts` and operator script `npm run seed:credit-negotiation-contacts` (`scripts/seedCreditNegotiationContacts.ts`). The helper requires `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, and `HARBOR_AP_CONTACT_EMAIL`, rejects malformed email before calling Supabase, upserts only `credit_account_contacts(account_id='ACC-HAR', role='ap')`, reads the row back from Supabase after upsert, verifies the read-back email hash matches the env-provided Harbor AP contact, and returns only SHA-256 hash/read-back proof rather than the raw email. RED/GREEN coverage: `tests/unit/credit-negotiation-contact-seed.test.ts`; focused regression `npm run test -- tests/unit/credit-negotiation-contact-seed.test.ts tests/unit/credit-negotiation-inbound.test.ts tests/unit/credit-negotiation-email-route.test.ts` passed (3 files / 28 tests); read-back hardening regression `npm run test -- tests/unit/credit-negotiation-contact-seed.test.ts tests/unit/credit-negotiation-inbound.test.ts tests/unit/credit-negotiation-email-route.test.ts tests/unit/credit-negotiation-live-email-readiness.test.ts` passed (4 files / 32 tests); current local script proof fails closed with `status=missing_config` and `missing=["HARBOR_AP_CONTACT_EMAIL"]`, so no Supabase write/read-back was attempted. Latest gates after the read-back fix: `npm run lint`, `npm run typecheck`, `git diff --check` (CRLF warnings only), and full `npm run verify` passed (`lint`, `typecheck`, Vitest 161 files / 1330 tests, depcruise 154 modules / 533 dependencies, release readiness).
- Fresh P3.0/P3.0a live-email readiness checkpoint (2026-07-10): added the no-mutation operator gate `npm run check:credit-negotiation-live-email` (`scripts/checkCreditNegotiationLiveEmailReadiness.ts`) with RED/GREEN coverage in `tests/unit/credit-negotiation-live-email-readiness.test.ts`. The checker loads local runtime env, lists only missing/present variable names, verifies `RECOUP_CREDIT_NEGOTIATION_ROOT_CATCHALL_APPROVED=approved`, and read-only checks Resend webhooks for `email.received` on `/api/credit/negotiation/inbound`. Current run wrote `output/credit-negotiation-live-email-readiness.json` and exits blocked: missing David-specific live-email env (`CREDIT_NEGOTIATION_FROM_EMAIL`, `HARBOR_AP_CONTACT_EMAIL`, `RESEND_INBOUND_SIGNING_SECRET`, rate-limit/retry keys, and root catch-all approval flag), root catch-all safety confirmation is not encoded in env, and the observed Resend webhook lookup succeeded (`lookupStatus=succeeded`, `webhookCount=1`) but the only `email.received` webhook is not registered to the Recoup David inbound route. The checker now also fails closed with a sanitized `blocked_lookup_failed` status and retry action if the Resend webhook lookup throws or returns non-OK, so transient provider/API issues cannot be confused with a configured route. Reset is reported separately as local QA optional (`localQaReset.requiredForLiveEmail=false`) so production/live-email readiness does not require enabling `RECOUP_CREDIT_NEGOTIATION_RESET_ENABLED`; the reset route itself remains local-only and explicit-gated. The artifact emits three no-secret, no-mutation next actions: configure missing David live-email env, confirm/dedicate the `north-bay.dev` root catch-all then set the approval flag, and register the Recoup David inbound route as the Resend `email.received` webhook. Green checks: RED then GREEN `npm run test -- tests/unit/credit-negotiation-live-email-readiness.test.ts` (1 file / 4 tests), focused `npm run test -- tests/unit/credit-negotiation-live-email-readiness.test.ts tests/unit/credit-negotiation-reset-route.test.ts tests/unit/credit-negotiation-contact-seed.test.ts` (3 files / 10 tests), `npm run lint`, `npm run typecheck`, `git diff --check` (CRLF warnings only), and full `npm run verify` passed (`lint`, `typecheck`, Vitest 161 files / 1330 tests, depcruise 154 modules / 533 dependencies, release readiness). Do not attempt a live David negotiation send until this gate returns `ready_for_live_email_test`.
- Fresh P3.0/P3.0a inbound-route guard update (2026-07-10): tightened `npm run check:credit-negotiation-live-email` so it also performs a no-mutation `GET` probe against the configured public Recoup inbound route with redirects disabled. `405` is treated as acceptable for a deployed POST-only webhook route, but `404`, provider lookup failure, or Vercel/SSO redirect keeps the live-email gate blocked. Current run exits blocked with the same missing David env and wrong Resend endpoint blockers, plus `inboundRoute.status=blocked_missing_route` because `https://recoup-self-eta.vercel.app/api/credit/negotiation/inbound` returns `404`. Green checks after this guard: `npm run test -- tests/unit/credit-negotiation-live-email-readiness.test.ts` (1 file / 7 tests), focused adjacent regression `npm run test -- tests/unit/credit-negotiation-live-email-readiness.test.ts tests/unit/credit-negotiation-reset-route.test.ts tests/unit/credit-negotiation-contact-seed.test.ts` (3 files / 13 tests), `npm run typecheck`, `npm run lint`, and full `npm run verify` (`lint`, `typecheck`, Vitest 163 files / 1341 tests, depcruise 154 modules / 533 dependencies, release readiness).
- Fresh P3 readiness reviewer closure (2026-07-10): closed the live-email false-ready review findings with subagents plus a final read-only reviewer. Inbound now accepts Resend `email.received` metadata with array `to`, `received_for`, and `data.message_id`, scans all recipients plus subject for the token, persists safe recipient metadata, and fetches the received body from `/emails/receiving/:email_id`. Readiness now checks Resend webhook origin+path, blocks protected/missing/error public route statuses, and requires all live route dependencies: David negotiation env, shared Maya email gateway env used by `sendNegotiationEmail`, David send allowlist, cockpit human principal plus auth token, OpenAI key, Supabase, Resend, inbound signing, rate-limit/retry env, and catch-all approval flag. Focused regression passed: `npm run test -- tests/unit/credit-negotiation-inbound.test.ts tests/unit/credit-negotiation-inbound-retry.test.ts tests/unit/credit-negotiation-live-email-readiness.test.ts tests/unit/credit-negotiation-email-route.test.ts tests/unit/credit-negotiation-email.test.ts` (5 files / 57 tests). Full `npm run verify` passed (`lint`, `typecheck`, Vitest 163 files / 1352 tests, depcruise 154 modules / 533 dependencies, release readiness). Final reviewer reported no findings in the scoped fix files. Current no-mutation readiness run remains blocked: missing David live-email env and catch-all flag, malformed/non-David current `RECOUP_COCKPIT_HUMAN_PRINCIPAL`/`RECOUP_EMAIL_SEND_PRINCIPALS`, Resend webhook on the old endpoint, and public inbound route `404`. Live Resend/webhook/provider proof remains pending.
- Fresh readiness-auth correction (2026-07-10): corrected the readiness gate to match the actual David send-route auth paths instead of over-requiring the global `RECOUP_COCKPIT_HUMAN_PRINCIPAL` to be David. The gate now accepts either direct David human auth or David demo-session auth (`human:david-lead`) when `RECOUP_DEMO_SESSION_SECRET` is present and `RECOUP_EMAIL_SEND_PRINCIPALS` includes the actual David principal emitted by the route. `RECOUP_COCKPIT_AUTH_TOKEN` and `OPENAI_API_KEY` remain required. Focused readiness test passed (`tests/unit/credit-negotiation-live-email-readiness.test.ts`, 20 tests), reviewer pass found no findings, and full `npm run verify` passed (`lint`, `typecheck`, Vitest 163 files / 1355 tests, depcruise 154 modules / 533 dependencies, release readiness). Current no-mutation readiness run is still intentionally blocked: missing David live-email env/catch-all flag, current send allowlist lacks an accepted David principal, Resend webhook is still on the old endpoint, and public inbound route is still `404`.
- Fresh provider/env audit (2026-07-10): read-only local `.env.local` scan shows generic Maya/forensics email keys (`RESEND_API_KEY`, `EMAIL_TO_BILLING`, `EMAIL_TO_RECOVERY`, `SENDER_EMAIL_ADDRESS`, `RECOUP_EMAIL_SEND_PRINCIPALS`) and the policy vector-store key are present, but no David-specific negotiation keys are present. Read-only `vercel env ls` confirms Production has generic Recoup/SAP/OpenAI/Supabase/Maya email keys and Preview has the generic API/auth/Supabase keys, but neither environment lists `CREDIT_NEGOTIATION_FROM_EMAIL`, `HARBOR_AP_CONTACT_EMAIL`, `RESEND_INBOUND_SIGNING_SECRET`, `RESEND_INBOUND_RATE_LIMIT_MAX_EVENTS`, `RESEND_INBOUND_RATE_LIMIT_WINDOW_MS`, `CREDIT_NEGOTIATION_INBOUND_RETRY_SECRET`, `CREDIT_NEGOTIATION_INBOUND_RETRY_LIMIT`, or `RECOUP_CREDIT_NEGOTIATION_ROOT_CATCHALL_APPROVED`. Treat Maya email readiness and David negotiation email readiness as separate gates; do not map generic `SENDER_EMAIL_ADDRESS`/billing/recovery recipients onto David's Harbor negotiation path.
- Fresh X.2 smoke-gate correction (2026-07-10): corrected `scripts/runPostProdPublicSmoke.ts` so the production David smoke expects `david-signals-in` expanded by default and only `david-assessment-timeline`, `david-verdict-banner`, and `david-action-packet` collapsed. Added RED/GREEN guard `tests/unit/post-prod-public-smoke.test.ts` after finding the post-prod script still asserted Signals In collapsed. Follow-up RED/GREEN added the explicit warmed `/credit` slow-load gate: `measureCreditWarmLoad` warms the page, reloads `/credit`, and fails the smoke if elapsed time exceeds `RECOUP_POST_PROD_CREDIT_WARM_LOAD_BUDGET_MS` (default `5000`) with the instruction to troubleshoot David `/credit` slow loading before continuing release. Green checks: `npm run test -- tests/unit/post-prod-public-smoke.test.ts` (1 file / 2 tests), `npm run test -- tests/unit/david-account-dossier.test.ts tests/unit/david-decision-flow.test.ts` (2 files / 2 tests), `npm run lint`, `npm run typecheck`, `git diff --check` (CRLF warnings only), and full `npm run verify` passed (`lint`, `typecheck`, Vitest 161 files / 1329 tests, depcruise 154 modules / 533 dependencies, release readiness). Public/prod smoke remains pending until deployment/readiness, but the scripted expectation now matches the X.2 UI and slow-load requirements.
- Fresh local browser proof (2026-07-10): refreshed `npm run test:e2e:david-negotiation` and it passed against `http://127.0.0.1:4317` with no approval/email/inbound side effects on workbench open, governed `Draft counter` approval path visible, send-approved-email control disabled before approval, manual counter before a sent round returning `dropped_unmatched`, email send before approval/live config failing closed with HTTP `503`, reset gated with HTTP `403`, and screenshots in `output\playwright\david-negotiation`. Refreshed `npm run test:e2e:david-v2` also passed with four David live LLM account scenarios in `live_openai_agents` mode, source read mode `live_sdk_mcp`, handoff count `1` per scenario, token usages `1540` / `1425` / `1681` / `1441`, audit hash `117b7ca8e1e7a2760db288f1f89e7207b88534e17f556b1766f48e42d52cd23d`, and screenshots in `output\playwright\david-v2`. This refresh proves local browser + David live-query readiness after the X.2 smoke-script correction and reviewer-gap work; it still does not satisfy the live Resend/webhook proof.
- Fresh reviewer-gap closure (2026-07-10): closed three local-only P3 gaps found by read-only review. First, `sendNegotiationEmail` now marks the durable `credit_negotiation_sends` row `failed` when Resend rejects delivery or returns no message id; the failed row stores no provider error detail and repeat approval stays fail-closed instead of leaving a permanent `pending` row. Second, added deterministic inbound counter-offer eval harness `evals/creditNegotiationCounterEval.ts` plus `tests/unit/credit-negotiation-inbound-eval.test.ts`; it evaluates sampled Harbor counter emails through the same parser/policy path for cited-span grounding, grammar validity, no model-computed terms, policy bounds, human-review adversarial routing, and raw-output suppression. Third, the David workbench now exposes the governed UI path for the backend-selected top deterministic candidate: `Draft counter` opens the existing David approval gate for `credit-v2:negotiation:<orderId>:r<nextRound>`, and `Send approved email` calls the dedicated `/api/credit/negotiation/email` route only after approval is recorded. Browser proof was refreshed so opening the workbench still has no side effects and the send button is disabled before approval. Green checks: RED/GREEN `npm run test -- tests/unit/credit-negotiation-email.test.ts tests/unit/credit-negotiation-email-route.test.ts`; RED/GREEN `npm run test -- tests/unit/credit-negotiation-inbound-eval.test.ts`; RED/GREEN `npm run test -- tests/unit/david-negotiation-workbench.test.ts`; focused regression `npm run test -- tests/unit/david-negotiation-workbench.test.ts tests/unit/credit-negotiation-email.test.ts tests/unit/credit-negotiation-email-route.test.ts tests/unit/credit-negotiation-inbound-eval.test.ts tests/unit/credit-negotiation-counter-parser.test.ts tests/unit/credit-negotiation-live-email-readiness.test.ts` (6 files / 30 tests); `npm run test:e2e:david-negotiation`; `npm run test:e2e:david-v2`; `npm run typecheck`; `npm run lint`; `git diff --check` (CRLF warnings only); and full `npm run verify` passed (`lint`, `typecheck`, Vitest 162 files / 1336 tests, depcruise 154 modules / 533 dependencies, release readiness).
- Fresh P3 local hardening closure (2026-07-10): closed the latest subagent/reviewer findings before live-email progression. `sendNegotiationEmail` now marks `credit_negotiation_sends` as `failed` for provider exceptions and non-OK provider responses, returns only sanitized generic errors, and does not expose provider response bodies, recipients, secrets, or approved draft text. The inbound route now requires `RESEND_INBOUND_SIGNING_SECRET` to use the same `whsec_` base64 shape as readiness before any reads/fetches/writes. Manual/live/retry inbound paths now fail closed on missing persistence seams, zero-row Supabase metadata/round updates, and downstream persistence failures; retry metadata is restored to `failed` when later counter/round writes fail, and duplicate/retry paths repair a matching grammar-valid counter when the round is still `sent` without inserting a duplicate or refetching provider body. David negotiation UI now hydrates send eligibility from the exact durable drafted action id, keeps local approval state action-id scoped, and uses draft-approval copy (`Approve draft counter?`) instead of implying the approval click sends email. Raw Supabase `TypeError("fetch failed")` on required credit-risk table reads now fails closed as JSON `503` with table-scoped `missingSource` instead of HTML `500`. Green checks: focused regression `npm run test -- tests/unit/credit-negotiation-inbound.test.ts tests/unit/credit-negotiation-inbound-retry.test.ts tests/unit/credit-negotiation-manual-inbound.test.ts tests/unit/credit-negotiation-email.test.ts tests/unit/credit-negotiation-email-route.test.ts tests/unit/david-negotiation-workbench.test.ts tests/unit/credit-v2-api.test.ts` (7 files / 94 tests), `npm run test:e2e:david-negotiation` (`ok: true`, screenshots `output\playwright\david-negotiation`), and full `npm run verify` (`lint`, `typecheck`, Vitest 163 files / 1380 tests, depcruise 154 modules / 533 dependencies, release readiness). Final reviewers reported no findings on signing-secret, provider-leak, source-wrapper, and retryability fixes. Live Resend send/webhook/provider proof remains pending under P3.0/P3.4.
- Fresh preview exposure check (2026-07-10): pushed draft PR #6 from `codex/david-dynamic-negotiation` (`14d45ac`) and deployed a non-prod Vercel preview from a clean worktree. Preview `/credit` returned HTTP 200, proving the branch can build outside the dirty development worktree, but `/api/credit/negotiation/inbound` is protected by Vercel deployment protection (`GET` redirects to SSO; unsigned `POST` returns protected-deployment 401). Therefore the preview is not usable as the Resend webhook target unless deployment protection is explicitly disabled or a safe bypass is configured. Stable production alias still requires owner-approved prod movement before the inbound route can be live-webhook-ready.
- Fresh operator handoff (2026-07-10): added no-secret live-email runbook `docs/superpowers/plans/2026-07-10-david-live-email-operator-runbook.md`. It captures the remaining P0/P3 owner/provider gates, required env variable names, Resend webhook endpoint/event, Harbor contact seed command, pre-live verification order, live smoke sequence, abort conditions, reset boundaries, and post-prod public smoke expectations. It intentionally contains no secret values and does not mutate provider state.
- Fresh P3.0 local env/contact checkpoint (2026-07-10): after owner supplied the David sender address and Harbor AP recipient, local ignored `.env.local` was configured with `CREDIT_NEGOTIATION_FROM_EMAIL`, `HARBOR_AP_CONTACT_EMAIL`, conservative test guardrails (`RESEND_INBOUND_RATE_LIMIT_MAX_EVENTS=10`, `RESEND_INBOUND_RATE_LIMIT_WINDOW_MS=60000`, `CREDIT_NEGOTIATION_INBOUND_RETRY_LIMIT=3`), a generated local retry secret, `RECOUP_CREDIT_NEGOTIATION_ROOT_CATCHALL_APPROVED=approved`, and a David demo principal added to `RECOUP_EMAIL_SEND_PRINCIPALS` without removing Maya. The Harbor AP contact seed then passed with Supabase read-back (`status=seeded`, `rows=1`, `readBack=true`, hash-only proof). Full `npm run verify` passed afterward (`lint`, `typecheck`, Vitest 163 files / 1380 tests, depcruise 154 modules / 533 dependencies, release readiness). Latest live-email readiness is now blocked only by `RESEND_INBOUND_SIGNING_SECRET`, the Resend `email.received` webhook not being registered to the Recoup David inbound route, and the public Recoup inbound route still returning `404`.
- Corrected prod-approval boundary (2026-07-10): owner clarified **do not move to production without explicit approval**. No code deploy happened, and no Resend webhook was registered or modified. David-specific Vercel Production environment variable names were staged before this corrected boundary, with values never printed or committed; owner must explicitly choose whether to keep those staged env keys pending deploy approval or remove them before any further production/provider work.
- Explicitly not done yet: full P3 email negotiation release proof. Do not claim "email negotiation complete" until P3.0 live readiness is green and P3.4 passes with live Resend send, live inbound webhook, remote Supabase read-back, LLM parser/eval proof, and browser e2e. The local route-level loop and local browser approval/send proof are green, but they are not substitutes for live Resend/webhook/browser proof.
- Still pending before prod movement: remaining P3 live-email/e2e work and X.2 David/Maya browser smoke including the `/credit` slow-load troubleshooting gate. Fresh Maya e2e passes with Supabase proxy sources, but SAP OData source readiness prints `Probe failed`; treat that as a source-health risk to investigate before prod movement if SAP direct-read proof is required for the release.

## Master checklist

- [x] **P0.0** research-backed `credit_negotiation_policy` candidate documented; owner accepts or overrides the values before seeding (bounds, default probs, collateral/financing limits) — blocking, no invented defaults
  - Status 2026-07-10: owner approved the researched candidate values in chat after the explicit input request. No values were invented or changed during approval capture.
- [x] **P0.1** `credit_negotiation_policy` + grammar bounds seed (from P0.0 values; fail-closed on missing key)
  - Status 2026-07-10: exact-row parser, fail-closed guard, and seed artifact are implemented/tested locally, and the reproducible readiness gate is now `npm run check:credit-negotiation-policy`. Runtime policy parsing now rejects generic `human:*` approval markers; active executable rows must carry the exact owner acceptance marker used by the seed artifact. Owner approval received for the candidate values, and read-only Supabase proof returned `remote_policy_seed_ready` with 11 active rows, policy version 1, policy hash `748b6a452224a294113f0560747db867f51340a957a83722ccbf2854d56b9e16`, and all 11 expected `credit_negotiation_policy:*:v1` source record IDs.
- [x] **P0.1a** policy rationale/owner-approval docs indexed for vector search; copilot policy Q&A cites vector records, but optimizer reads constants only from exact `credit_negotiation_policy` rows
  - Status 2026-07-10: live no-mutation readiness gate `npm run check:credit-negotiation-policy-vector` now proves the configured OpenAI policy vector store returns current hash/version rationale records for all 11 executable policy keys. Keep executable policy values anchored to exact `credit_negotiation_policy` rows; vector search is rationale/citation only.
- [x] **P0.2** `creditSimulationModel` + `POST /credit/v2/simulate` (reuse `computePartialHold`/`proposeTerms`)
- [x] **P0.3** "Simulate alternatives" enabled in dossier + sensitivity line + e2e (no approval fires)
- [x] **P1.1** sim tables (`credit_orders`, `sim_3pl_inventory`, `sim_cost_of_capital`, `sim_pos_sellthrough`, `credit_deal_scenarios`) + seed
- [x] **P1.2** new `core/dealExpectedValue.ts` (Decimal, seeded EV/grid — NOT `reconstructExpectedPosition`) + `dealOptimizer` persisting `source_hash`/`policy_hash`/`source_record_ids`/`optimizer_run_id` + golden/determinism/replay tests
- [x] **P1.3** `GET /credit/v2/orders/:id/deals` + workbench UI + synthetic badges + e2e
- [x] **P2.1** `credit_negotiation.draft_structures` LLM tool (structure-params-only, Zod) + engine pricing/rejection
- [x] **P2.2** LLM eval harness (grammar adherence, no-dollars, citation-scope, rejection, downstream determinism)
- [x] **P2.3** copilot "draft an option" UI (agent-drafted vs engine-generated, rejection reasons) + e2e
- [ ] **P3.0** `credit_account_contacts` seeded from env (`HARBOR_AP_CONTACT_EMAIL`); register Resend `email.received` webhook → deployed route + `RESEND_INBOUND_SIGNING_SECRET` in env
  - Status 2026-07-10: local contact seed is complete with hash-only Supabase read-back proof after owner supplied the David sender and Harbor AP recipient. P3.0 remains open because live readiness still requires the deployed public inbound route, the Resend `email.received` webhook registered to that route, and the resulting `RESEND_INBOUND_SIGNING_SECRET` stored in the target environment. No live David email send is approved until owner explicitly approves production movement and the readiness gate returns `ready_for_live_email_test`.
- [x] **P3.0a** before any live-email/prod test, confirm `north-bay.dev` root catch-all is safe to dedicate to Resend receiving (no real mailbox depends on the root MX)
  - Status 2026-07-10: owner approved the root catch-all safety gate in chat after the explicit input request. A temporary-env readiness run with `RECOUP_CREDIT_NEGOTIATION_ROOT_CATCHALL_APPROVED=approved` removed the catch-all blocker and reported `catchAllSafety.status=approved`; local env now encodes that approval and Harbor contact seed/read-back is complete. P3.0 remains open because Resend still points `email.received` to the old relay-mail-client endpoint rather than the Recoup David inbound route, the public Recoup inbound route currently returns `404` on the stable alias, and the one-time `RESEND_INBOUND_SIGNING_SECRET` is not available until webhook registration. Do not send live David negotiation traffic until the Recoup deployed route is exposed publicly, the route is registered in Resend, its signing secret is stored in the target environment, the readiness gate returns `ready_for_live_email_test`, and owner explicitly approves production/provider movement. `RECOUP_CREDIT_NEGOTIATION_RESET_ENABLED` is intentionally not required for live email; keep it local-QA-only for fresh manual tests.
- [x] **P3.1** rounds/counter-offers tables (w/ idempotency uniques) + **`findPendingAction` `credit-v2:negotiation:` branch ordered BEFORE the generic `credit-v2:` branch** (RED test: negotiation id resolves; generic packet id still resolves)
  - Status 2026-07-10: resolver branch + red/green unit test done; schema artifact declares durable P3 tables; additive remote migration `david_negotiation_p3_communication_tables` applied and read-back is green for rounds, sends, counter-offers, inbound emails, and contacts. Harbor AP contact seed now has hash-only Supabase read-back proof.
- [x] **P3.2** new **`sendNegotiationEmail` primitive** (explicit `to`/`from`/`replyTo`/`headers`, reusing the emailGateway hash/idempotency/error-handling internals but not `sendResendEmail` directly) + dedicated **`POST /api/credit/negotiation/email`** route (NOT Maya's `.strict()` forensics route): david-gated, idempotent, `credit_negotiation_sends` written, own schema + tests + approval + audit
  - Status 2026-07-10: source implementation complete and locally verified. Primitive, David route, Maya-auth rejection, Supabase send-ledger adapter, typed approved-draft readback from `credit_negotiation_rounds`, hash-only approval memory receipt, and Maya email regression tests are green. Reviewer hardening now marks the durable send row `failed` when provider delivery fails, omits a message id, throws, or returns non-OK; outward errors are sanitized and do not expose provider bodies, recipients, secrets, or approved draft text. Browser path exposes `Send approved email` only after exact-action David approval. Real Resend send remains gated under P3.0/P3.4 live readiness.
- [x] **P3.3** inbound route: `node:crypto` HMAC verify (svix `v1`, `whsec_` base64-decode, multi-sig, ±5min tolerance, `timingSafeEqual`) + test vectors → plus-token correlate → sender-auth vs contact → Received-Emails-API body fetch w/ retry → **LLM cited-spans-only + code dollar-extraction (no model dollars → else human review)** → round advance; catch-all fail-closed + rate-limit; manual-paste fallback
  - Status 2026-07-10: source implementation complete and locally verified. Raw signature verify now requires `whsec_` base64 shape before reads/fetches/writes; plus-token/subject-token correlation, AP-role sender auth, body fetch, failed/fetched-without-counter replay, Supabase inbound adapter, counter parser, exact policy-bound validation, RFC `message_id` continuity on counter offers, grammar-valid round advancement, manual-paste fallback, default OpenAI structured-output extractor, production fail-closed rate-limit config, catch-all/sender/sender-domain rate-limit buckets, and cron-callable durable retry route unit tests are green. Persistence hardening prevents false `countered` success on missing seams, zero-row updates, metadata failures, insert failures, and round-mark failures; retry metadata is restored to `failed` where needed, and duplicate/retry repairs grammar-valid existing counters with stale `sent` rounds without duplicate insert/refetch. Manual fallback writes only `credit_counter_offers(source='manual')` plus round status, not fake `credit_negotiation_inbound_emails` rows. Deterministic inbound eval covers sampled counter emails, no model-computed terms, cited spans, adversarial out-of-scope routing, and policy bounds. Real-provider extractor proof is complete locally; live Resend webhook proof and live cron/provider proof remain gated under P3.0/P3.4.
- [ ] **P3.4** full negotiation e2e (draft→approve→send→inbound→re-optimize→re-counter) + idempotency + HITL assertions
  - Status 2026-07-10: backend re-optimization prerequisite is TDD-green and reviewer-hardened through complete/malformed/incomplete/human-review counter-offer cases, order-scoped replay evidence, and fail-closed `credit_negotiation_rounds` reads. Local route-level full-loop proof is green through approved draft -> default Supabase outbox send -> duplicate-send idempotency -> manual counter -> persisted-row re-optimization into a cited `counter-offer:<counterOfferId>` candidate. The named browser gate `npm run test:e2e:david-negotiation` is green for workbench no-side-effects, optimizer/provenance render, governed top-candidate `Draft counter` approval path, exact-action approval dialog (`Approve draft counter?`), send-approved-email disabled before approval, send-approved-email enabled after approval, email POST captured, success UI, manual-counter fail-closed before a sent round, email-send fail-closed before approval/live config, and reset gating. Raw Supabase transport failures on `/credit/v2` now fail closed as JSON 503 rather than blocking the browser path with HTML 500. Full live Resend/webhook/browser loop remains pending.
- [x] **P3.5** fresh-test reset switch: David workbench control + `POST /api/credit/negotiation/reset` clears only selected-order communication rows and matching `credit-v2:negotiation:<orderId>:` approval receipts; preserves source/policy rows and generic David credit approvals
- [x] **P4** (optional) sentinel re-underwrite + accepted-deal staged settlement draft + CFO summary
  - Status 2026-07-10: deferred/skipped by owner for the current demo path because it is not critical to David live email negotiation readiness. Do not implement P4 in this branch unless owner reopens scope with an accepted-deal settlement mini-spec.
- [x] **X.1** provenance matrix extended; `npm run verify` + all e2e + LLM evals green; visual ≥4.5/5
  - Status 2026-07-10: PASS locally. Latest full `npm run verify` passed after the P3 local hardening pass (`lint`, `typecheck`, Vitest 163 files / 1380 tests, depcruise 154 modules / 533 dependencies, release readiness). `npm run test:e2e:david-negotiation` passed with local approval/send UI proof, prior `npm run test:e2e:david-v2` passed with four live David LLM queries, `npm run test:e2e:shared-surfaces` passed after clean rerun, and `npm run test:e2e:maya-real` passed. Visual reviewer scored 4.55/5 with refreshed desktop/tablet/mobile evidence. Remaining public/prod smoke stays under X.2.
- [ ] **X.2** David UI e2e asserts **Signals in** is expanded by default, and prod/browser smoke includes `/credit` slow-load troubleshooting if warmed load exceeds 5s
  - Status 2026-07-10: local David e2e assertion is green, and `smoke:post-prod` now asserts the same drawer defaults for public smoke (`Signals in` expanded; assessment/verdict/action collapsed). The post-prod script also enforces the warmed `/credit` budget through `RECOUP_POST_PROD_CREDIT_WARM_LOAD_BUDGET_MS` (default `5000`) and stops with a David `/credit` slow-loading troubleshooting message when exceeded. Keep X.2 open because the actual public/prod smoke still requires production deployment/readiness.

## Open questions (non-blocking)

1. ~~Inbound Harbor address/mailbox~~ **OWNER SAFETY APPROVED; LOCAL CONFIG DONE; LIVE PROVIDER PROOF PENDING:** `north-bay.dev` root catch-all (Resend receiving verified 2026-07-09); inbound `deals@north-bay.dev` + plus-tokens; Harbor contact = owner Gmail from env. Owner approved the root catch-all safety gate on 2026-07-10, local env now encodes `RECOUP_CREDIT_NEGOTIATION_ROOT_CATCHALL_APPROVED=approved`, and Harbor contact seed/read-back is complete. Live-email/prod testing still needs explicit production movement approval, public route exposure, Resend webhook registration, and `RESEND_INBOUND_SIGNING_SECRET`.
2. ~~Deal-grammar bounds — start from defaults~~ **RESOLVED:** a research-backed `credit_negotiation_policy` candidate exists, owner accepted it on 2026-07-10, and read-only Supabase proof returned 11 active policy rows with the current policy hash.
   - Current proof boundary: `Resend receiving verified` here means root catch-all/domain readiness only. Live app-side webhook/provider proof and full browser e2e remain pending.
3. ~~Should P4 (continuous re-underwrite) be in-scope for the demo, or narrated?~~ **RESOLVED/DEFERRED:** owner approved skipping P4 for the current demo path on 2026-07-10. Keep it narrated/deferred unless owner reopens with a settlement mini-spec.
